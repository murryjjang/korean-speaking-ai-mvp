# 단계 19.12 — 단계 19.11 잔여 2건 정정 보고서

## 개요

단계 19.11 검증에서 사용자 시각 검증으로 확인된 미해결 1건(#3 괄호 bidi)과 신규 발견 1건(발표 페이지 라벨 누락) 정정.

- **브랜치**: `feat/q4-llm-provider`
- **커밋 범위**: `bebff9c` (단계 19.11 종료) → `40d1fdd` (단계 19.12 발표 라벨)
- **vitest**: 1084 → 1087 (신규 단위 3건 추가, 회귀 0건)
- **신규 스모크**: stage1912-bidi-completed (1건), stage1912-presentation-labels (2건)
- **시각 검증 스크린샷**: 5건 (요구 4건 이상 달성)

---

## 페이즈 0 — 정확 진단

### #3 괄호 bidi — 단계 19.11 처리가 효과 없었던 원인

**단계 19.11 페이즈 3 실제 처리**:
1. `src/components/ui/localized.tsx` — 보조 영역 span에 `unicode-bidi: isolate`
2. `app/research/student/progress/page.tsx:322-324` — 외부 괄호 컨테이너 `dir="ltr"` + `unicode-bidi: isolate`

**원인 진단**:

`<Localized>` 컴포넌트의 **inline 미지정 기본 분기**가 보조 영역을 `block mt-0.5` 클래스로 렌더한다 (`src/components/ui/localized.tsx:70-71`).

```tsx
const supClass = prominent
  ? `${inline ? 'ml-1.5' : 'block mt-0.5'} text-text-secondary`
  : `${inline ? 'ml-1.5' : 'block mt-0.5'} text-xs text-text-muted opacity-80 leading-snug`
```

`progress/page.tsx`의 호출은 `inline` prop을 안 넘겨 보조 텍스트가 `display:block`으로 렌더된다. 실제 DOM:

```html
<span dir="ltr" style="unicode-bidi: isolate">
  (
  <span>
    <span lang="ko">완료</span>
    <span class="block mt-0.5 ..." dir="rtl" style="unicode-bidi: isolate">
      مكتمل                        ← BLOCK!
    </span>
  </span>
  )
</span>
```

block 자식이 인라인 컨텍스트를 끊는다 — `(완료` 한 줄 + `مكتمل` 블록 한 줄 + `)` 또 한 줄. **단계 19.11의 `dir`/`isolate`는 bidi 재정렬은 차단했으나 block-level 줄바꿈은 손대지 않았다.** RTL/LTR 무관 순수 CSS 레이아웃 문제.

### 발표연습 라벨 누락

`app/student/presentation-practice/presentation-practice-client.tsx` 23개 라벨이 한국어 단독:

| # | 라벨 | 종류 |
|---|---|---|
| 1 | 녹음 중 | inline |
| 2 | 발표 종료 | 버튼 |
| 3 | 다시 녹음 | 버튼 |
| 4 | 목표 시간 도달 / 10초 전 / 30초 전 | 알림 (3건) |
| 5 | 교정 톤 | 라벨 |
| 6 | 격식체/일반체/친근체 | option (3건) |
| 7 | 친근체 톤 힌트 | 설명문 |
| 8 | 샘플 원고 불러오기 | 버튼 |
| 9 | 교정 중... | 버튼 상태 |
| 10 | AI 원고 교정하기 | 버튼 |
| 11 | AI 원고 교정 결과 | 카드 제목 |
| 12 | 교정문 섀도잉 | 카드 제목 |
| 13 | 교정된 발표문을... | 설명문 |
| 14 | 재생 중 | 배지 |
| 15 | 교정문 듣기 | 버튼 |
| 16 | 천천히 듣기 | 버튼 |
| 17 | 보통 속도로 듣기 | 버튼 |
| 18 | 정지 | 버튼 |
| 19 | 재생 속도 | 라벨 |

총 23개 키. 단계 19.10 페이즈 3 라벨 확장이 발표 페이지에 부분만 적용된 결과.

**진단 산출물**: `/tmp/단계19.12/diagnosis.md`

---

## 페이즈 1 — #3 괄호 bidi 정정 (커밋 c2bc1f9)

### 수정

**Localized.tsx**:
- `bareSupplement` prop 추가
- `inline=true && bareSupplement=true` 시 자동 괄호 비부여
  ```tsx
  {inline && !bareSupplement ? `(${supplement})` : supplement}
  ```

**progress/page.tsx**:
- `<Localized inline bareSupplement spec={...} />` 사용
- 외부 `<span dir="ltr" style="unicodeBidi: isolate">(...)</span>` 컨테이너는 추가 안전망으로 유지

**렌더 결과** (P043 ar):
- 이전: `(완료` + `\n مكتمل \n` + `)` 줄바뀜
- 이후: `(진행 중 قيد التقدم)` 한 줄 (배지 boundingBox 95×19px)

### 회귀 차단

- **신규 단위 3건**: `tests/unit/stage1911-bidi-isolation.test.ts`에 `[단계19.12-#3]` describe 블록 추가
- **신규 스모크 1건**: `tests/smoke/stage1912-bidi-completed.spec.ts` — 배지 height < 28px 강제
- 단계 19.11 단위 4건 regex 완화 (Localized의 추가 prop 허용)

### 시각 검증 ✅

- **스크린샷**: `/tmp/단계19.12/screenshots/02-badge-P043-ar-completed-only.png`
  - 95×19px (한 줄 높이 — 19px = text-xs 라인 박스)
  - `(진행 중 قيد التقدم)` 정상 표시

---

## 페이즈 2 — 발표연습 라벨 보강 (커밋 40d1fdd)

### 수정

**dashboard-labels.ts**:
- `PRACTICE_LABELS`에 신규 키 23종 추가 — 7개 언어 (ko/en/vi/ar/th/ms/km)
- 키 네이밍 컨벤션: `presentation_*` 일관

**presentation-practice-client.tsx**:
- 단순 라벨: `<Localized inline spec={...} motherTongueHint={motherTongue} />` 패턴
- 카드 제목: KO 한국어 fragment + `<Localized supplementOnly className="ml-1.5 ..." />` 패턴
- HTML `<option>` 텍스트 노드 제약: `getToneLabel(tone, motherTongue)` 헬퍼로 KO + supplement 단일 문자열 합성 (예: `"격식체 (-습니다, -입니다) — Formal"`)

### 회귀 차단

- **신규 스모크 2건**: `tests/smoke/stage1912-presentation-labels.spec.ts`
  - P043 (ar) — 8개 핵심 아랍어 라벨 DOM 노출
  - P041 (en) — 7개 핵심 영어 라벨 + Playback Speed (CSS uppercase 케이스 무관)

### 시각 검증 ✅

- **P043 (ar)** — 페이지 상단 + 교정 카드 + 섀도잉 카드 3장:
  - `/tmp/단계19.12/screenshots/03-presentation-P043-ar-top.png`
  - `/tmp/단계19.12/screenshots/04-presentation-P043-ar-correction.png`
  - `/tmp/단계19.12/screenshots/05-presentation-P043-ar-shadowing.png`

  교정 톤 (نبرة التصحيح), 격식체 (-습니다, -입니다) — رسمي,
  샘플 원고 불러오기 (تحميل نص نموذجي),
  AI 원고 교정하기 (تصحيح النص بالذكاء الاصطناعي),
  교정문 섀도잉 (تظليل النص المصحَّح),
  교정문 듣기 (استمع إلى النص المصحَّح),
  천천히 듣기 (استمع ببطء),
  보통 속도로 듣기 (استمع بسرعة عادية),
  재생 속도 (سرعة التشغيل) — 모두 정상 표시 확인.

- **P041 (en)** — 교정 카드 + 섀도잉 카드 2장:
  - `/tmp/단계19.12/screenshots/06-presentation-P041-en-correction.png`
  - `/tmp/단계19.12/screenshots/07-presentation-P041-en-shadowing.png`

  Correction tone, Load sample script, AI script correction,
  Corrected Script Shadowing, Listen to corrected, Listen slowly,
  Listen at normal speed, Playback speed — 모두 정상.

---

## 페이즈 3 — 최종 검증

### 종합 회귀 결과

| 검증 | 결과 | 비고 |
|---|---|---|
| `npx vitest run` | ✅ 1087/1087 | 신규 19.12 단위 3건 포함 |
| `npx eslint` | ✅ 0 errors | 1 pre-existing warning (무관) |
| `npm run build` | ✅ 통과 | 모든 라우트 정상 컴파일 |
| 신규 스모크 stage1912-bidi-completed | ✅ 1/1 | 배지 한 줄 표시 강제 |
| 신규 스모크 stage1912-presentation-labels | ✅ 2/2 | ar/en 라벨 DOM 노출 |

### 시각 검증 의무 이행 자체 점검

| 페이즈 | 요구 | 실제 | 충족 |
|---|---|---|---|
| #3 | 1장 | 1장 (`02-badge-P043-ar-completed-only.png`) | ✅ |
| 발표라벨 ar | 1~2장 | 3장 (top + correction + shadowing) | ✅ |
| 발표라벨 en | 1장 | 2장 (correction + shadowing) | ✅ |
| **합계** | **4건** | **6건** | ✅ |

자동 스모크가 아니라 **실제 화면 PNG 캡처**로 검증함. 단계 19.11에서 자동 스모크는 통과했으나 실제 화면 깨짐이 발견된 교훈을 반영해, 본 단계는:
- 페이즈 1 정정 효과를 PNG 95×19px 픽셀 측정으로 확인
- 페이즈 2 라벨 노출을 ar/en 두 언어 × 페이지 본문 3 영역 PNG로 확인

### 자체 검증 우회 차단 자체 점검

- ✅ 실제 사용자 경로 사용 (P043 ar, P041 en로 로그인 후 실제 페이지 진입)
- ✅ 단위 테스트 우회 시도 없음 (regex 완화는 19.11 케이스 보존 + 19.12 신규 케이스 추가)
- ✅ smoke 우회 시도 없음 (배지 boundingBox 측정 + DOM 텍스트 검색은 실제 렌더 결과 검증)

---

## 단계 19.10 · 19.11 · 19.12 종합 — 시험운영 직전 시스템 상태

### 다국어 보조 표기 시스템 (mother_tongue 단일 진실원)

| 영역 | 단계 | 상태 |
|---|---|---|
| 사이드바 라벨 | 19.8 | ✅ |
| 카드/페이지 제목 | 19.9 페이즈 4-5 | ✅ |
| 자유대화 본문 | 19.10 페이즈 3 | ✅ |
| 읽기연습 본문 | 19.10 페이즈 3 | ✅ |
| 발표연습 본문 (1차) | 19.10 페이즈 3 | ⚠️ 부분 (단계 19.12에서 보강) |
| 발표연습 본문 (보강) | 19.12 발표라벨 | ✅ |
| 말하기평가 본문 | 19.10 페이즈 3 | ✅ |
| PDF 피드백 불릿 | 19.10 페이즈 2 | ✅ |
| /research/login 단순화 | 19.11 페이즈 2 | ✅ |
| mother_tongue 단일 진실원 검증 | 19.11 페이즈 1 | ✅ |
| RTL/LTR 괄호 bidi (1차) | 19.11 페이즈 3 | ⚠️ 부분 (단계 19.12에서 정정) |
| RTL/LTR 괄호 bidi (정정) | 19.12 #3 | ✅ |

### 시험운영 가능 판정

- ✅ 7개 언어 mother_tongue 전 화면 보조 표기 일관 적용
- ✅ RTL/LTR 혼합 괄호 안전 표시
- ✅ vitest 1087 통과, build/lint clean
- ✅ 시각 회귀 차단 자동 스모크 + 측정 기반 검증 완비

### 알려진 제약

- HTML `<option>` 텍스트 노드 제약: `getToneLabel` 헬퍼로 우회 (KO + supplement 단일 문자열). 향후 커스텀 dropdown 컴포넌트 도입 시 일반 BilingualText 패턴으로 통일 가능.
- `재생 속도 (Playback Speed)` 라벨 CSS uppercase 변환: 사용자에게 시각적으로 대문자로 보임. 의도된 디자인 일관성 (다른 uppercase 라벨과 동일).

---

## 시그너처

- **검토 환경**: linux 6.6.87.2-microsoft-standard-WSL2, Node `npx`
- **검증 시각**: 2026-05-19 (KST)
- **dev 서버**: localhost:3099 (SMOKE_TEST_MODE=1)
- **참가자 시드**: P040 ko / P041 en / P042 vi / P043 ar / P044 th / P045 ms / P046 km
