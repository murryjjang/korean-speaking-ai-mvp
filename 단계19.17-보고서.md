# 단계 19.17 보고서 — 환각 차단 임계값 조정 + Pause 활용 진단·보강

- **기간**: 2026-05-20 (페이즈 0 → 3, 약 1시간)
- **베이스라인**: vitest 1142 통과, 단계 19.16 종료
- **최종**: vitest 1167 통과 (+25 신규), lint 0 errors, next build OK, playwright API 7/7
- **브랜치**: `feat/q4-llm-provider`
- **커밋**: `7858d40` (페이즈 0) · `6813500` (페이즈 1) · `3e8a7e7` (페이즈 2) · 본 보고서

---

## 0. 시작 배경

단계 19.16 시연(자유 대화, 친구 페르소나, 발음 평가 ON)에서 두 가지 회귀 발견:

1. **60점 환각 칭찬** — 학습자가 의도적으로 어색하게 "음...그....항쿡 음쒹 마시쒀요"
   발화. STT는 "한국 음식 맛있어요"로 재인식하고 발음 점수 60. LLM 피드백 칸에
   `✓ 자연스럽게 잘 말씀하셨어요` 평면 칭찬 노출.
2. **멈춤 미언급** — 학습자의 명확한 "음.. 그.." 멈춤이 LLM 응답에서 한 마디도 반영 X.

(별도 STT 환각 — gpt-4o-mini-transcribe도 "이 김밥 좋아요"로 잘못 인식한 모델 자체
한계는 1차 시험운영 후 Naver Clova PoC로 별도 처리.)

---

## 1. 페이즈 0 — 진단

### 발견 1 — 60점 환각 칭찬

| 지점 | 위치 | 동작 |
|---|---|---|
| UI 칭찬 라벨 | `app/student/conversation-practice/free-conversation-client.tsx:1384~1403` | `correction.corrected===original` 시 `✓ {reason}` |
| reason LLM 출력 | `src/lib/llm/build-persona-system-prompt.ts:147` | grammar 오류 없으면 `reason="자연스럽게 잘 말씀하셨어요."` 고정 |
| reason 서버 폴백 | `app/api/conversation/free/respond/route.ts:170, 314, 335` | LLM 누락 시 동일 문구로 덮어씀 |
| 단계 19.16 발음 가이드 | `build-persona-system-prompt.ts:320~326` | `npc_response` 톤만 규율 (50점 미만 칭찬 금지) |

근본 원인: 단계 19.16의 발음 가이드는 `npc_response`에 한정. `learner_correction.reason`
칭찬 텍스트는 발음 점수와 무관하게 동일 문구로 노출됨.

### 발견 2 — Pause 미언급 시나리오 확정

| 시나리오 | 판정 | 근거 |
|---|---|---|
| B (데이터 미전달) | ❌ 아님 | `free-conversation-client.tsx:803~820`이 `buildSpeechFlowContext` 결과를 `respond` API에 정상 전달. parser(`route.ts:62~91`)도 정상. |
| C (임계값 부적절) | ⭐⭐ 주요 | `PAUSE_SHORT_MS=800, PAUSE_LONG_MS=1500` (`pronunciation-context.ts:19~20`). 학습자 "음...그..." 단어 간 gap이 500~700ms이면 감지 자체가 안 됨 → 시스템 프롬프트 블록 미포함. |
| A (LLM 무시) | ⭐ 부분 | `build-persona-system-prompt.ts:351~355` "shortPauseCount≤1 → 별도 언급 X" strict 가이드. |

### 단계 19.13~16 효과 보존 확인

- `feedback_inputs_version` 메타 — 키 분리로 보존(`stage19.16` → `stage19.17`).
- 친구 페르소나 톤, `antiHallucinationBlock`, 시제·수사 교정 — 미변경.
- ⚠ PAUSE 임계 변경의 단계 19.13 `llm-eval` 영향 점검 — `evaluateSpeakingDetail`은
  `longPauses/shortPauses` 객체를 직접 받으므로 임계 영향 X (페이즈 2에서 재검증).

**산출**: `/tmp/단계19.17/diagnosis.md` (휘발성, 본 보고서로 보존).

**커밋**: `7858d40 [단계19.17-페이즈0] 환각·pause 진단`

---

## 2. 페이즈 1 — LLM 점수 구간별 차등 피드백

### 점수 구간 정의 (4단계)

| 구간 | npc_response 톤 | reason 톤 |
|---|---|---|
| **0~49 (매우 낮음)** | 약점 단어 1개 다정하게 짚고 격려 | "자연스럽게 잘 말씀하셨어요" 등 평면 칭찬 **절대 금지** |
| **50~69 (낮음)** | 약점 단어 1개 자연스럽게 발음 모델링 | "○○ 부분이 조금 어려웠죠? 천천히 다시 해봐도 좋아" 류 친근 짚기 |
| **70~84 (보통)** | 적절한 격려 + 자연스러운 호응 | "잘 전달됐어요" 가벼운 인정 OK ("완벽" 같은 과한 표현 X) |
| **85+ (좋음)** | 자연스러운 칭찬 가능 | "자연스럽게 잘 말씀하셨어요" 칭찬 OK |

가이드는 [교정 역할] 섹션의 기본 칭찬 문구를 명시적으로 덮어쓴다고 표기 —
correction_severity가 "none"이어도 < 70점이면 평면 칭찬 금지.

### 코드 변경

1. `src/lib/llm/build-persona-system-prompt.ts pronunciationContextBlock` —
   점수 구간 4단계 가이드 명시. npc_response·reason 양쪽 적용.

2. `app/api/conversation/free/respond/route.ts` —
   - `FEEDBACK_INPUTS_VERSION` `'stage19.16'` → `'stage19.17'` (메타 분기)
   - `REASON_PRAISE_MIN_ACCURACY=70` 임계
   - `fallbackCorrectionReason(ctx)` 신규 — 점수에 따라 분기:
     - 컨텍스트 없음 / ≥70: `"자연스럽게 잘 말씀하셨어요."` (회귀 보호)
     - 50~69: `"잘 전달은 됐어요. 조금만 더 또박또박 하면 더 자연스러워요."`
     - <50: `"조금 더 또박또박 해봐도 좋아요. 천천히 다시 한 번 해볼까요?"`
   - mock 응답 / LLM 실패 폴백 / empty correction 폴백 / ≥0.85 일치율 누름 — 4곳
     모두 `fallbackCorrectionReason` 사용.

### 시뮬레이션 검증 (`tests/unit/stage1917-score-tier-reason.test.ts`, 14 케이스)

```
40점 fallback: "조금 더 또박또박 해봐도 좋아요. 천천히 다시 한 번 해볼까요?"
  → 평면 칭찬 X, 격려·재시도 톤 ✓
60점 fallback: "잘 전달은 됐어요. 조금만 더 또박또박 하면 더 자연스러워요."
  → 평면 칭찬 X, 약점 친근 짚기 ✓
75점 fallback: "자연스럽게 잘 말씀하셨어요."
  → 회귀 없는 적절한 인정 ✓
90점 fallback: "자연스럽게 잘 말씀하셨어요."
  → 자연스러운 칭찬 ✓
```

경계값(49/50/69/70) 분기도 검증. `pronunciationContext` 미전달 시 기본 칭찬 그대로
(회귀 방지).

**vitest**: 1142 → 1156 (+14)

**커밋**: `6813500 [단계19.17-페이즈1] LLM 점수 구간별 차등 피드백 (환각 차단 임계값 조정)`

---

## 3. 페이즈 2 — Pause 데이터 활용 강화

### 임계 하향 (시나리오 C 처리)

| 상수 | 단계 19.13 | 단계 19.17 |
|---|---|---|
| `PAUSE_SHORT_MS` | 800ms | **500ms** |
| `PAUSE_LONG_MS` | 1500ms | **1000ms** |

근거: 한국어 자연 발화에서 500ms+ 단어 간 멈춤은 어휘 탐색이 시작되는 지점.
"음...그..." 같은 학습자 멈춤이 800ms 미만에서도 감지되도록.

잦은 트리거 우려는 시스템 프롬프트의 "한 턴 흐름 코멘트 최대 1회" 가드로 흡수.
450ms 이하는 여전히 미감지(검증 케이스로 확보).

### 시스템 프롬프트 가이드 완화 (시나리오 A 처리)

| 단계 19.16 (strict) | 단계 19.17 (완화) |
|---|---|
| "긴 멈춤 ≥2회 → 격려" | "긴 멈춤 ≥1회 OR 짧은 멈춤 ≥2회 → 격려" |
| "짧은 멈춤 ≤1회 → 별도 언급 X" | "짧은 멈춤 1회만 → 언급 선택. 발음 약점 짚는 호응 안에 녹여도 OK" |

임계 표시도 `PAUSE_SHORT_MS`/`PAUSE_LONG_MS` 상수에서 동적으로 — `"≥1000ms"` /
`"500~999ms"`. 코드와 가이드 동기화.

보존: "친구이지 코치가 아님", "한 턴 최대 1회" 가드.

### 시뮬레이션 검증 (`tests/unit/stage1917-pause-threshold.test.ts`, 11 케이스)

```
600ms gap: 이전 ❌ → 이제 shortPauseCount=1 ✓ (단계 C 핵심)
1100ms gap: 이전 short → 이제 longPauseCount=1 ✓
450ms gap: 여전히 미감지 ✓ (잦은 트리거 방지)
긴 멈춤 1회 → 격려 가이드 적용 ✓ (단계 A 핵심)
짧은 멈춤 1회만 → "선택" 가이드 적용 (이전 "없이" 삭제 확인) ✓
한 턴 최대 1회 가드 보존 ✓
```

### 단계 19.13 llm-eval 영향

`evaluateSpeakingDetail`은 `longPauses/shortPauses` 객체를 직접 받으므로 임계 영향 X.
기존 `tests/unit/stage1913-llm-eval-integration.test.ts` 그대로 통과 확인.

**vitest**: 1156 → 1167 (+11)

**커밋**: `3e8a7e7 [단계19.17-페이즈2] Pause 데이터 활용 강화 (시나리오 C+A)`

---

## 4. 페이즈 3 — 자동 검증

| 항목 | 결과 |
|---|---|
| `npm run test:unit` | **1167 / 0** (+25 신규: 14 reason + 11 pause) |
| `npm run lint` | **0 errors** (4 pre-existing warnings in `scripts/` — 본 단계 무관) |
| `npm run build` | ✓ next build 통과 |
| `npx playwright test tests/smoke/stage1916-respond-api-smoke.spec.ts` | **7 / 7** (28초). `feedback_inputs_version='stage19.17'` 검증 포함 |

---

## 5. 점수 구간별 LLM 응답 시뮬레이션 종합

| 점수 | reason 폴백 (서버) | 시스템 프롬프트 가이드 (LLM에) |
|---|---|---|
| 40 | "조금 더 또박또박 해봐도 좋아요. 천천히 다시 한 번 해볼까요?" | 약점 단어 1개 다정하게 짚고 격려. 평면 칭찬 절대 금지 |
| 60 | "잘 전달은 됐어요. 조금만 더 또박또박 하면 더 자연스러워요." | "조금 어려웠죠?" 류 친근 짚기. 평면 칭찬 금지 |
| 75 | "자연스럽게 잘 말씀하셨어요." | 적절한 격려. "완벽" 같은 과한 표현 X |
| 90 | "자연스럽게 잘 말씀하셨어요." | 자연스러운 칭찬 OK |

**환각 차단 확인**: 60점 입력에 대해 fallback이 평면 칭찬을 반환하지 않음
(`tests/unit/stage1917-score-tier-reason.test.ts:65~70` 경계값 테스트로 검증).

**멈춤 활용 확인**: 600ms gap이 short pause로 감지되고
(`tests/unit/stage1917-pause-threshold.test.ts:42~52`), 시스템 프롬프트에
"긴 멈춤 1회 이상이거나 짧은 멈춤 2회 이상이면 다정한 한 마디로 격려" 가이드 적용
(같은 파일 :89~95).

---

## 6. 단계 19.13~16 효과 보존 체크

| 항목 | 상태 |
|---|---|
| `feedback_inputs_version` 메타 | ✓ 분기 (`stage19.13` llm-eval / `stage19.17` respond) |
| 친구 페르소나 톤 | ✓ 시스템 프롬프트 분기로만 조정 |
| 환각 차단(허위 정보) `antiHallucinationBlock` | ✓ 미변경 |
| 시제·수사 교정 `correctionBlock` | ✓ 미변경 (기본 칭찬 문구도 유지) |
| 자유 대화 NPC 발음 컨텍스트 흐름 | ✓ 단계 19.16 흐름 유지 + 가이드 강화 |
| 자유 대화 STT 흐름 | ✓ 미변경 (단계 19.16의 gpt-4o-mini-transcribe 유지) |
| 단계 19.13 llm-eval | ✓ PAUSE 임계 변경 영향 없음(객체 직접 입력) |

---

## 7. 미처리 후속

| 항목 | 처리 시점 |
|---|---|
| 사용자 실제 음성 시연 (4시나리오: 40/60/75/90점) | 사용자 환경 수동 (시뮬레이션은 단위 테스트로 완료) |
| STT 환각 — Naver Clova PoC | 1차 시험운영 후 |
| NPC voice 차별화 | 단계 25+ |
| 자유 대화 외 화면(예: 말하기 평가)의 평면 칭찬 점검 | 회귀 보고 시 별도 단계 |

---

## 8. 변경 파일 요약

```
app/api/conversation/free/respond/route.ts            (페이즈 1)
src/lib/llm/build-persona-system-prompt.ts            (페이즈 1+2)
src/lib/pronunciation-context.ts                      (페이즈 2)
src/types/providers.ts                                (페이즈 2)

tests/unit/stage1917-score-tier-reason.test.ts        (페이즈 1 신규)
tests/unit/stage1917-pause-threshold.test.ts          (페이즈 2 신규)
tests/unit/stage1913-pronunciation-context.test.ts    (페이즈 2 픽스처 정정)
tests/unit/stage1916-free-conversation-pronunciation-context.test.ts
                                                       (페이즈 1+2 라벨/문자열 정정)
tests/unit/stage1916-respond-route-parsing.test.ts    (페이즈 1 버전 정정)
tests/smoke/stage1916-respond-api-smoke.spec.ts       (페이즈 1 버전 정정)
```

푸시 X (작업 지시서 절대 규칙).
