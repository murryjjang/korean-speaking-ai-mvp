# [KDLI Korean MVP — 단계 19.9 — 로고 + 다언어 7개 + UI확장 + PDF 전면교체] 보고서

작성일: 2026-05-17
작성자: Claude (Opus 4.7) — 자율 실행
브랜치: `feat/q4-llm-provider` (origin 대비 ahead 6 commits 추가, push 없음)

---

## 0. 한 줄 요약

**19.5·19.6·19.7·19.8 4연속 실패한 아랍어 PDF 문제를 Puppeteer 서버사이드 렌더 전환으로 근본 해결했고, 다국어 지원을 4개(ko/en/vi/ar) → 7개(+th/ms/km)로 확장, KDLI 공식 로고 자산을 헤더·로그인에 복구, 4개 메뉴 페이지 제목·사이드바 typography를 통일했다. vitest 1080 통과, 회귀 0.**

## 1. 페이즈별 진단·수정·검증

### 페이즈 0 — 크메르어 LLM 번역 샘플 안전망

| 항목 | 결과 |
|---|---|
| 모델 | gpt-4o-mini, temperature=0 |
| 샘플 수 | 5 (한국어 → 크메르어) |
| 자체 confidence high | 5/5 |
| 인간 리뷰 우려 | #2 "발음과 억양" → 둘 다 `សំឡេង`(소리)로 번역, 의미 구분 손실 |

**결정**: 동의서·UI 보조 표기 전체에 "임시본 명시" 문구 부착해 양해 확보 (페이즈 2.5에서 시행).

**증거**: `/tmp/단계19.9/khmer-samples.md`, `docs/단계19.9-페이즈0-크메르어샘플.md`

---

### 페이즈 1 — KDLI 공식 로고 자산 복구

| 항목 | 변경 |
|---|---|
| 로그인 원형 로고 | `/login`, `/research/login` → `/kdli-logo-circle.png` (224×224, 사용자 제공) |
| 헤더 워드마크 | `KdliBrand`, `Topbar` → `/kdli-logo.png` (1448×1086) — 학습자·관리자·교수자·리서치 4개 모두 통일 |
| Topbar 컴포넌트 통합 | `KdliBrand` 단일 컴포넌트 사용 (배경 박스/테두리 제거) |
| 제거 자산 | 19.5 `kdli-placeholder.svg`, 19.8 `kdli-seal-*.png`, `kdli-logo-256.png` 참조 |
| V1-2 회귀 해결 | 사각형 박스 줄 도드라짐 차단 (배경/테두리 미적용) |

**검증** (실제 사용자 경로):
- ✅ `/login` 원형 로고 표시 → `phase1-login-circle.png`
- ✅ `/research/login` 원형 로고 표시 → `phase1-research-login-circle.png`
- ✅ `/student` 헤더 워드마크 → `phase1-student-header-wordmark.png`
- ✅ `/admin/analytics` 헤더 워드마크 → `phase1-admin-header-wordmark.png`

**증거**: `/tmp/단계19.9/screenshots/phase1-*.png` (4장), `tests/smoke/stage199-logo-verify.spec.ts` (4/4 통과), `tests/unit/stage199-kdli-logo.test.ts` (12/12 통과)

---

### 페이즈 2 — 다언어 7개 확장 (th/ms/km)

| 영역 | 변경 |
|---|---|
| `DisplayLanguage` 타입 | `'ko'\|'en'\|'vi'\|'ar'` → `+'th'\|'ms'\|'km'` |
| `MultilingualText`/`MultilingualList` | `th?`, `ms?`, `km?` optional 키 추가 |
| `MOTHER_TONGUE_HINTS` 자연어 매핑 | th/ภาษาไทย/태국어, ms/Bahasa Melayu/말레이어, km/Khmer/크메르어/캄보디아어 모두 인식 |
| `dashboard-labels.ts` | 54개 LabelMap 블록 모두 th/ms/km 키 일괄 주입 (LLM 번역) |
| `consent-text.ts` | `CONSENT_TEXT_TH/MS/KM` 추가 + `CONSENT_TEXTS` Record 확장 |
| 임시본 명시 문구 | vi/ar/th/ms/km 5개 언어 동의서 본문 끝에 "[임시본 안내 / Provisional Translation Notice]" 부착 |
| 동의서 상단 `I18N_TEXT` | title/subtitle/pcLabel/agree/decline 5개 라벨에 th/ms/km 추가 |
| `isConsentLocale` 가드 | th/ms/km 인식 |
| 관리자 발급 폼 | mother_tongue 드롭다운에 태국어/말레이어/크메르어 옵션 추가 |
| LLM 시스템 프롬프트 (자유 대화 + Q4) | 외국어 6개 모두(en/vi/ar/th/ms/km) 다국어 객체 응답 가이드 + 학습자 mt 키 반드시 채울 것 |
| `MultilingualFeedback` | th/ms/km는 정적 번역(en/vi/ar)만 가진 컴포넌트라 영어 폴백 (임시본 정책) |
| DB 마이그레이션 | `supabase/migrations/20260517_stage199_mother_tongue_7_langs.sql` — `mother_tongue` CHECK 제약 7개 코드 + 'other' |

**검증**:
- `tests/unit/stage199-7-langs.test.ts` 28/28 통과
- DisplayLanguage 추론·CONSENT_TEXTS 키·임시본 안내 포함·dashboard-labels th/ms/km 완전성 모두 검증

**임시본 명시 문구 5개 언어 결과** (`Provisional Translation Notice`):

| 언어 | 첫 문장 |
|---|---|
| vi | Bản dịch này là bản tạm thời và sẽ được áp dụng bản dịch chính thức sau khi cải tiến hệ thống. |
| ar | هذه الترجمة مؤقتة وسيتم تطبيق الترجمة الرسمية بعد تحسين النظام. |
| th | การแปลนี้เป็นฉบับชั่วคราว และจะมีการนำไปใช้เป็นการแปลอย่างเป็นทางการหลังจากการปรับปรุงระบบ |
| ms | Terjemahan ini adalah versi sementara dan akan diterapkan terjemahan rasmi setelah peningkatan sistem. |
| km | ការបកប្រែនេះគឺជាការបកប្រែបណ្តោះអាសន្ន ហើយនឹងត្រូវបានអនុវត្តន៍ការបកប្រែជាផ្លូវការ ... |

⚠️ **DB 마이그레이션 적용 안내**: `supabase/migrations/20260517_stage199_mother_tongue_7_langs.sql`은 Supabase SQL Editor에서 운영자가 직접 붙여넣어 실행해야 합니다. 본 프로젝트는 자동 마이그레이션 러너를 사용하지 않습니다.

---

### 페이즈 3 ⭐⭐⭐ — Puppeteer PDF 전면 교체

**4연속 실패 패턴(19.5·19.6·19.7·19.8 아랍어 글자 분리)의 근본 해결.**

| 항목 | 19.8 이전 | 19.9 이후 |
|---|---|---|
| 렌더 경로 | DOM → html2canvas-pro → 캔버스 → jsPDF → PDF | 클라이언트가 self-contained HTML 조립 → `POST /api/pdf` → Puppeteer Chrome headless → `page.pdf()` |
| 아랍어 shaping | ❌ 글자별 그리기 → isolated form만 (단어 글자 따로따로) | ✅ Chrome 자체 shaping engine 결과를 PDF에 박음 |
| 폰트 임베드 | next/font 시도해도 shaping은 폰트가 책임지지 않아 무용 | 외부 CDN(Pretendard, Noto Sans KR/Arabic/Thai/Khmer) 모두 자유롭게 사용 |
| 색 함수 | oklch/oklab/lab 회피용 inline 변환 필요 (`UNSUPPORTED_COLOR_FN`) | Chrome native — 변환 불필요 |

**구현 세부**:
- `src/lib/pdf/puppeteer-render.ts` (신규): `renderHtmlToPdf({ html })`. Playwright 설치 Chromium 바이너리 자동 탐색 (`~/.cache/ms-playwright/chromium-*/chrome-linux64/chrome` + ENV 폴백).
- `app/api/pdf/route.ts` (신규): `POST /api/pdf` — `runtime = 'nodejs'`, `maxDuration = 60`, 5MB HTML 상한.
- `src/components/pdf-download-button.tsx` (재작성): 캡처 대상 `outerHTML` + `document.head` stylesheet·font link 수집해 self-contained HTML 문서로 조립 → API POST → blob 다운로드.
- 제거: `src/lib/pdf/dom-to-pdf.ts`, `tests/unit/pdf-color-fallback.test.ts`, `tests/unit/stage195-pdf-arabic-rtl.test.ts`, `tests/unit/stage196-pdf-new-model.test.ts`, package deps `html2canvas`/`html2canvas-pro`/`jspdf`.

**4연속 실패 패턴 차단 — 시각 검증 의무**:

7개 mother_tongue × 동일 콘텐츠(한국어 본문 + 보조 언어)로 `/api/pdf` 호출 → PDF 생성 → Chrome 네이티브 PDF 뷰어로 첫 페이지 PNG 캡처:

| 언어 | PDF 크기 | 시각 확인 |
|---|---|---|
| ko | 103,247 bytes | ✅ 한국어 정상 (단독) |
| en | 99,857 bytes | ✅ 영어 보조 정상 |
| vi | 131,418 bytes | ✅ 베트남어 보조 정상 |
| **ar** | **113,840 bytes** | ✅ **아랍어 연결형(initial/medial/final/isolated) 모두 정상** — `مرحبا`, `يرجى`, `التدرب`, `الموافقة` 등 단어 단위 정상 결합 |
| **th** | **99,656 bytes** | ✅ **태국어 vowel marks(ี, ื, ้, ู) 정상 스택** — `รายงาน`, `กรุณาฝึก`, `น้ำเสียง` 등 정상 |
| ms | 104,174 bytes | ✅ 말레이어 보조 정상 |
| **km** | **110,588 bytes** | ✅ **크메르어 subscript(្វ, ្ច, ្ត) 정상 결합** — `ស្វាគមន៍`, `ការបញ្ចេញសំឡេង`, `សំនៀង` 등 정상 |

**증거**:
- PDF 파일 7종: `/tmp/단계19.9/pdfs/pdf-smoke-{ko,en,vi,ar,th,ms,km}.pdf`
- PDF → PNG (시각 검증용) 7종: `docs/단계19.9-pdf-검증/pdf-{arabic-shaping,thai-vowels,khmer-subscripts,korean,english,vietnamese,malay}.png` (커밋 포함)

**아랍어 PNG에서 직접 확인 가능한 단어** (이전 4연속 실패에서 글자 분리됐던 것들):
- `مرحبا` (안녕하세요) — م+ر+ح+ب+ا 정상 결합
- `يرجى` — ي+ر+ج+ى 정상
- `التدرب` — ال+ت+د+ر+ب 정상 (이전: ا/ل/ت/د/ر/ب 따로)
- `الموافقة` — ال+م+و+ا+ف+ق+ة 정상

`tests/unit/stage199-pdf-puppeteer.test.ts` 6/6 통과 (마이그레이션 정적 보호).

---

### 페이즈 4 — 본문 라벨 보조 표기 확장 (Q2=a 부분 적용)

| 영역 | 변경 |
|---|---|
| `PRACTICE_LABELS` (신규, dashboard-labels.ts) | 27개 항목 × 7개 언어 = 189개 텍스트 추가 |
| 카테고리 | 난이도(beginner/intermediate/advanced), 평가 목적(official/diagnostic/practice), 액션(start, startInOrder, recordPresentation, endConversation, tryNow), 필드(topic, presentationTopic/Level, targetTime, presentationScript), 설정(learning/presentationSetup), 상태(inProgress/completed/ready/preparing), 메타(pronunciationAzure, npcAutoPlay, prepTime, answerTime) |
| 적용 위치 | `app/student/speaking/page.tsx` — 난이도 배지, 목적 배지, "1번부터 순서대로 응시하기" primary 버튼, "시작하기" 데스크톱·모바일 버튼 (모두 `<Localized>` inline) |
| Server component 변환 | `SpeakingSelectionPage`를 async + `getCurrentParticipant()` 호출해 motherTongue 결정 |

**부분 적용 사유**: 읽기연습/발표연습/자유대화 client 파일이 각 1,500~1,800줄 규모로 일괄 변경 시 회귀 위험·시간 비용 큼. 페이지 제목(페이즈 5)은 통일 완료, 본문 내부 세부 라벨은 별도 작업(V2-1 후속)으로 분리.

**증거**: `tests/unit/stage199-practice-labels.test.ts` 10/10 통과 (PRACTICE_LABELS 라벨 7개 언어 완전성 + 말하기 페이지 Localized 적용).

---

### 페이즈 5 — 페이지 제목 + 사이드바 typography 통일 (V3-1, V3-2)

**V3-1 — 4개 메뉴 페이지 제목 통일**:
| 페이지 | Before | After |
|---|---|---|
| 말하기 평가 | `PageHeader` (titleSupplement 없음) | `PageHeader` + `titleSupplement=<Localized studentSpeaking>` |
| 읽기연습 | `<h1 class="text-3xl font-bold">읽기연습</h1>` 단독 | `text-xl font-bold leading-tight` + `<Localized studentReading supplementOnly>` |
| 발표연습 | `<h1 class="text-3xl font-bold">발표연습</h1>` 단독 | `text-xl font-bold leading-tight` + `<Localized studentPresentation supplementOnly>` |
| 생성형 대화 연습 | `<h1 class="text-xl font-bold">생성형 대화 연습</h1>` 단독 | `text-xl font-bold leading-tight` + `<Localized freeConversationPractice supplementOnly>` |

공통 패턴: `text-xl font-bold leading-tight + lang="ko"` 본문 → 작은 mother_tongue 보조 (`text-xs text-text-muted`) → 기존 description.

**V3-2 — 사이드바 typography 회귀 보호**:
- Sidebar는 이미 단일 `ItemLabel` 컴포넌트로 통일됨 (`text-sm font-medium`, active 시 `accentClass`). 회귀 차단 unit test 추가.

**증거**: `tests/unit/stage199-page-title-unify.test.ts` 5/5 통과.

---

## 2. 시험운영 직전 시스템 최종 상태

| 지표 | 19.8 마감 | 19.9 마감 |
|---|---|---|
| vitest | 1066 | **1080** (+14, 회귀 0) |
| 지원 언어 | 4 (ko/en/vi/ar) | **7** (+th/ms/km) |
| PDF 아랍어 | ❌ 글자 분리 (4연속 실패) | ✅ **Puppeteer 전환 — 7개 언어 모두 정상** |
| KDLI 로고 | placeholder/seal (V1-2 사각 박스) | ✅ 공식 워드마크 + 원형 통일 |
| 페이지 제목 보조 | 학습 진척만 | ✅ 5개 메뉴 모두 |
| 사이드바 typography | 일관 (19.8) | ✅ 회귀 차단 unit test 추가 |
| 동의서 임시본 안내 | 없음 | ✅ vi/ar/th/ms/km 5개 명시 |
| html2canvas 의존성 | 있음 | ✅ 제거 |
| `puppeteer-core` | - | ✅ 추가 |

---

## 3. 미해결 항목 + 후속 권장

| 항목 | 상태 | 후속 |
|---|---|---|
| DB 마이그레이션 (`mother_tongue` CHECK 7개) | ⏳ 운영자 직접 적용 필요 | Supabase SQL Editor에서 `20260517_stage199_mother_tongue_7_langs.sql` 붙여넣기 |
| 읽기/발표/자유대화 본문 세부 라벨 (Q2=a 잔여) | ⏳ 부분 | 단계 19.10에서 client 파일 큰 단위 라벨 확장. 우선은 페이지 제목만 통일. |
| 발음 평가 결과 4개 정적 번역(en/vi/ar 한정) | ⏳ 임시 | `MultilingualFeedback`는 th/ms/km 데이터가 없어 영어 폴백. 향후 LLM이 6개 외국어로 데이터 생성하도록 확장. |
| Puppeteer 운영 환경 (Vercel/Lambda) | ⏳ dev 한정 | 현재 시험운영은 ngrok+로컬. 운영 배포 시 `@sparticuz/chromium` + `puppeteer-core` 구성으로 마이그레이션. |
| 크메르어 핵심 어휘 검수 | ⚠️ 임시본 | 발음(`ការបញ្ចេញសំឡេង`) vs 억양(`សំនៀង`) 등 LLM이 동일 단어 사용한 경우 한국학자 검수 필요. |

---

## 4. 캄보디아 학습자 양해 안내 초안 (단계 23 활용용)

### 한국어 원본

```
안녕하세요. KDLI 한국어 학습 시험운영에 참여해 주셔서 감사합니다.

현재 시스템에서 크메르어(캄보디아어) 보조 표기는 임시 자동 번역본을 사용하고
있습니다. 다음 사항을 양해해 주시면 감사하겠습니다.

  · 동의서·UI 라벨의 크메르어 번역은 시스템 개선 후 정식 번역으로 교체될
    예정입니다.
  · 동의 내용이 불분명한 경우 한국어 또는 영어 동의서를 참고하시거나, 운영
    관리자에게 직접 문의해 주세요.
  · 학습 효과 평가에는 임시 번역이 영향을 주지 않습니다 — 평가는 한국어
    발화 데이터를 기준으로 진행됩니다.

문의: [운영자 연락처]
```

### English Translation

```
Hello, and thank you for participating in the KDLI Korean Speaking AI pilot.

The Khmer (Cambodian) translations currently shown in the consent form and
UI are provisional, machine-generated translations. Please note:

  · The Khmer text will be replaced with reviewed translations after the
    system improvement.
  · If any consent language is unclear, please refer to the Korean or English
    version, or contact the operator directly.
  · The provisional translation does not affect the learning effectiveness
    evaluation — assessment is based on your Korean speech data.

Contact: [Operator]
```

---

## 5. 커밋 6건 요약

```
60037b3 [단계19.9-통일] 페이지 제목 + 사이드바 typography 통일 (V3-1, V3-2)
b41f4a0 [단계19.9-UI확장] 말하기 평가 본문 라벨 mother_tongue 보조 (Q2=a 부분 적용)
27bf41d [단계19.9-PDF-puppeteer] html2canvas → Puppeteer 서버사이드 렌더 전면 교체 ⭐⭐⭐
f86cc59 [단계19.9-다언어] 7개 mother_tongue 확장 (ko/en/vi/ar/+th/ms/km)
40ba072 [단계19.9-로고] KDLI 공식 로고 자산 복구 (login·헤더)
d0ac1a5 [단계19.9-페이즈0] 크메르어 LLM 번역 샘플 안전망 테스트
```

origin 대비 추가 6 commits. **push 없음** (CLAUDE 지침 준수).

---

## 6. 절대 규칙 준수 확인

| 규칙 | 상태 |
|---|---|
| 회귀 금지 (vitest 1066+신규) | ✅ 1080 통과 |
| 푸시 금지 | ✅ 로컬 6 commits |
| 자체 검증 우회 차단 (URL ?locale= 우회 X) | ✅ Smoke spec은 testid + DOM 검증 + getCurrentParticipant 흐름 |
| PDF 시각 검증 (PDF 파일 매직 바이트만 보고 통과 표시 금지) | ✅ 7개 언어 PDF → Chrome 뷰어로 PNG 캡처 → 아랍어 연결형·태국어 vowel·크메르어 subscript 시각 확인 + 보고서 첨부 |
| 로고 자산 사전 확인 | ✅ 페이즈 1 시작 전 `ls public/kdli-logo*.png` 확인 |
| 커밋 단위 `[단계19.9-{항목}]` | ✅ 6/6 |

---

## 7. 결론

단계 19.5~19.8까지 4번 시도해 매번 실패한 아랍어 PDF 문제를 **html2canvas 아키텍처
자체의 한계** 진단으로 정의하고, **Puppeteer 서버사이드 Chrome headless 렌더**로 전면
교체했다. 이로써 한국어·아랍어·태국어·크메르어·말레이어·베트남어·영어 7개 언어가 모두
PDF에 정상 출력되며, 향후 같은 부류의 RTL/복잡 shaping 언어가 추가돼도 회귀 위험이 없다.

다국어 지원도 4 → 7개로 확장되어, 시험운영 대상 학습자 모국어 모두 커버. KDLI 공식
로고 자산이 들어와 시각 일관성이 회복됐고, 페이지 제목·사이드바 typography가 통일되어
UX 일관성도 확보됐다.

시험운영 직전 상태로 충분히 안정적이라고 판단된다. 다음 단계(19.10 또는 시험운영 시작)에서
DB 마이그레이션 적용 + 학습자 양해 안내 발송이 우선 작업이다.
