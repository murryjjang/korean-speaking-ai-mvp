# 단계 19 — 종합 수정 보고서

- **브랜치**: `feat/q4-llm-provider` (origin 대비 45 commits ahead, **push 금지** 유지)
- **작업 기간**: 2026-05-16
- **베이스라인 vitest**: 868 통과 (단계 18 종료 시점)
- **완료 후 vitest**: 905 통과 / 0 실패 (+37 신규)
- **Playwright chromium**: 7 통과 (PDF 1 + console errors 6)
- **lint / build**: 모두 통과 (Turbopack 경고 0건)

## 1. 페이즈별 결과 요약

| 페이즈 | 항목 | 커밋 | 신규 vitest | 상태 |
|---|---|---|---|---|
| 1 | PDF 실작동 [G] + DiceBear 9.x [E] + NFT 경고 0 | 3f393a7 | +4 + e2e 7 | ✅ |
| 2a | mother_tongue 자동 [D6] + 라벨 i18n [D6.2/3/5] + RTL [D6.4] + 피드백 일관 [D8.1] | 3bd478e | +17 | ✅ |
| 2b | grammar_note BilingualText [D7] + D10 부분 | d7cd171 | +6 | ✅ |
| 3 | 시제 검증 [LLM-1] + severity 정책 [K] + 다중 오류 [K.2] | 5a7168f | +10 | ✅ |
| 4 | 디자인 학술 톤 [L1/L2/L3] | bcb2872 | — | ✅ |
| 5 | 최종 검증 + 보고서 | — | — | ✅ |

---

## 2. 자체 검증 vs 실제 동작 일치 — 단계 18 갭 재발 방지

단계 18의 핵심 회귀 원인은 **"단위 테스트는 통과했지만 실제 브라우저에서는 실패"** 였다. 단계 19는 다음 장치로 차단:

### 2.1 PDF 다운로드 [G] — Playwright e2e로 실제 검증

- **단계 18 보고**: "PDF 해결 (oklch 폴백 적용, 단위 테스트 18건 통과)"
- **단계 18 실측**: 3개 화면 모두 "PDF 생성에 실패했습니다" 표시
- **단계 19 원인 진단**: html2canvas v1.4.1의 `inlineUnsupportedColors` 폴백이 `getComputedStyle`을 통해 oklch를 잡으려 했지만, modern Chrome은 oklch를 이미 resolve된 형태로 반환할 수도 있고, 일부 속성(box-shadow 등)에서는 잡지 못함.
- **단계 19 수정**: html2canvas-pro v2.0.2 (oklch/lab/color-mix 네이티브 지원 modern fork)로 교체. 기존 폴백은 belt-and-suspenders로 유지.
- **단계 19 검증**: `/dev/pdf-smoke` (Tailwind v4 oklch + 다국어 + 표 포함) → 실제 Chromium에서 PDF 다운로드 → 매직 바이트 `%PDF-` 확인 + 파일 ≥ 1KB + 콘솔 에러 0건.

```
✓ /dev/pdf-smoke — Tailwind v4 oklch + 다국어 본문이 유효한 PDF로 다운로드된다 (768ms)
```

### 2.2 DiceBear 아바타 [E] — 스키마 enum 직접 검증

- **단계 18 보고**: "DiceBear 옵션 적용 (longHair*/shortHair* prefix, 단위 테스트 4건 통과)"
- **단계 18 실측**: 4명 모두 이니셜 표시 (S/J/S/Y)
- **단계 19 원인 진단**:
  ```
  $ curl https://api.dicebear.com/9.x/avataaars/svg?seed=sua&top=longHairStraight
  HTTP 400 Bad Request
  {"message":"querystring/top/0 must be equal to one of the allowed values"}
  ```
  DiceBear 9.x avataaars 스키마에서 8.x의 `longHair*`/`shortHair*` prefix가 제거되고 `bob`/`bigHair`/`curly`/`shortFlat` 등으로 평탄화. 단계 18 테스트는 소스 코드 텍스트에서 prefix 매치만 확인 → API 거부를 못 잡음.
- **단계 19 수정**: PERSONA_OPTIONS의 모든 top 값을 9.x enum 값으로 교체. 테스트도 9.x 스키마 enum(`DICEBEAR_9X_AVATAARS_TOP`)에 대해 어셜션.
- **단계 19 검증 보호**: 향후 DiceBear 옵션 변경 시 스키마 enum과 실제 코드 매칭이 단위 테스트에서 즉시 잡힘.

### 2.3 "X Issues" Next.js dev indicator [Issues0]

- **단계 18 보고**: "1 issue 사라짐 (build PASS)"
- **단계 18 실측**: 페이지마다 "1 → 2 → 3 Issues" 증가
- **단계 19 원인 진단**: `convert-for-etri.ts`의 `path.join(process.cwd(), ...)` + `require('ffmpeg-static')`가 Turbopack NFT 추적에 걸려 "unexpected file in NFT list" 경고. 단계 18은 build를 본 거지만 dev mode indicator 동작과 다름.
- **단계 19 수정**:
  - `next.config.ts`에 `serverExternalPackages: ['ffmpeg-static']` 추가 (Node native require로 외부화).
  - `turbopack.ignoreIssue`로 의도된 runtime fs 호출 경고 silencing.
- **단계 19 검증**: `npm run build` 경고 0건 + Playwright로 공개 6페이지 console.error 0건 확인.

### 2.4 mother_tongue 자동 적용 [D6]

- **단계 18 보고**: "mother_tongue 자동 적용 + i18n 컨텍스트 초기값"
- **단계 18 실측**: 첫 화면 한국어 고정, 토글로 수동 변경 필요
- **단계 19 원인 진단**: `useDisplayLanguage`가 추론을 `useEffect`에서 수행 → 첫 렌더(SSR + hydration)는 항상 default 'ko'로 시작, effect 실행 후에야 storage 갱신 + re-render. UI에는 한 번 ko가 표시된 후 외국어로 "전환"되는 flash가 발생했고, 사용자는 첫 인상을 "한국어 고정"으로 인식.
- **단계 19 수정**: 추론을 `getSnapshot`/`getServerSnapshot`에 inline → SSR 시점부터 즉시 모국어로 렌더. 명시 선택(localStorage)이 있으면 그것이 최우선.
- **단계 19 검증 보호**: 정적 검사로 hook 소스에 `useEffect` 기반 storage 초기화가 제거되고 `getServerSnapshot`이 inferDisplayLanguageFromMotherTongue를 호출하는지 어셜션.

### 2.5 학습 피드백 카드 [D8.1]

- **단계 18 보고**: "헤더 단일 토글 + write-through로 legacy 동기화"
- **단계 18 실측**: 라벨 "영어 (ENGLISH)"인데 콘텐츠는 베트남어
- **단계 19 원인 진단**: `MultilingualFeedback`이 legacy `useLanguageHelper`를 구독 → `useDisplayLanguage`의 write-through StorageEvent dispatch가 같은 탭에서 항상 트리거되지 않거나, race condition으로 라벨·콘텐츠 갱신 타이밍 불일치.
- **단계 19 수정**: `MultilingualFeedback`이 `useDisplayLanguage`를 직접 구독. legacy 의존 제거. ko 선택 시 카드 비표시(본문 한국어 중복 회피).

---

## 3. 페이즈별 상세

### 페이즈 1 — 회귀 복구 + 신뢰 회복 (3f393a7)

| 항목 | 변경 | 신규 테스트 |
|---|---|---|
| [G] PDF | `html2canvas` → `html2canvas-pro` v2.0.2 | e2e 1건 (실제 PDF 다운로드 + 매직 바이트) |
| [E] 아바타 | DiceBear 9.x avataaars enum 값 교체 + 9.x 스키마 정적 검증 | 단위 8건 (스키마 호환 + 성별 표현) |
| [Issues0] | `serverExternalPackages` + `turbopack.ignoreIssue` | e2e 6건 (공개 페이지 console.error 0건) |

**신규 파일**:
- `app/dev/pdf-smoke/page.tsx` (prod 404, dev/smoke 전용)
- `tests/smoke/pdf-download.spec.ts`
- `tests/smoke/console-errors.spec.ts`

### 페이즈 2 — 다국어 시스템 완성

**2a (3bd478e)**:
- `dashboard-labels.ts`: MODE_CARD_LABELS(8키) + DATA_DOWNLOAD_LABELS(2키) + SCORE_UNIT_LABELS(1키) 추가, 4언어 완비.
- `localized-extras.tsx`: `<LocalizedModeLabel>` / `<LocalizedScore>` 클라이언트 헬퍼.
- `localized-mode-donut.tsx`: 도넛 차트 범례 i18n 클라이언트 래퍼.
- `dir-html-sync.tsx`: AR 토글 시 `documentElement.dir = 'rtl'` 자동 적용.
- `use-display-language.ts`: SSR-aware 추론 (페이즈 2 D6).
- `multilingual-feedback.tsx`: useDisplayLanguage 직접 구독 (D8.1).

**2b (d7cd171)**:
- `dialogue-mission-panel.tsx`: Q4 대화 grammar_note → BilingualText emphasize 모드.
- `free-conversation-client.tsx`: ChatTurn.correction.reason 타입을 `string | { ko, en?, vi?, ar? }`로 확장. BilingualText 적용.

### 페이즈 3 — LLM 교정 정책 강화 (5a7168f)

`build-persona-system-prompt.ts` / `build-q4-persona-system-prompt.ts`에 추가:
- **시제 검증 규칙(LLM-1)**: 시간 부사 ↔ 동사 시제 매칭 필수 검증. 시제 충돌은 meaning_error.
- **minor reason 포맷(K)**: `"[원문] → [정답]"` 한 줄, 설명 문장 금지.
- **다중 오류 우선순위(K.2)**: meaning_error > minor, 한 번에 주 교정 최대 1개.
- Few-shot 예시: `"내일은 학교에 갔어요" → "내일은 학교에 갈 거예요"` (시제 충돌).

### 페이즈 4 — 디자인 학술 톤 (bcb2872)

- **L1 진척 페이지**: `max-w-2xl → max-w-3xl`, 헤더 `border-b`, 섹션 간 `pt-6 border-t border-border/60` 일관.
- **L2 차트**: DailyBars · ScoreLine을 `max-w-sm` 컨테이너로 감싸 SVG fontSize 비례 확대 차단.
- **L3 P020 표**: 헤더/셀에 `whitespace-nowrap` 추가. mother_tongue 표시를 `"ko" → "한국어 (KO)"` 형식으로 친화화.

---

## 4. 검증 결과 — 페이즈별

### 페이즈 1
- ✅ `npm run test:unit` 872 통과
- ✅ `npm run build` Turbopack 경고 0건
- ✅ Playwright PDF 다운로드 e2e (`%PDF-` 매직 바이트, 1.4KB+, 콘솔 에러 0건)
- ✅ Playwright 공개 6페이지 console.error / pageerror 0건

### 페이즈 2
- ✅ `npm run test:unit` 895 통과 (+23)
- ✅ Playwright 공개 6페이지 0건 유지
- ✅ `npm run build` 경고 0건

### 페이즈 3
- ✅ `npm run test:unit` 905 통과 (+10)
- ✅ 프롬프트에 "시제 검증 규칙" + "강도 분류" + "다중 오류 우선순위" 정적 검증
- ✅ 시연 사례 "어제 학교 가요" / "내일은 학교에 갔어요" / "일 조각" / "카드 교체" 4건 모두 프롬프트에 포함

### 페이즈 4
- ✅ `npm run test:unit` 905 유지
- ✅ `npm run lint` 통과
- ✅ `npm run build` 경고 0건

### 페이즈 5 (최종)
- ✅ vitest 905 / 0 실패
- ✅ Playwright chromium 7 (PDF 1 + console errors 6)
- ✅ Turbopack build 경고 0건

---

## 5. 미해결 항목

| 항목 | 사유 | 후속 권장 |
|---|---|---|
| [L4] KDLI 로고 SVG | 원본 벡터 또는 재익스포트 필요. 자동 변환은 품질 보장 X | 디자이너에게 원본 의뢰. 임시로는 PNG를 actual displayed size(160×160)로 재익스포트 |
| [L1] OPIc/TOPIK 식 전면 리디자인 | 종합 점수 박스, 표 구조, 학술 transcript 톤 등 큰 작업 | 별도 단계로 분리. 본 단계에서는 분리·여백·차트 컴팩트화로 1차 정돈 |
| [L3] 동의서·자유 대화 본문·로그인 톤 통일 | 화면 수 많고 콘텐츠 다양 | 페이지별 점진 적용. 디자인 토큰 일관성 가이드 문서화 권장 |
| [D10] BilingualText 전수 적용 (자유 대화 요약·평가 결과 카드) | 기존 dual-rendering이 작동 중. emphasize 모드로 전환은 grammar_note만 적용 | summary/feedback 카드 리팩토링과 L1 OPIc 리디자인 통합 권장 |
| [D6.6] max-width 일관성 다른 페이지 점검 | 진척 페이지만 max-w-3xl로 정돈 | 학생/교수자/관리자 페이지 전수 점검 |
| 3개 실제 PDF 화면(자유 대화 요약·평가 결과·진척) 직접 e2e | 인증·DB 데이터 의존이라 smoke 테스트 셋업 복잡 | `/dev/pdf-smoke`가 동일 코드 경로(PdfDownloadButton → renderDomToPdf with html2canvas-pro) + 동일 Tailwind v4 oklch 스타일을 실제 실행하므로 회귀 보호 유효. 운영 환경에서 수동 1회 확인 권장 |

---

## 6. 새 발견 사항

1. **DiceBear 8.x → 9.x breaking change**: avataaars의 `top` 값 enum이 완전히 재명명됨. 다른 컬렉션(personas, micah 등)도 비슷한 변화 가능성. 외부 API 의존 자산은 스키마 endpoint를 정기 점검하는 정책 권장.
2. **Turbopack NFT vs Webpack 차이**: Webpack은 `path.join(process.cwd(), ...)`를 silent하게 허용하지만 Turbopack은 NFT 추적에서 경고. 마이그레이션 시 `serverExternalPackages` + `turbopackIgnore`로 일괄 정리 필요.
3. **useSyncExternalStore의 hydration mismatch 회피**: getSnapshot과 getServerSnapshot이 동일 값을 반환하지 않으면 hydration mismatch. 추론을 두 함수 모두에 inline하면 일치 보장.
4. **html2canvas-pro의 modern color 지원**: v2.x는 oklch/lab/color-mix 네이티브 처리. Tailwind v4 환경에서 PDF 생성을 한다면 표준 선택.

---

## 7. 비용 영향

- **시제 검증 가이드 추가(LLM-1)**: 시스템 프롬프트 +~400자 (자유 대화·Q4 둘 다). 사용자 위임 명시: 1주 +~73원 수준, 무시 가능.
- **html2canvas-pro 패키지**: ~250KB (lazy import — 첫 PDF 클릭 전까지 로드 X).

---

## 8. 회귀 보호 요약 (단계 19 신규 단위 테스트)

| 테스트 파일 | 신규 케이스 | 보호 대상 |
|---|---|---|
| `persona-avatar-stage18.test.ts` | 8 (재작성) | DiceBear 9.x enum 매치, 성별 표현 |
| `stage19-i18n-d6-d8.test.ts` | 17 | mother tongue 자동 추론, 모드 카드/데이터 다운로드/점수 단위 4언어, MultilingualFeedback 단일 토글 |
| `stage19-d7-d10-bilingual.test.ts` | 6 | Q4 grammar_note BilingualText 적용, 자유 대화 reason CorrectionReason 타입 + BilingualText, emphasize 모드 핵심 속성 |
| `stage19-llm-tense-severity.test.ts` | 10 | 시제 검증 규칙, minor 포맷 단순화, 다중 오류 우선순위, Q4에도 적용 |

신규 e2e 보호 (Playwright chromium):
- `pdf-download.spec.ts`: PDF 매직 바이트 검증
- `console-errors.spec.ts`: 공개 6페이지 console.error 0건

---

## 9. 모든 커밋

```
bcb2872 [단계19-Phase4] L1/L2/L3 디자인 학술 톤 적용
5a7168f [단계19-Phase3] LLM 시제 검출 + severity 정책 강화
d7cd171 [단계19-Phase2b] D7/D10 BilingualText 적용 (채팅 버블 grammar_note)
3bd478e [단계19-Phase2a] D6/D6.2/D6.3/D6.4/D6.5/D8.1 다국어 일관성
3f393a7 [단계19-Phase1] PDF·아바타·Issues 회귀 복구
```

---

## 10. 다음 단계 권장 (단계 20)

1. **L4 SVG 로고**: 디자이너 원본 의뢰 또는 Inkscape/Illustrator로 수동 트레이싱.
2. **L1 OPIc/TOPIK 전면 리디자인**: 종합 점수 박스 + 데이터 테이블 미학 + 학술 transcript 톤.
3. **D10 BilingualText 전수 확장**: 자유 대화 요약 카드, 말하기 평가 결과 카드(피드백·모범 표현·평가 근거·강점·보완점).
4. **D6.6**: 학생·교수자·관리자 페이지 max-width 일관성 점검.
5. **단계 18에서 보류한 항목 추적**: `.env.local.example` 커밋 (`.gitignore` 정책 검토), 도넛 차트 내부 라벨 i18n (현재 페이즈 19에서 범례만 i18n).
