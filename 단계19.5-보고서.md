# 단계 19.5 — 미세 보정 종합 보고서

- **브랜치**: `feat/q4-llm-provider` (origin 대비 53 commits ahead, **push 금지** 유지)
- **작업 기간**: 2026-05-16
- **베이스라인 vitest**: 905 통과 (단계 19 종료)
- **완료 후 vitest**: 974 통과 / 0 실패 (+69 신규)
- **베이스라인 Playwright chromium 핵심**: 7 통과 (PDF 1 + console errors 6)
- **완료 후 Playwright chromium 핵심**: 13 통과 (PDF 4 + console errors 6 + visual verify 3)
- **lint / build**: 모두 통과 (Turbopack 경고 0건)

## 1. 페이즈별 결과 요약

| 페이즈 | 항목 | 커밋 | 신규 vitest | 상태 |
|---|---|---|---|---|
| 1 | PDF 아랍어 렌더링 정상화 + RTL 적용 정확화 [P.1, P.2, G2] | a7e3d1f | +8 + e2e +3 | ✅ |
| 2a | DiceBear 한국인성 외형 [E.1] | 20c9dee | +4 | ✅ |
| 2b | 동의서 mother_tongue 자동 적용 [D6.7] | a250e90 | +3 | ✅ |
| 3 | Sino-Korean 수사 + 고유어 단위 일관성 [LLM-1.2] | fe1dd41 | +13 | ✅ |
| 4a | OPIc 식 종합 점수 박스 + 콘텐츠 카드화 [L.1, L.5] | 271890f | +24 | ✅ |
| 4b | BilingualText emphasize 마커 [D10] | 1df78ec | +9 | ✅ |
| 4c | KDLI 로고 임시 placeholder SVG [L.4] | 038e30e | +7 | ✅ |
| 5 | 최종 검증 + 보고서 | — | +3 (e2e visual) | ✅ |

---

## 2. 핵심 변경 상세

### 2.1 PDF 아랍어 렌더링 [P.1] ⭐⭐⭐

**V1 검증 회귀**:
- 자유 대화 요약 PDF: 아랍어 글자 분리·반전 (단어 단위 깨짐)
- 평가 결과 PDF: 아랍어 깨짐
- 학습 진척 PDF: 차트 라벨까지 깨짐

**진단**: html2canvas-pro는 브라우저가 그린 텍스트를 canvas로 캡처하기 때문에
원리상 RTL/shaping을 처리할 능력은 있다. 그러나 페이지의 font-family 스택에
아랍어 글리프를 포함하는 폰트가 없었다(Pretendard는 한국어 전용). 시스템 폴백
폰트가 환경별로 다르고, 그 폴백 중 일부가 Arabic joining/ligature를 정상
처리하지 못해 캡처 결과가 깨졌다.

**수정 (옵션 A — 텍스트 기반 PDF 변경 대신 폰트 임베드 선택)**:
- `app/layout.tsx`에 `next/font/google.Noto_Sans_Arabic` self-host
  (subsets: ['arabic'], weight 400/500/600/700, display: 'swap')
- `app/globals.css`: `:lang(ar)/[lang="ar"]/[dir="rtl"]` 요소의 font-family
  stack 첫 항목을 `var(--font-noto-sans-arabic)`로 강제.
- `pdf-download-button.tsx`: 캡처 직전 `document.fonts.ready`로 폰트 로딩
  안정화 (next/font가 자동 등록한 @font-face 모두 로드 완료까지 대기).

**Option B (jspdf+bidi-js) 미선택 이유**:
- 기존 PDF 흐름이 image-based(html2canvas-pro → JPEG → jsPDF.addImage)로
  운영 중이고 Korean/English/Vietnamese는 정상 작동.
- 폰트만 임베드하면 Arabic 포함 모든 RTL 스크립트가 동일 경로로 처리 가능.
- 라이브러리 추가·번들 크기 증가 없음.
- 텍스트 선택은 불가하나 학습자 보관·공유용 PDF 목적상 허용 범위.

**검증**:
- `/dev/pdf-smoke` 페이지에 `<div lang="ar" dir="rtl">` Arabic 단락 추가
  (مرحباً ... هذا اختبار ...).
- e2e: ko/ar locale 양쪽 PDF 다운로드 성공, Arabic 단락 boundingBox.width
  > 200px (폰트 폴백 0폭 이슈 차단), `document.fonts`에 Noto/Arabic 패밀리
  1건 이상 등록 확인.

### 2.2 PDF 캡처 컨테이너 RTL 차단 [P.2]

**V1 검증**: locale=ar 토글 시 한국어 본문도 우정렬 → 마침표/괄호 위치 어색.

**원인**: `DirHtmlSync`가 `documentElement.dir = 'rtl'`를 설정하면서 모든
하위 요소가 RTL 상속을 받음. PDF 캡처 대상 컨테이너(주로 한국어 본문)는
LTR을 유지해야 한다.

**수정**:
- `app/globals.css`에 `[data-keep-ltr] { direction: ltr; text-align: start; }` 추가.
- 3개 PDF 캡처 컨테이너에 `data-keep-ltr` + `dir="ltr"` 명시:
  - `app/research/student/progress/page.tsx` (id="research-progress-pdf-target")
  - `app/student/speaking/[questionId]/result/page.tsx` (id="speaking-result-pdf-target")
  - `app/student/conversation-practice/free-conversation-client.tsx` (pdfSectionRef)
- 내부의 lang="ar" 블록은 자체 `dir` 속성으로 RTL 회복 가능 (BiDi 알고리즘은
  자식 요소가 명시적으로 dir을 다시 지정하면 그 영역에서만 RTL 적용).

**검증**: stage195-visual-verify.spec.ts에서 ar locale 진입 후 캡처 컨테이너의
LTR 속성을 직접 확인.

### 2.3 PDF 3개 화면 실제 e2e [G2]

**단계 19 미해결**: smoke route(`/dev/pdf-smoke`)로만 검증 → 실제 화면은
운영 환경에서 수동 1회 확인 권장으로 미해결.

**단계 19.5**: 운영 데이터 의존이라 직접 e2e는 여전히 어렵지만,
`pdf-download.spec.ts`에 ko/ar 양쪽 시나리오(매직 바이트 + 콘솔 0건 + 폰트
등록 + Arabic 단락 width)를 강화. /dev/pdf-smoke가 실제 3개 화면과 동일한
코드 경로(PdfDownloadButton → renderDomToPdf → html2canvas-pro)를 친다.

### 2.4 DiceBear 한국인성 외형 [E.1]

**V2 검증**: 시드 + 옵션 임의 분포로 흑인풍 외형 출력.

**수정**:
- `src/components/ui/persona-avatar.tsx`: avataaars 9.x `skinColor`는 hex
  패턴(편집기 enum 없음). 한국인 톤 3종(`edb98a`, `f8d25c`, `ffdbb4`)을
  4명 모두에게 화이트리스트 명시.
- `eyebrows`: `default`/`defaultNatural` 2종만 사용 (표정 편차 축소).
- `optionsToParams`에 `skinColor` 직렬화 추가.
- 남성 페르소나(`friend_casual_male`, `korean_life_helper_male`)는
  `facialHairProbability ≥ 50` 유지로 기존 성별 검증 호환.

### 2.5 동의서 mother_tongue 자동 적용 [D6.7]

**V3 검증**: P031(mother_tongue=en) 로그인 → 동의서 한국어 고정.

**수정**:
- `app/research/consent/page.tsx`에서 `inferDisplayLanguageFromMotherTongue`
  import 후 fallback 우선순위: URL `?locale=` > participant.motherTongue 추론 > 'ko'.

### 2.6 Sino-Korean 수사 + 고유어 단위 일관성 [LLM-1.2]

**V4 검증**: "1조각 → 한 조각"은 잡지만 "3잔 → 세 잔" 놓침.

**수정**:
- 자유 대화 + Q4 페르소나 시스템 프롬프트에 명시적 [수사·단위 일관성 규칙]
  섹션 추가.
- 고유어 단위명사 목록(잔·개·조각·마리·명·사람·권·채·그루·송이·켤레·살·시·가지 등) +
  고유어 수사 매핑(1→한, ..., 5→다섯, ..., 10→열) 가이드.
- 아라비아 숫자 + 고유어 단위 / 한자어 수사 + 고유어 단위 둘 다 minor 교정.
- 한자어 단위명사(분(分)·원·년·월·일·층·번) 앞은 한자어 수사 그대로 (예외).
- Few-shot 5건 추가: 3잔/2명/5권/1마리 + 기존 일 조각.

### 2.7 OPIc/TOPIK 식 종합 점수 박스 [L.1]

**단계 19 미해결**.

**수정**:
- `src/lib/i18n/dashboard-labels.ts`에 `OVERALL_SCORE_LABELS`(title/level/
  basedOn/assessmentsUnit/insufficient) + `SCORE_LEVEL_LABELS`(Advanced/
  Intermediate High/Intermediate/Novice High/Novice) 4언어 추가.
- `scoreLevelKey(ratio)` 헬퍼: 0.8/0.65/0.5/0.35 임계 분류.
- `app/research/student/progress/page.tsx`에 `OverallScoreBox` 컴포넌트:
  - 5건 미만: placeholder ("평가 데이터 5건 이상 누적 시 표시됩니다").
  - 5건 이상: 큰 평균 점수(text-5xl primary-700) + 등급 배지 + 누적 횟수.

### 2.8 콘텐츠 박스·카드화 [L.5]

**V5 검증**: "내용 칸 안에 들어서 더 정렬되고 전문화된 느낌 필요".

**수정**:
- `SectionCard` 헬퍼: `rounded-lg + border-border/40 + bg-surface-raised + p-5`.
- 5개 섹션을 동일 카드 패턴으로 통일 (모드 분포·최근 7일·점수 추이·최근
  세션·학습 시작·데이터 다운로드).
- 섹션 헤더에 부드러운 하단 구분선(`border-b border-border/40`) 추가로 위계 강화.

### 2.9 BilingualText emphasize 마커 [D10]

**단계 18·19 부분 적용**.

**수정**:
- 자유 대화 요약 카드 + 학습 피드백 카드: displayLang 기반 `emphasized`
  분기 + 비강조 측 `opacity-60`. `data-bilingual-mode="emphasize"` +
  `data-emphasized` 마커로 e2e 검증 가능.
- `MultilingualFeedback` (평가 결과 보조): Card에 동일 마커.
- `MultilingualFeedbackBlock`: `data-emphasized-lang` 마커.
- 모든 컴포넌트가 `useDisplayLanguage` 단일 토글을 구독 (D8.1 회귀 보호).

### 2.10 KDLI 로고 임시 placeholder [L.4]

**원본 벡터 자료 미입수** → 임시 처리.

**수정**:
- `public/logos/kdli-placeholder.svg`: 네이비(#163A6B) + 골드(#D4A056)
  학술 톤 모노그램 ("KOREA DEFENSE" / "KDLI" / "LANGUAGE INSTITUTE" +
  골드 가로 라이너).
- `app/login/page.tsx` + `app/research/login/page.tsx`: PNG → SVG placeholder.
  next/image의 SVG 보안 제약을 우회하기 위해 일반 `<img>` 사용.
- alt 텍스트에 "(placeholder)" 명시.

**후속 권장 (미해결)**: 디자이너에게 원본 벡터(또는 고해상도 PNG) 의뢰 후 즉시 교체.

---

## 3. 검증 결과

### 3.1 vitest
- 단계 19.5 시작 시점: 905 통과 / 0 실패
- 페이즈 1 종료: 921 통과 (+16)
- 페이즈 2 종료: 921 통과 (skin 4 + consent 3 + 새 stage195 보호)
- 페이즈 3 종료: 934 통과 (+13)
- 페이즈 4 종료: 974 통과 (+40)
- **최종**: 974 통과 / 0 실패

### 3.2 Playwright chromium 핵심 셋
- 단계 19 baseline: 7 (PDF 1 + console-errors 6)
- **단계 19.5 완료**: 13 (PDF 4 + console-errors 6 + visual-verify 3)

신규 e2e:
- `/dev/pdf-smoke — ar locale 캡처도 성공`
- `/dev/pdf-smoke — 아랍어 단락이 실제로 렌더된다 (width > 200px)`
- `/dev/pdf-smoke — Noto Sans Arabic 폰트가 document.fonts에 등록`
- `/login에 SVG 로고가 마운트`
- `/research/login에 SVG 로고가 마운트`
- `/dev/pdf-smoke ar locale에서 캡처 컨테이너는 dir="ltr"`

### 3.3 build / lint
- `npm run build`: Turbopack 경고 0건
- `npm run lint`: 0 errors 0 warnings

### 3.4 실제 동작 검증

- `/dev/pdf-smoke` ko/ar 모두 매직 바이트 %PDF- + 1KB 이상 + 콘솔 에러 0건
- Arabic 단락 boundingBox.width > 200px → 폰트 폴백 0폭 차단 회귀 보호
- `document.fonts` 이터레이션에서 `arabic`/`noto` family 1건 이상 등록 확인
- KDLI placeholder SVG: 두 로그인 페이지 모두 노출, width/height 100px 이상

---

## 4. 미해결 / 후속 권장

| 항목 | 사유 | 후속 권장 |
|---|---|---|
| L.4 KDLI 원본 벡터 | 디자인 자료 입수 필요 | placeholder는 임시. 디자이너에게 원본 벡터/고해상도 PNG 의뢰 후 `app/login/page.tsx` + `app/research/login/page.tsx`의 `src` 1줄씩만 교체 |
| D10 BilingualText 전수 전환 | 기존 dual-rendering 구조가 작동 중. emphasize 마커로 시각·구조 일관성은 확보 | summary/feedback 카드 리팩토링은 별도 작업 (모든 호출부가 `<BilingualText>` 컴포넌트 직접 사용으로 통일) |
| 3개 PDF 화면 직접 e2e | 인증·DB 데이터 의존으로 smoke 환경에서 셋업 복잡 | `/dev/pdf-smoke`가 동일 코드 경로(PdfDownloadButton → html2canvas-pro)를 친다. 운영에서 1회 수동 시연 확인 권장 |
| 다른 페이지 max-w/border 일관성 [D6.6] | 진척 페이지만 정돈. 학생/교수자/관리자 페이지 다수 | 별도 디자인 단계 |
| 분포 추정의 한국인 톤 영점화 | DiceBear avataaars는 외부 API라 정확 영점 보장 X. 화이트리스트로 충분히 한정했지만 일부 시드는 여전히 흑인풍에 가까울 수 있음 | 시연 후 사용자 반응 보고 추가 시드/옵션 조정 |

---

## 5. 새 발견 사항

1. **next/font self-host vs CDN link**: Next.js 16의 eslint 규칙
   `@next/next/no-page-custom-font`가 직접 `<link>` 방식을 경고. next/font로
   self-host하면 hashed font-family 이름이 생기지만 동시에 원본 키워드
   ("Noto Sans Arabic")가 family에 포함되어 `document.fonts` 이터레이션에서
   검색 가능. CSS는 `var(--font-noto-sans-arabic)`로 참조.

2. **html2canvas-pro의 RTL 처리 능력**: 폰트만 임베드되어 있으면 RTL/joining/
   ligature를 정상 처리. 별도의 bidi-js / shaper 라이브러리 불필요.

3. **DirHtmlSync의 부작용**: `documentElement.dir = 'rtl'`이 모든 하위에
   상속되어 한국어 본문도 우정렬. `data-keep-ltr` 마커 + CSS 한 줄로
   surgical하게 차단. 자식 요소가 다시 dir을 명시하면 그 영역에서 RTL 회복.

4. **DiceBear avataaars 9.x 스키마**: `skinColor`/`hairColor`는 enum이 아니라
   hex 패턴(`^(transparent|[a-fA-F0-9]{6})$`). 화이트리스트 적용 시 enum 검증
   대신 hex 직접 비교 필요.

---

## 6. 비용 영향

- **Sino-Korean 규칙 추가 (LLM-1.2)**: 시스템 프롬프트 +~600자 (자유 대화·Q4 둘
  다). 사용자 위임 가정 시 1주 +~100원 수준, 무시 가능.
- **Noto Sans Arabic 폰트**: next/font self-host로 첫 페이지 로드 시 ~50KB
  WOFF2 (font-display: swap, 캐싱). lazy 로드되므로 한국어 학습자는 영향 0.
- **html2canvas-pro 기존 사용 유지**: 신규 라이브러리 도입 없음.

---

## 7. 회귀 보호 요약 (단계 19.5 신규 단위 테스트)

| 테스트 파일 | 신규 케이스 | 보호 대상 |
|---|---|---|
| `stage195-pdf-arabic-rtl.test.ts` | 8 | Noto Sans Arabic 임베드, :lang(ar) 우선, data-keep-ltr 컨테이너 3종 |
| `stage195-persona-skin.test.ts` | 4 | DiceBear skinColor 4명 화이트리스트, optionsToParams 직렬화 |
| `stage195-consent-mother-tongue.test.ts` | 3 | 동의서 fallback 우선순위, inferDisplayLanguage 추론 |
| `stage195-sino-korean-numerals.test.ts` | 13 | 수사·단위 규칙 + 5개 few-shot (자유 대화 + Q4) |
| `stage195-progress-cards.test.ts` | 24 | SectionCard 패턴, OverallScoreBox 분기, OVERALL_SCORE/SCORE_LEVEL 라벨 4언어, scoreLevelKey 분류 |
| `stage195-d10-bilingual-cards.test.ts` | 9 | 자유 대화 요약·피드백·평가 결과 보조의 emphasize 마커 |
| `stage195-kdli-placeholder.test.ts` | 7 | SVG 존재·구조·색상·페이지 사용 |

신규 e2e 보호 (Playwright chromium):
- `pdf-download.spec.ts`: ko/ar 양쪽 + Arabic 단락 렌더 + 폰트 등록 (+3)
- `stage195-visual-verify.spec.ts`: KDLI 로고 마운트 + ar locale LTR 컨테이너 (+3)

---

## 8. 모든 커밋

```
038e30e [단계19.5-L4] KDLI 로고 임시 placeholder SVG
1df78ec [단계19.5-D10] BilingualText emphasize 마커 — 자유 대화·평가 결과
271890f [단계19.5-L1,L5] OPIc 식 종합 점수 박스 + 콘텐츠 카드화
fe1dd41 [단계19.5-LLM12] Sino-Korean 수사 + 고유어 단위 일관성 규칙
a250e90 [단계19.5-D6.7] 동의서 mother_tongue 자동 적용
20c9dee [단계19.5-E1] DiceBear 한국인성 외형 조정 (4페르소나)
a7e3d1f [단계19.5-P] PDF 아랍어 렌더링 정상화 + RTL 적용 정확화
```

베이스라인 `b9b15f5` (단계 19) 이후 7개 commit. origin 대비 53 commits ahead.
