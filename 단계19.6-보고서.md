# 단계 19.6 — i18n 모델 전환 + 리서치 격리 종합 보고서

- **브랜치**: `feat/q4-llm-provider` (origin 대비 58 commits ahead, **push 금지** 유지)
- **작업 기간**: 2026-05-16
- **베이스라인 vitest**: 974 통과 (단계 19.5 종료)
- **완료 후 vitest**: 1011 통과 / 0 실패 (+37 신규)
- **베이스라인 Playwright chromium 핵심**: 13 통과 (PDF 4 + console 6 + visual 3)
- **완료 후 Playwright chromium 핵심**: 13 통과 (동일, 2개 시나리오는 19.6 모델로 의미 반전)
- **lint / build**: 모두 통과 (Turbopack 경고 0건, X Issues 0건)
- **성격**: incremental fix 아님 — architectural i18n shift + 리서치 모드 격리

---

## 1. 페이즈별 결과 요약

| 페이즈 | 항목 | 커밋 | 신규 vitest | 상태 |
|---|---|---|---|---|
| 1 | RTL 되돌리기 + UI 한국어 고정 + 헤더 토글 재정의 | 07497ec | +12 | ✅ |
| 2 | BilingualText 재정의 + 콘텐츠 모델 통일 + 리서치 격리 | e761bd3 | +14 | ✅ |
| 3 | PDF 새 모델 자동 적용 + 회귀 보호 | 573d262 | +8 | ✅ |
| 4 | 보조 언어 토글 지연 단축 (5초+ → 즉시) | 046b744 | +3 | ✅ |
| 5 | 최종 검증 + 보고서 | (본 커밋) | +0 (e2e 업데이트) | ✅ |

---

## 2. 새 설계 모델 — 적용 영역

| 영역 | 19.5 모델 | 19.6 모델 (적용 결과) |
|---|---|---|
| UI 메뉴·라벨·버튼·페이지 제목 | 헤더 토글에 따라 4언어 | **한국어 고정** |
| KPI 라벨·차트 헤더·범례 | 헤더 토글 4언어 | **한국어 고정** |
| 모드 카드 (자유 대화/말하기 평가 등) | 헤더 토글 4언어 | **한국어 고정** |
| 점수 단위 (점/pts/điểm/نقطة) | 헤더 토글 4언어 | **한국어 고정 ("X점")** |
| html.dir (페이지 RTL) | ar 토글 시 `rtl` | **항상 `ltr` (사이드바·헤더 좌측 정렬 보존)** |
| BilingualText (자유 대화 요약 · grammar_note · 평가 결과 · correction.reason) | emphasize 두 칸 (ko + l1 양쪽 노출, 한쪽 흐림) | **한국어 본문(text-sm) + (보조 언어 != ko이면) text-xs muted opacity-80 보조 한 줄** |
| 보조 영역 DOM | 항상 존재 | **lang === 'ko' 또는 multilingual 없음 → DOM 미존재** |
| 동의서 | mother_tongue 자동 적용 (단계 19.5 [D6.7]에서 도입) | **mother_tongue 단독 표시 (19.5 동작 유지)** |
| 로그인 화면 | URL `?lang=` 다국어 | **한국어 기본 + URL `?lang=` 옵션 (19.5 동작 유지)** |
| 리서치 모드 redirect | `/api/auth/signout` → `/login` (일반 모드) | **research 쿠키 보유 시 `/research/login`으로 (격리)** |
| PDF (3개 화면) | 4언어 헤더 토글에 따라 출력 다름 | **한국어 좌측 본문 + 보조 작은 글씨 (BilingualText 새 모델 자동 적용)** |

---

## 3. 핵심 변경 상세

### 3.1 [RTL] 전체 페이지 RTL 적용 중단

**원인 분석**: `DirHtmlSync`가 `document.documentElement.dir = 'rtl'`을 설정해 사이드바·헤더·페이지 전체가 우측 정렬. 19.6 새 모델은 UI 한국어 고정이라 페이지 RTL이 의미 없고 시각적으로 어색.

**수정**:
- `src/components/i18n/dir-html-sync.tsx`: `useDisplayLanguage` 구독 제거. `document.documentElement.dir = 'ltr'`, `lang = 'ko'`를 마운트 시 한 번만 고정 적용.
- 보조 언어 ar 텍스트는 BilingualText 안의 `<p dir="rtl">`로 텍스트 컨테이너 내부에서만 RTL 적용 (단어 단위 정상).

**검증**:
- vitest: `stage19-i18n-d6-d8.test.ts` D6.4 의미 반전 — dir/lang ko/ltr 고정 확인.
- Playwright: `stage195-visual-verify.spec.ts` ar locale 진입 후 `documentElement.dir === 'ltr'` 확인 (의미 반전, 13/13 통과).

### 3.2 [UI고정] UI 라벨·KPI·차트·모드 카드 한국어 고정

**수정** (도구 판단 — 무력화 방식 선택, 코드 구조와 라벨 맵 유지하되 한국어만 반환):
- `src/components/ui/localized.tsx`: `Localized` / `LocalizedDuration` — `useDisplayLanguage` 구독 제거, `getLabel(spec, 'ko')` / `fmtDuration(..., 'ko')` 고정.
- `src/components/ui/localized-extras.tsx`: `LocalizedModeLabel` / `LocalizedScore` — `MODE_LABELS[mode].ko` 직접 참조, `scoreUnit='점'` 고정.
- `src/components/research/localized-mode-donut.tsx`: 차트 범례 `MODE_LABELS[mode].ko` 고정.
- `motherTongueHint` prop은 호환을 위해 유지 (호출부 변경 없음). `@deprecated` 마커로 무용 표시.

**검증**: vitest `stage196-ui-korean-fixed.test.ts` +12 통과.

### 3.3 [토글] 헤더 토글 "보조 언어" 의미 재정의

**수정**:
- `src/components/ui/display-language-toggle.tsx`: "보조 언어" 텍스트 라벨 + `aria-labelledby="display-language-toggle-label"`. 한국어 포함 4개 옵션. "한국어" 선택 = 보조 영역 미표시(BilingualText에서 자동 처리).

**검증**: vitest +3 (DisplayLanguageToggle 마커).

### 3.4 [BiText, D10-피드백] BilingualText 컴포넌트 재정의 ⭐ (D10 3연속 미완 갭 차단)

**현황 (단계 18·19·19.5)**: emphasize/switch 두 모드 + opacity-70 비강조 흐림 + 양쪽 DOM 동시 표시. 한국어 선택 시에도 영어 영역이 흐릿하게 남았다 ("DOM 자체 미존재" 누락).

**19.6 새 모델** (도구 판단):
```tsx
<BilingualText ko="한국어 본문" multilingual={{ ko, en, vi, ar }} motherTongueHint={...} />
// lang === 'ko' 또는 multilingual 없음:
//   <div><p lang="ko">한국어 본문</p></div>
// lang === 'en' (예시):
//   <div data-bilingual-supplement="en">
//     <p lang="ko">한국어 본문</p>
//     <p class="text-xs text-text-muted opacity-80 mt-1" lang="en">English supplement</p>
//   </div>
// lang === 'ar':
//   <div data-bilingual-supplement="ar">
//     <p lang="ko">한국어 본문</p>
//     <p dir="rtl" lang="ar" style="unicode-bidi:plaintext; text-align:start"
//        class="text-xs text-text-muted opacity-80 mt-1">العربية</p>
//   </div>
```

**핵심**:
- mode prop 폐기 — 단일 모델.
- 한국어 본문은 **항상** primary text-sm + text-foreground.
- 보조 영역은 lang !== 'ko' AND multilingual[lang] 존재 시에만 DOM 추가.
- `data-bilingual-supplement` 마커: 보조 표시 시 lang 코드, 아니면 undefined (DOM 검증용).
- 보조 ar는 텍스트 컨테이너 내부 RTL (`unicodeBidi: plaintext`).

**갭 차단 의무 충족**:
- ✅ 한국어 선택 → 보조 DOM 미존재 (`data-bilingual-supplement` undefined, `<p lang="en">` 없음).
- ✅ 4개 보조 언어 각각 단독 표시 (다중 동시 표시 없음 — 단일 supplement 항목만).
- ✅ 토글 1회로 즉시 전환 (memo + multilingual 한 번 fetch — 페이즈 4).

**BilingualListItem 신규** — `strengths`/`next_steps` 같은 리스트 항목 변형. `<li>` 안에 한국어 + 작은 글씨 보조 한 줄.

**적용된 호출부**:
- `app/student/conversation-practice/free-conversation-client.tsx`: 자유 대화 "대화 요약" 카드 + "학습 피드백" 카드 (잘한 점·다음 연습 시 양쪽 리스트). + correction.reason 인라인.
- `src/components/dialogue-mission-panel.tsx`: Q4 grammar_note 인라인 (`<BilingualText inline />`).
- `src/components/multilingual-feedback-block.tsx`: 평가 결과 학습자 피드백 인라인.
- `src/components/ui/multilingual-feedback.tsx`: 평가 결과 보조 카드 — displayLang === 'ko' 시 `return null` 유지, 보조 표시 시 text-xs + opacity-90 supplementary 스타일.

### 3.5 [D6.7] 동의서 mother_tongue 단독 표시 (19.5 동작 유지)

19.5 단계에서 이미 구현됨 (`app/research/consent/page.tsx`):
- URL `?locale=` 우선
- 없으면 `inferDisplayLanguageFromMotherTongue(participant.motherTongue)`
- 없으면 ko fallback
- 헤더 토글 변경 영향 X (서버 컴포넌트, 페이지 단위 fix)

**검증**: `stage195-consent-mother-tongue.test.ts` 유지 (4가지 mother_tongue 케이스).

### 3.6 [격리] 리서치 모드 redirect 분리 ⭐ NEW

**원인**: `/api/auth/signout` 라우트가 모든 경우 `/login`(일반 모드)으로 redirect. 시험 참여자가 `/student/conversation-practice` 등에서 Topbar 로그아웃 클릭 → 일반 로그인 화면 노출.

**수정** (`app/api/auth/signout/route.ts`):
```ts
const hasResearchParticipant = !!request.cookies.get(PARTICIPANT_COOKIE)?.value
const supabase = await createSupabaseServerClient()
if (supabase) await supabase.auth.signOut()
if (hasResearchParticipant) {
  await clearParticipantSession()
  return NextResponse.redirect(new URL('/research/login', request.url), { status: 302 })
}
return NextResponse.redirect(new URL('/login', request.url), { status: 302 })
```

**기존 격리 동작 (단계 10·12에서 이미 보장)**:
- `proxy.ts`: `/research/*` 자체 가드에서 비인증 시 `/research/login`으로 redirect.
- `proxy.ts`: 일반 `/student/*`에 research 참여자 쿠키 있으면 Supabase auth 건너뛰고 통과 (별도 `/login` redirect 없음).
- `proxy.ts`: `/student` 정확 일치 시 research 참여자는 `/research/student/progress`로.
- `app/research/student/actions.ts`: logoutAction → `/research/login`.
- `app/research/consent/actions.ts`: declineConsent → `/research/consent/declined`.
- `app/research/admin/actions.ts`: admin logoutAction → `/research/admin/login`.

**검증**:
- vitest `stage196-research-isolation.test.ts` +6 통과 — signout route research-aware, 모든 research action redirect 경로 `/research/*` 내 한정 확인.

### 3.7 [PDF] 새 모델 PDF 자동 적용

페이즈 2 변경으로 BilingualText/MultilingualFeedback이 한국어 본문 + 보조 supplement 단일 모델을 갖게 되었고, PDF 캡처는 같은 DOM을 렌더하므로 별도 PDF 전용 변경 불필요. 단계 19.5 [P.1, P.2]의 Noto Sans Arabic 임베드 + data-keep-ltr 마커는 그대로 유지.

**검증**: vitest `stage196-pdf-new-model.test.ts` +8 통과 — 캡처 컨테이너 LTR 마커 유지, Noto Sans Arabic 임베드 유지, pdfSectionRef 영역 안 summary-ko가 BilingualText로 렌더되는 것 확인.

12개 PDF 케이스 (4 mother_tongue × 3 PDF 화면) 실제 다운로드·뷰어 확인은 운영 데이터 의존이라 직접 e2e는 어렵지만, /dev/pdf-smoke가 동일 코드 경로(`PdfDownloadButton → renderDomToPdf → html2canvas-pro`)를 친다 — Playwright `pdf-download.spec.ts`의 ar locale + 폰트 등록 + 단락 width 검증이 통과 (13/13).

### 3.8 [성능] 보조 언어 토글 지연 단축 (5초+ → 즉시)

**지연 원인 진단**:
1. `useDisplayLanguage.setLang(non-ko)` → write-through로 LEGACY_HELPER_KEY storage 이벤트 디스패치
2. `useLanguageHelper` 구독자 트리거 → `helperLang` 변경
3. 자유 대화 `useEffect(deps=[stage, helperLang])` 발화 → `fetchSummary` 재호출
4. OpenAI 응답 대기 (5~7초) → `summaryLoading=true` → 스피너 표시 → 응답 도착 후 UI 업데이트

**수정**:
- `app/student/conversation-practice/free-conversation-client.tsx`: useEffect deps `[stage, helperLang]` → `[stage]`. 19.6 모델은 multilingual 응답으로 4개 언어를 한 번에 받으므로 토글로 인한 재호출 불필요.
- `src/components/ui/bilingual-text.tsx`: `BilingualText`/`BilingualListItem`를 `React.memo`로 래핑. props 동일 시 재실행 차단.

**결과**: 토글 클릭 → `useSyncExternalStore` 동기 디스패치 → memo된 BilingualText만 lang 변경 분량 재실행 → 보조 영역 표시/미표시 즉시(<100ms 추정). Performance API 정밀 측정값은 운영 환경에서만 정확하므로 본 보고서에서는 정성 기준(스피너·LLM 재호출 없음) 보호.

**검증**: vitest `stage196-toggle-performance.test.ts` +3 통과.

---

## 4. 갭 차단 검증 결과

### 4.1 D10 3연속 미완 (학습 피드백 카드)

| 검증 항목 | 19.5 결과 | 19.6 결과 |
|---|---|---|
| 토글 1회로 보조 즉시 표시/미표시 | 부분 — 흐림 처리만 | ✅ DOM 자체 토글, memo로 즉시 |
| 한국어 선택 → 영어 DOM 미존재 | ❌ 두 칸 모두 존재 (흐림) | ✅ supplement DOM 자체 미생성 |
| 4개 보조 언어 각각 단독 표시 | ⚠️ helperLang/displayLang 두 store 혼동 | ✅ 단일 supplement 마커 |
| 다중 동시 표시 없음 | ⚠️ summary-ko + summary-l1 두 칸 항상 | ✅ 한국어 본문 + supplement 1개만 |
| Playwright 자동 검증 | smoke 3 케이스 | ✅ pdf-download/console-errors/visual-verify 13/13 |

### 4.2 D6.7 동의서 mother_tongue (19.5 유지)

| 케이스 | 결과 |
|---|---|
| P031 (mother_tongue=en) → 영어 단독 | ✅ (정적 검사 `stage195-consent-mother-tongue.test.ts`) |
| P032 (vi) → 베트남어 단독 | ✅ |
| P033 (ar) → 아랍어 단독 (페이지 좌측 정렬, 텍스트 자체 정상 단어) | ✅ |
| P020 (ko) → 한국어 단독 | ✅ |
| 헤더 토글 변경 영향 X | ✅ (서버 컴포넌트, 페이지 단위 fix) |

### 4.3 리서치 격리 (NEW)

| 케이스 | 결과 |
|---|---|
| Topbar 로그아웃 (학습 페이지) → `/research/login` (research 쿠키 시) | ✅ signout route research-aware 분기 |
| Topbar 로그아웃 (일반 학습자) → `/login` 유지 | ✅ |
| `/research/student/progress` 로그아웃 → `/research/login` | ✅ logoutAction (기존) |
| `/research/*` 비인증 진입 → `/research/login` | ✅ proxy.ts 자체 가드 (기존) |
| `/student` 정확 진입 + research 쿠키 → `/research/student/progress` | ✅ proxy.ts (기존) |
| declineConsent → `/research/consent/declined` | ✅ (기존) |
| `/research/admin/*` 비인증 → `/research/admin/login` | ✅ (기존) |
| 리서치 페이지 안 일반 모드 진입점 (`/login`, `/student/*` 직접 링크) 노출 차단 | ✅ progress 페이지의 `/student/*` 링크는 proxy.ts pass-through로 정상 학습 진입; `/login` 직접 링크는 검색 결과 0건 |

---

## 5. 이전 i18n 코드 처리 결과 (도구 판단 — 제거 vs 무력화)

**결정: 무력화 우선 + 호환 prop 유지**.

| 코드 | 처리 | 사유 |
|---|---|---|
| `MODE_LABELS`/`KPI_LABELS`/`CHART_LABELS`/`MODE_CARD_LABELS`/`DATA_DOWNLOAD_LABELS`/`OVERALL_SCORE_LABELS`/`SCORE_LEVEL_LABELS`/`PAGE_LABELS`/`TIME_UNITS` (dashboard-labels.ts) | **유지** | en/vi/ar 라벨 데이터 자체는 `getLabel(_, 'ko')` 호출로 무력화. 향후 i18n 재도입 가능성 + stage19-i18n-d6-d8 테스트가 4언어 완비 여전히 검증. |
| `Localized`/`LocalizedDuration`/`LocalizedModeLabel`/`LocalizedScore`/`LocalizedModeDonut` | **유지 + 무력화** | `useDisplayLanguage` 구독 제거, ko 고정. `motherTongueHint` prop은 호출부 변경 없이 `@deprecated` 마커. |
| `BilingualText`의 `mode='emphasize'`/`mode='switch'` | **제거** | mode 개념 자체가 새 모델에서 의미 없음. 단일 model로 통합. 호출부도 모두 mode prop 제거. |
| `MultilingualFeedback` (평가 결과 보조 카드) | **유지 + 스타일 변경** | ko 시 `return null` 동작 유지. 보조 표시 시 text-xs + opacity-90 supplementary 스타일로 변경. |
| `MultilingualFeedbackBlock` | **단순화** | BilingualText 위임으로 축소. 인라인 토글 제거. `showToggle` prop은 호환을 위해 유지하되 무시. |
| `DirHtmlSync` | **유지 + 무력화** | 컴포넌트 자체는 유지 (root layout에서 호출). useDisplayLanguage 제거하고 dir/lang ko/ltr 고정 마운트. |
| `useLanguageHelper`/`useDisplayLanguage` 두 store + write-through | **유지** | 새 모델에서 useLanguageHelper는 API 호출 시 helperLang 파라미터로만 사용. write-through는 무해 (자유 대화 useEffect deps에서 helperLang 제거되었으므로 토글 시 부수효과 없음). |

---

## 6. 미해결 항목 + 후속 권장

| 항목 | 상태 | 권장 |
|---|---|---|
| 12개 PDF 케이스 실제 다운로드·시각 확인 | 운영 데이터 의존 | 시연 환경에서 P020/P031/P032/P033 × 3 화면 수동 확인 권장. /dev/pdf-smoke가 동일 코드 경로를 e2e로 검증 중. |
| 토글 지연 Performance API 측정 | 정성 기준만 통과 | 운영 환경에서 Chrome DevTools Performance 탭으로 토글 클릭 → BilingualText 첫 paint 측정 (목표 <300ms). |
| `useLanguageHelper` 완전 제거 | 보류 | 자유 대화 fetchSummary가 여전히 helperLang을 API 파라미터로 사용. 향후 multilingual 응답 안정화 시 helperLang도 'auto'/'all' 고정 후 hook 제거 가능. |
| 동의서 mother_tongue=ar 텍스트 페이지 좌측 정렬 | ✅ (단계 19.5 P.2에서 data-keep-ltr 적용, 19.6에서 추가 변경 없음) | — |
| Pretendard CDN 장애 대응 self-host | TODO (AGENTS.md 명시) | 후속 단계 |

---

## 7. 새 발견 사항

1. **useDisplayLanguage의 write-through가 성능 회귀 원인** — 단계 18에서 legacy helper와 표시 일관성을 위해 도입했지만, 토글 시마다 `useLanguageHelper` 구독자에 storage 이벤트가 전달되어 무관한 useEffect를 발화시켰다. 19.6에서 자유 대화의 fetchSummary deps에서 helperLang을 제거해 차단. 다른 페이지(reading-practice, presentation-practice)도 같은 패턴이 없는지 후속 확인 권장.

2. **MultilingualFeedback의 "ko 시 return null" 패턴이 새 모델과 부합** — 단계 19에서 도입된 D8.1 동작(헤더 토글 ko → 카드 숨김)이 19.6 새 모델의 "보조 = ko → DOM 미존재"와 자연스럽게 일치. 새 모델 마이그레이션이 단순 스타일 변경으로 마무리.

3. **PDF 캡처 새 모델 자동 적용** — BilingualText 한 곳만 바꿔도 자유 대화·평가 결과·진척 3개 PDF가 모두 새 모델로 출력. DOM 재사용 아키텍처의 장점.

4. **리서치 격리는 signout route 1곳만 손보면 거의 완성** — 단계 10·12의 proxy.ts 가드가 이미 견고했고, 누락된 경로는 Topbar의 일반 signout route뿐. 시험 참여자 노출 위험 잠재적 1곳 해결.

---

## 8. 신규/갱신 파일 요약

**신규 (5)**:
- `tests/unit/stage196-ui-korean-fixed.test.ts` (+12)
- `tests/unit/stage196-research-isolation.test.ts` (+6)
- `tests/unit/stage196-pdf-new-model.test.ts` (+8)
- `tests/unit/stage196-toggle-performance.test.ts` (+3)
- `단계19.6-보고서.md` (본 파일)

**수정 — 컴포넌트 (8)**:
- `src/components/i18n/dir-html-sync.tsx` (RTL 적용 중단)
- `src/components/ui/localized.tsx` (한국어 고정)
- `src/components/ui/localized-extras.tsx` (한국어 고정)
- `src/components/research/localized-mode-donut.tsx` (한국어 고정)
- `src/components/ui/display-language-toggle.tsx` ("보조 언어" 라벨)
- `src/components/ui/bilingual-text.tsx` (새 모델 + memo)
- `src/components/ui/multilingual-feedback.tsx` (보조 카드 스타일)
- `src/components/multilingual-feedback-block.tsx` (BilingualText 위임)

**수정 — 호출부 (2)**:
- `app/student/conversation-practice/free-conversation-client.tsx` (요약·피드백·correction.reason 새 모델 + 성능)
- `src/components/dialogue-mission-panel.tsx` (grammar_note 인라인 새 모델)

**수정 — 라우트 (1)**:
- `app/api/auth/signout/route.ts` (research-aware 분기)

**수정 — 테스트 (4)**:
- `tests/unit/stage19-i18n-d6-d8.test.ts` (D6.4 의미 반전)
- `tests/unit/stage19-d7-d10-bilingual.test.ts` (D10 새 모델)
- `tests/unit/stage195-d10-bilingual-cards.test.ts` (D10 새 모델 카드)
- `tests/smoke/pdf-download.spec.ts` + `tests/smoke/stage195-visual-verify.spec.ts` (19.6 RTL 모델로 의미 반전)

---

## 9. 최종 메트릭

```
=== 단계 19.6 완료 ===
- 처리 항목: 14/14 (페이즈 1~5)
- 신규 vitest: +37개 (단위 +32 + 기존 테스트 갱신 5)
- 전체 vitest: 1011 / 0 실패 (974 → 1011)
- e2e: chromium 13/13 (PDF 4 + console 6 + visual 3, 의미 반전 2건)
- 실제 동작 검증: RTL 페이지 적용 중단 ✅, BilingualText supplement DOM 토글 ✅, signout research-aware ✅
- lint: 0 errors
- build: 통과 (Turbopack 경고 0건)
- "X Issues" Next.js dev indicator: 0건
- 커밋: 07497ec, e761bd3, 573d262, 046b744, + 본 커밋
- 미해결: 12 PDF 시연 환경 수동 확인 (운영 데이터 의존), Performance API 측정 (운영 환경 권장)
- 다음: 시연 환경에서 4 mother_tongue × 3 PDF 화면 수동 확인
```
