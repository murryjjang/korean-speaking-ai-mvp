# 단계 19.14 — 단계 19.13 후 회귀 2건 긴급 정정 보고서

작성일: 2026-05-20
브랜치: `feat/q4-llm-provider`
범위: R1·R2 회귀 정정 + 동적/재로그인 시각 검증 보강

---

## 1. 요약

| 항목 | 회귀 | 본질 | 정정 결과 |
|---|---|---|---|
| R1 | 자유 대화 주제 선택 후 "다음" 버튼 비활성화 | 버튼 disabled prop 정상, 모바일 fixed 하단 내비가 다음 버튼을 가려 "도달 불가" | 양쪽 액션 버튼 sticky bottom + scrollIntoView + 터치 영역 보강 |
| R2 | 재로그인 시 모바일 학습 시작 CTA "맨 아래로" | mobile-top과 모바일 하단 내비의 breakpoint 불일치 (sm vs md) — 640~767px 구간에서 mobile-top 숨김 + 데스크톱 instance 노출 동시 발생 | 두 instance 모두 md breakpoint로 정렬 |

회귀 영향: 시험운영 중 V3(발음·pause LLM 피드백) 검증이 R1으로 차단됨. 본 단계 종료 후 재개 가능.

---

## 2. 페이즈별 결과

### 페이즈 0 — 회귀 원인 진단 (커밋 `aacf9d7`)

신규 도구: `tests/smoke/stage1914-regression-diagnosis.spec.ts`

#### R1 진단
- Playwright 시나리오: P062 ar 로그인 → /student/conversation-practice → 첫 주제 카드 클릭 → "다음" 버튼 상태/클릭/이동 검증.
- 결과: `disabled=false`, `aria-pressed=true`, click → 페르소나 화면 정상 이동. **disabled prop은 정확.**
- 시각 단서: `/tmp/단계19.14/screenshots/r1-02-topic-clicked.png` — 모바일 375px에서 9개 카드 중 3개만 viewport 안. "다음" 버튼은 카드 그리드 아래 + 모바일 fixed 하단 내비 뒤에 위치.
- 결론: 사용자 "비활성화" 인식 = "버튼 도달 불가" (스크롤 후에도 하단 내비에 가려짐).

#### R2 진단
- Playwright 시나리오: 375px에서 첫 로그인 → 로그아웃 → 재로그인 → 새로고침, 3개 시나리오에서 mobile-top 위치 측정.
- 결과: 3개 시나리오 모두 `mobile-top y=228`, viewport 내. **동일 viewport에서는 재현 안 됨.**
- 코드 단서: 단계 19.13 페이즈 3 코드에서
  - mobile-top: `sm:hidden` (<640px에서 노출)
  - 데스크톱 instance: `hidden sm:block` (640px+에서 노출)
  - 모바일 하단 내비(Sidebar): `md:hidden` (<768px에서 노출)
  - 640~767px 구간에서 mobile-top 숨김 + 데스크톱 노출 + 모바일 내비 노출 동시 발생.
- 결론: 사용자가 재로그인 직후 모바일 viewport 스케일 변동/외부 키보드 도크·언도크 등으로 700px 부근에 진입했을 때 학습 시작 CTA가 페이지 맨 아래로 보이는 회귀.

---

### 페이즈 1 — R1 정정 (커밋 `866ec9d`)

수정: `app/student/conversation-practice/free-conversation-client.tsx`

1. **scrollIntoView** — `selectedCardLabel` 또는 `customTopic`이 truthy로 바뀌면 해당 액션 버튼을 `{ block: 'center', behavior: 'smooth' }`로 viewport 중앙에 끌어옴. 카드 클릭/텍스트 입력 즉시 사용자에게 버튼이 보임.
2. **sticky bottom 액션 바** — 양쪽 "다음" 버튼 영역에 적용:
   ```tsx
   <div
     className="sticky -mx-5 px-5 py-3 bg-surface-raised border-t border-border z-10"
     style={{ bottom: 'calc(env(safe-area-inset-bottom, 0px) + 52px)' }}
   >
   ```
   - 모바일 하단 내비 높이(min-h-44 + 여유 8px) + iOS safe-area 합산 offset.
   - md+에서는 하단 내비 부재이나 sticky offset은 유지 (시각 회귀 수준 아님).
3. **버튼 자체** — `min-h-[44px]` + `w-full sm:w-auto` — 터치 영역 + 모바일 가시성 보강.

신규 검증: `tests/smoke/stage1914-r1-flow.spec.ts` (4/4)
- 추천 주제 → 페르소나 선택 → 대화 화면 진입
- 직접 입력 → 페르소나 선택 진입
- 말하기 평가 Q1 → "준비 시작" 버튼 작동
- 발표연습 페이지 콘텐츠 노출

---

### 페이즈 2 — R2 정정 (커밋 `4bfb1bf`)

수정: `app/research/student/progress/page.tsx`

```diff
- <div className="sm:hidden" data-testid="start-learning-mobile-top">
+ <div className="md:hidden" data-testid="start-learning-mobile-top">

- <div className="hidden sm:block">
+ <div className="hidden md:block">
```

- 두 instance 모두 `md` breakpoint(768)로 정렬해 모바일 하단 내비의 `md:hidden`과 동일 범위.
- 데스크톱 testid (`nav-free-conversation` 등)는 `hidden md:block` instance에 그대로 유지 — 기존 테스트(Desktop Chrome 1280x720 기본) 호환.

추가 검증 시나리오: `stage1914-regression-diagnosis.spec.ts`에 700px (640~767 구간) 테스트 추가 — mobile-top이 y=228에 정상 노출되는 것을 확인.

---

## 3. 시각 검증

### 인터랙션 후 상태 (R1)

| 시나리오 | 결과 |
|---|---|
| 첫 로그인 후 자유 대화 진입 (mobile 375) | ✅ 9개 카드 + sticky "다음" 버튼 (disabled) |
| 주제 카드 클릭 | ✅ scrollIntoView로 즉시 sticky 버튼 viewport 중앙 (`r1-fix-01-topic-selected-sticky.png`) |
| "다음" 클릭 → 페르소나 페이지 | ✅ `free-conversation-persona-select` 화면 (`r1-fix-02-persona-page.png`) |
| 페르소나 → 대화 진입 | ✅ `free-conversation-chat` + `conversation-input-area` 노출 (`r1-fix-03-chat-entered.png`) |
| 직접 입력 textarea → "다음" 활성화 | ✅ 양쪽 sticky 버튼 동시 노출 (`r1-fix-04-custom-filled.png`) |
| 말하기 평가 Q1 진입 → 준비 시작 | ✅ 버튼 visible + enabled (`r1-fix-05~06`) |
| 발표연습 페이지 | ✅ 콘텐츠 노출 (`r1-fix-07-presentation.png`) |

### 재로그인·새로고침·tablet (R2)

| 시나리오 | 결과 |
|---|---|
| 첫 로그인 (375 mobile) | ✅ mobile-top y=228 (`r2-01-first-login-mobile.png`) |
| 로그아웃 → 재로그인 | ✅ mobile-top y=228 (`r2-02-relogin-mobile.png`) |
| 새로고침 | ✅ mobile-top y=228 (`r2-03-refresh-mobile.png`) |
| 700px (tablet portrait, breakpoint 정렬 검증) | ✅ mobile-top y=228, h=333.6875 — 학습 시작이 상단에 노출 (`r2-04-tablet-700px.png`) |

---

## 4. 단계 19.13 시각 검증 한계 — 자체 점검

| 항목 | 단계 19.13 | 단계 19.14에서 보강 |
|---|---|---|
| 정적 스크린샷 | 23장 (첫 로그인 + 정적 페이지) | 동적 인터랙션 후 상태 캡처 추가 |
| 인터랙션 후 상태 | 누락 | 카드 클릭 → 페르소나 → 대화 진입까지 4단계 캡처 |
| 재로그인 시나리오 | 누락 | 첫 로그인 / 로그아웃 → 재로그인 / 새로고침 3개 시나리오 명시 검증 |
| Breakpoint 횡단 | 모바일 viewport만 (375·412·768) | 640~767px 중간 구간 (700px) 추가 |
| 평가/발표 페이지 버튼 작동 | 정적 캡처만 | 클릭 + 다음 단계 진입 명시 검증 |

→ 단계 19.13 종료 시 vitest 1101 통과 + 정적 스크린샷 OK였으나, 동적·재로그인 회귀를 자동화로 잡지 못한 것이 본 단계의 출발점. 본 단계에서 추가한 인터랙션·시나리오 검증 패턴을 향후 모든 모바일 UX 작업의 기본 검증 묶음으로 유지한다.

---

## 5. 종합 검증

| 검증 | 결과 |
|---|---|
| vitest | 1101/1101 (기존 + 무회귀) |
| lint | 0 errors (4 warnings, 모두 `scripts/`, 19.14 외부) |
| build | ✓ Compiled successfully in 11.1s |
| Playwright stage1914 진단 | 3/3 |
| Playwright stage1914 R1 플로우 | 4/4 |
| Playwright stage1913 mobile-after | 3/3 (회귀 없음) |
| Playwright stage1913 mobile-diagnosis | 3/3 (회귀 없음) |
| Playwright stage198 UI supplement | 8/8 (`nav-*` testid 호환 유지) |
| **합계** | **21/21 + 1101 unit** |

---

## 6. 1차 시험운영 영향

- **차단 → 재개**: R1 정정으로 V3 (발음·pause LLM 피드백) 검증 진행 가능.
- **데이터 영향 없음**: 회귀는 UI/CSS 레벨이며 평가 데이터·세션 데이터에 영향 없음.
- **연구자 안내 권장**: 작업 직후 모바일 시연 한 차례 (P062 ar, P060 en) 직접 검증 후 시험운영 재개.

---

## 7. 커밋 이력 (단계 19.14)

```
4bfb1bf [단계19.14-R2] 재로그인 시 모바일 학습 콘텐츠 위치 회귀 정정
866ec9d [단계19.14-R1] 자유 대화 다음 버튼 가려짐 회귀 정정
aacf9d7 [단계19.14-페이즈0] 회귀 2건 원인 진단
```

다음 단계: 본 보고서 커밋 후 시험운영 재개 신호.
