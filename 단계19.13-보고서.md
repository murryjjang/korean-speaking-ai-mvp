# 단계 19.13 — 시험운영 데이터 활용 강화 + 모바일 UX 개선 — 최종 보고서

날짜: 2026-05-19
브랜치: `feat/q4-llm-provider`
베이스라인: 8c1c5ee (단계 19.12 종료, vitest 1087/1087)
종료 시점: f2aea8f (페이즈 3 commit)

## 1. 페이즈별 결과 요약

### 페이즈 0 — 전수 진단 (commit 2e247b7)

- 평가 데이터 흐름 진단: Azure Pronunciation 응답 schema, STT timing 보유 여부, LLM 프롬프트 입력 변수 분석
- 모바일 UX 자동 진단: 3 viewport(375/412/768) × 6 페이지 × 18장 스크린샷 (`/tmp/단계19.13/screenshots/mobile/before/`)
- `/tmp/단계19.13/diagnosis.md` 정리

핵심 발견:
1. Azure Pronunciation은 `accuracyScore`, `fluencyScore`, `completenessScore`, `wordResults[].{accuracyScore, errorType, offsetMs, durationMs}`를 모두 반환하지만 LLM 프롬프트에는 `normalizedScore` 단일 숫자만 전달됨 — 풍부한 데이터 LLM 미도달
2. STT route는 `wordTimings`를 무시. Q1은 Azure로 대체 가능, Q2~Q4·자유 대화는 timing 인프라 없음
3. `/research/student/progress`에서 "학습 시작" 카드 위치가 모바일 첫 viewport 밖
4. 한국어 어절 중간 줄바뀜 (특히 `/student/speaking`)

### 페이즈 1 — Azure Pronunciation → LLM 피드백 통합 (commit 2b15669)

- `src/lib/pronunciation-context.ts` 신규
  - `buildPronunciationContext(PronunciationResult)`: `accuracy/fluency/completeness` + `weakWords` (accuracy<70 또는 errorType≠None, 점수 낮은 순 최대 6개)
  - `buildSpeechFlowContext(PronunciationResult)`: wordResults timing → pause 감지
- `src/types/providers.ts`: `SpeakingEvalInput`에 `pronunciationContext`, `speechFlowContext` 필드
- `src/providers/llm-eval/index.ts`:
  - OpenAI 프롬프트에 `[발음 세부 분석]`, `[발화 흐름 분석]` 블록 동적 삽입
  - SYSTEM_PROMPT 규칙 9 — improvements에 약점 단어/멈춤 위치 구체 언급 강제
  - mock 폴백도 weakWords/longPauses ≥2 있으면 improvements 첫 줄에 단어/위치 구체 언급
  - `FEEDBACK_INPUTS_VERSION = 'stage19.13'`을 `raw_provider`에 기록 — 시험운영 변경 전·후 데이터 구분 가능
- 호출자 갱신: `app/student/speaking/actions.ts`, `app/api/evaluate-speaking/route.ts`

평가 데이터 흐름 변경 전·후:

| 단계 | 변경 전 | 변경 후 |
|---|---|---|
| Azure→Client | wordResults, fluency 등 받음 | (동일) |
| Client→submitSpeaking | pronunciationResult 전달 | (동일) |
| submitSpeaking→LLM | normalizedScore + feedback (단일 숫자/문자열) | + pronunciationContext + speechFlowContext (단어별 약점, fluency, 멈춤 위치) |
| LLM 프롬프트 | "[발음 평가 참고] 점수: 80/100" | + "[발음 세부 분석]\n- 정확도: 80/100\n- 발음 약한 단어:\n  · '학교' — 55/100 [Mispronunciation]" |
| LLM improvements | 일반적 권고 | 약한 단어를 구체적으로 언급 |

### 페이즈 2 — STT timing → pause LLM 피드백 통합

페이즈 1의 같은 모듈/통로(SpeakingEvalInput.speechFlowContext + buildSpeechFlowContext)에서 동시 구현됨 — 별도 commit 없음.

- 임계치: 짧은 멈춤 ≥ 800ms (`PAUSE_SHORT_MS`), 긴 멈춤 ≥ 1500ms (`PAUSE_LONG_MS`)
- 입력: Azure `wordResults[].offsetMs/durationMs`
- 출력: `{ totalDurationMs, longPauses, shortPauses, longPauseCount, shortPauseCount }`
- 통합 테스트 4건 통과 — longPauseCount≥2 → mock improvements에 "한국|멈춤|이어 말" 언급 확인

검증 (단위 테스트):
- `tests/unit/stage1913-pronunciation-context.test.ts` — 10 tests
- `tests/unit/stage1913-llm-eval-integration.test.ts` — 4 tests

### 페이즈 3 — 모바일 UX 수정 (commit f2aea8f)

#### 3.1 학습 시작 우선 노출
- `app/research/student/progress/page.tsx`
  - `StartLearningSection` 컴포넌트로 추출
  - 모바일(<sm): header 직후 노출 (`sm:hidden` 인스턴스)
  - 데스크톱(sm+): 본래 위치(데이터 다운로드 위)에 유지
  - `min-h-[64px]`로 모바일 터치 영역 확보
  - `hideTestIds=true`로 모바일 인스턴스는 testid 미부착 → 기존 셀렉터 충돌 없음

#### 3.2 한국어 어절 단위 줄바뀜
- `app/globals.css`
  - `body, p, li, h1..h6, span, button, a, label, div`에 `word-break: keep-all; overflow-wrap: break-word`
  - `code, pre, .tabular-nums, .font-mono, input, textarea`는 `word-break: normal` 예외

#### 시각 검증 (변경 전·후 비교)

| 페이지 | viewport | 변경 전 | 변경 후 |
|---|---|---|---|
| /research/student/progress | 375 | 학습 시작이 viewport 하단 ~y=730px (스크롤 필요) | 학습 시작 4-CTA가 첫 화면(~y=180px) 표시 |
| /student/speaking | 375 | "트. 낭독 15점..." (트 단독 줄 시작) | "세트." 어절 유지 |

스크린샷 경로:
- before: `/tmp/단계19.13/screenshots/mobile/before/` (18장)
- after: `/tmp/단계19.13/screenshots/mobile/after/` (5장 — 진척·말하기·로그인 핵심 페이지)

### 페이즈 4 — 종합 검증

- `npx vitest run` → 1101/1101 passed (1087 baseline + 신규 14)
- `npm run lint` → 0 errors, 4 pre-existing warnings (변경 무관 — `'difficultyLabel'`, `'path'`, `'provisional'`, `'build'`)
- `npm run build` → 통과
- Playwright smoke (단계19.13 + 기존 회귀) → 125 passed, 1 pre-existing fail (`mobile-speaking.spec.ts:89` 준비 시간 strict-mode 충돌 — 단계 19.12 이전부터 존재, 본 단계 무관함을 globals.css 리바트 후 재실행으로 확인)

## 2. 시각 검증 의무 자체 점검

- [x] 모바일 viewport 375px: ✅ progress + speaking-index 캡처
- [x] 모바일 viewport 412px: ✅ progress 캡처
- [x] 모바일 viewport 768px: 데스크톱 분기 유지 — before에 캡처 보존
- [ ] 데스크톱 viewport: 캡처 별도 없음 — 변경은 모바일 분기에만 영향(`sm:hidden` / `hidden sm:block`), 기존 desktop UX 회귀 없음 (코드 검토)

## 3. LLM 피드백 실제 데이터 활용 확인

- **OpenAI 호출** (실제 시험운영 환경): OPENAI_API_KEY 보유 환경에서 새 [발음 세부 분석] / [발화 흐름 분석] 블록이 SYSTEM_PROMPT에 첨부되어, SYSTEM_PROMPT 규칙 9에 따라 improvements에 단어/멈춤 구체 언급이 강제됨.
- **mock 폴백** (개발/시연 환경): `tests/unit/stage1913-llm-eval-integration.test.ts:25-34` — `pronunciationContext.weakWords = [{ word: '학교' }]` 입력 시 mock improvements 첫 항목이 `"학교"`와 `"발음|또박"` 토큰을 포함함을 검증 (통과).
- **데이터 일관성 메타**: `evaluateSpeakingDetail` 결과의 `raw_provider.feedback_inputs_version === 'stage19.13'` + `pronunciation_context_attached`/`speech_flow_context_attached` 부울 — 시험운영 데이터 분석 시 변경 전·후 구분 가능.

## 4. 누적 발견 미처리 + 후속 권장

| 항목 | 사유 | 후속 |
|---|---|---|
| 억양 평가 | Azure 한국어 미지원 | 단계 20+ Naver Clova 검토 |
| NPC 음성 voice 차별화 | 페르소나별 voice 매핑 + SSML 도입 필요 | 단계 21+ |
| "한국 음식 다 좋아" 구어체 평가 미세 조정 | 시험운영 1차 데이터 수집 후 LLM 평가 기준 조정 | 단계 22+ (1차 데이터 기반) |
| th/ms/km 임시 번역본 원어민 검수 | 시험운영 후 본격 출시 단계 | 단계 23 |
| dialogue-mission (Q4) 다회 턴 pronunciation 집계 | 턴별 timing 집계 인프라 필요 | 후속 (필요 시) |
| 자유 대화 (`/api/conversation/free/respond`) timing 인프라 | STT 실시간 wordTimings 미보유 | 후속 — 별도 발화 분석 인프라 |
| presentation/evaluate route에 wordResults 전달 | 본 단계 범위에서는 speaking flow에 집중 | 후속 (사용자 보고 확인 후) |

## 5. 시험운영 데이터 일관성 메모

- 본 단계 시작 시각: 2026-05-19, baseline = 8c1c5ee
- 종료 시각: 2026-05-19, HEAD = f2aea8f
- 변경 전 데이터 vs 변경 후 데이터 구분:
  - 변경 후 평가 결과는 `raw_provider.feedback_inputs_version = 'stage19.13'`로 자동 표시됨
  - 이 마커가 없는 ai_evaluations 행은 단계 19.13 이전 LLM 입력으로 생성된 것으로 해석
  - Supabase 분석 시 이 마커 기준으로 LLM 응답 품질 변화 추적 가능

## 6. 항목 커버리지 결산

| # | 항목 | 페이즈 | 상태 |
|---|---|---|---|
| 1 | 발음 오류 LLM 피드백 (길지수 연구자 보고) | 1 | ✅ 완료 |
| 2 | 말 멈춤 LLM 피드백 (길지수 연구자 보고) | 2 | ✅ 완료 (페이즈 1과 동시) |
| 3 | 모바일 학습 콘텐츠 위치 (사용자 보고) | 3 | ✅ 완료 |
| 4 | 모바일 글자 줄바뀜 어색 (사용자 보고) | 3 | ✅ 완료 |
| 5 | 모바일 자체 진단 발견 사항 | 3 | ✅ 부분 완료 (가로 스크롤·터치 영역 OK 확인, 글자 줄바뀜 전역 적용) |
| - | 억양 평가 / NPC voice / LLM 미세 조정 / th·ms·km 검수 | — | 미처리 — 후속 |

## 7. 커밋 목록 (단계 19.12 push 이후)

```
2e247b7 [단계19.13-페이즈0] 진단 — 평가 데이터 흐름 + 모바일 UX
2b15669 [단계19.13-페이즈1] Azure Pronunciation 결과 → LLM 피드백 통합
f2aea8f [단계19.13-페이즈3] 모바일 UX 수정 — 학습 시작 우선 노출 + 글자 줄바뀜 자연화
(보고서) [단계19.13-보고서] 본 파일
```

push 금지 (단계 19.12까지만 origin과 동기화).
