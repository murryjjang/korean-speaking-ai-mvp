# 단계 19.7 — 페이즈 0 진단 보고서

**작업 대상**: D6.7 동의서 4연속 미완, PDF 아랍어 2연속 깨짐, PDF 보조 언어 100% 한국어 출력.

**원칙**: 자체 검증 ≠ 실제 사용자 경로. 단계 18·19·19.5·19.6의 자체 검증은 URL `?locale=` 우회를 사용한 가능성이 높고, 그 경로는 실제 로그인을 거치는 사용자 경로와 다르다. 진단은 실제 코드 흐름을 따라가서 작성한다.

---

## 0.1 D6.7 동의서 4연속 미완 — 근본 원인 발견

### 자체 검증에서는 통과한 이유

`app/research/consent/page.tsx:118-130`을 보면 mother_tongue 추론 로직이 분명히 존재한다:

```typescript
const inferred = inferDisplayLanguageFromMotherTongue(participant.motherTongue)
const locale: ConsentLocale = isConsentLocale(params.locale)
  ? params.locale
  : (inferred && isConsentLocale(inferred) ? inferred : 'ko')
```

즉 **URL `?locale=`이 우선, 없으면 mother_tongue 추론, 폴백 `ko`**. URL 파라미터 없이 호출하면 mother_tongue으로 자동 분기되어야 한다.

자체 Playwright 검증이 `/research/consent?locale=en` 또는 URL 파라미터 없이 직접 진입했다면 통과. **그러나 실제 사용자는 로그인을 거치므로 그 경로엔 URL 파라미터가 강제 부착된다.**

### 4연속 실패의 진짜 코드 위치

**`app/research/login/page.tsx:105-122`**:

```typescript
export default async function ResearchLoginPage({ searchParams }) {
  const params = await searchParams
  // L109: 로그인 페이지 UI 언어 토글의 lang 값 (기본 'ko')
  const lang: Lang = isLang(params.lang) ? params.lang : 'ko'
  ...
  const action = async (formData: FormData) => {
    'use server'
    ...
    // L121: ★★★ 여기서 항상 ?locale=${lang} 강제 부착 ★★★
    redirect(ok.consentRequired ? `/research/consent?locale=${lang}` : redirectTo)
  }
}
```

`lang`은 로그인 화면의 UI 언어 토글 값으로, **사용자가 토글을 만지지 않으면 기본 `ko`**다.

### 실제 사용자 경로 추적

1. P041 (mother_tongue='en') 사용자가 `/research/login`에 진입 — 한국어 UI (기본)
2. 자격증명 입력 후 로그인 버튼 클릭
3. `loginWithCode` 성공 → `redirect('/research/consent?locale=ko')` (lang='ko'가 강제 부착)
4. consent/page.tsx 진입 → `params.locale === 'ko'` → URL 우선 → mother_tongue 무시 → **한국어 동의서 표시**

### 단계 18~19.6 각 시도가 미작동한 이유

| 단계 | 시도 | 미작동 원인 |
|---|---|---|
| 18 | mother_tongue 힌트를 useDisplayLanguage에 전달 | 동의서 페이지의 URL `?locale=` 우선순위가 압살. 학습 페이지엔 적용됐지만 동의서엔 적용 안 됨 |
| 19 | use-display-language inferDisplayLanguage 추가 | 같음 — 동의서는 RSC 직접 분기, hook 불사용 |
| 19.5 | consent/page.tsx에 inferDisplayLanguageFromMotherTongue 추가 | 코드는 정상이나 login → consent redirect가 URL 파라미터를 항상 부착해 우선순위에서 우회됨 |
| 19.6 | BilingualText 재정의·격리 등 | 동의서는 BilingualText 대상 외(특수 케이스) — 무관계 작업 |

### 자체 검증이 통과한 이유 가설

도구 Playwright가 실제 로그인 흐름을 거치지 않고 직접 `/research/consent`에 진입(쿠키 세팅만으로) — URL 파라미터 없이 호출 → mother_tongue 분기 발동 → "통과" 보고. 하지만 실제 사용자는 로그인 페이지를 거친다.

### 페이즈 2 수정 전략

1. **`app/research/login/page.tsx:121`**: redirect URL에서 `?locale=${lang}` 제거. 단순히 `/research/consent`로.
2. **`app/research/consent/page.tsx`**: URL `?locale=` 우선순위 제거 (또는 backdoor 비활성화). mother_tongue 기반으로만 결정.
3. **mother_tongue 단독 (한국어 본문 X)**: 현재 코드는 이미 단일 locale로 본문 분기 중이므로 mother_tongue → locale 결정 후 본문은 1개 언어만 표시 (한국어 + 보조의 이중 패턴 아님). 단 동의서는 학습 도구가 아니므로 mother_tongue 단독 패턴이 적합.
4. **consent locale toggle UI 제거**: 사용자가 변경할 수 없게.

---

## 0.2 PDF 아랍어 2연속 깨짐 — 기술 결정

### 현재 PDF 생성 흐름

`src/lib/pdf/dom-to-pdf.ts:104-170`:

1. `html2canvas-pro`로 DOM element 캡처 → canvas
2. canvas를 A4 페이지 높이로 slice
3. 각 slice를 jsPDF에 image로 추가
4. **이미지 기반 PDF**(텍스트 선택 불가) — 캡처된 그대로 보관·공유용

### 아랍어 폰트 임베드 (단계 19.5에서 추가)

`app/layout.tsx:11-20`: `Noto_Sans_Arabic`을 `next/font/google`로 self-host, CSS 변수 `--font-noto-sans-arabic`.

`app/globals.css:97-106`:
```css
:lang(ar),
[lang="ar"],
[dir="rtl"] {
  font-family: var(--font-noto-sans-arabic), 'Noto Sans Arabic', ...;
}
```

→ DOM에 `lang="ar"` 또는 `dir="rtl"` 요소가 **존재해야** 폰트 셀렉터가 적용된다.

### 아랍어가 깨지는 정확한 단계

1. `next/font`는 페이지에 그 폰트를 사용하는 요소가 마운트되어야 lazy 로드된다 (CSS 변수만 있어도 fetch는 trigger 안 됨 — 실제 사용 시점에 swap).
2. **아랍어가 깨지는 시나리오 1**: P043(ar) 사용자가 학습 페이지를 보더라도 페이지 본문(한국어 한정)에 아랍어 요소가 없으면 폰트는 시스템 폴백(글리프 없음) → 깨진 박스/단순 글리프.
3. **아랍어가 깨지는 시나리오 2**: 보조 영역(`BilingualText`의 `supplement`)이 실제로 렌더되었더라도 html2canvas-pro 캡처 직전 `fonts.ready` 대기가 있음(pdf-download-button.tsx:54-56). 그러나 `next/font`는 swap 전략이라 첫 페인트에서 시스템 폴백 텍스트로 그려질 수 있음. 캔버스에 시스템 폴백 글자가 박힘.
4. **html2canvas-pro 한계**: 텍스트 shaping은 브라우저에 의존. 브라우저가 시스템 폰트로 그렸으면 캔버스도 시스템 폰트 결과. Arabic ligature/joining이 빈약한 시스템 폰트면 깨진 채로 raster image가 됨.

### html2canvas-pro로 해결 가능 여부

**가능. 다음 조건이 모두 만족되면**:
1. 캡처 대상 DOM 안에 `lang="ar"` 요소가 실제로 마운트되어 있음 (단계 19.5 BilingualText는 이미 lang/dir 속성을 붙임 — 보조 영역이 렌더되어야 한다는 게 조건)
2. `document.fonts.ready` 보장 (이미 코드에 있음)
3. PDF 캡처 직전 한 번 force-load (예: 비표시 영역에 작은 아랍어 텍스트 prerender) — 폰트 swap 보장

### 마이그레이션 검토 (참고용)

| 옵션 | 장점 | 단점 |
|---|---|---|
| html2canvas-pro 유지 + 강제 폰트 로드 | 코드 변경 최소, 캡처 모델 그대로 | 텍스트 비-선택, 폰트 swap 타이밍 의존 |
| react-pdf / pdfme | 텍스트 선택 가능, 폰트 임베드 명시 | 페이지를 PDF용 컴포넌트로 재작성 필요 — 비용 큼 |
| jsPDF + bidi-js + fontkit | 텍스트 선택 가능, 가벼움 | 페이지 레이아웃 1:1 재작성 필요, 시간 비용 큼 |
| Puppeteer 서버 측 렌더 | 표시 그대로 PDF, 폰트 임베드 명확 | 서버 인프라 추가, MVP 범위 초과 |

**결정**: **html2canvas-pro 유지 + 강제 폰트 로드 보강**. 이유:
- 페이즈 1에서 mother_tongue 단일 진실원이 적용되면 P043(ar) 사용자의 페이지엔 항상 보조 영역이 마운트됨 → lang="ar" 요소 존재 보장
- pdf-download-button에 캡처 직전 폰트 워밍업 추가 (비표시 영역에 lang="ar" 텍스트 prerender, fonts.ready)
- 마이그레이션 비용 vs 시연까지 일정을 고려할 때 최소 수정으로 동작 가능

### 페이즈 3 수정 전략

1. **`src/components/pdf-download-button.tsx`**: `beforeCapture`에 폰트 워밍업 추가 — 캡처 대상에 lang="ar" 요소가 있을 시 폰트 강제 로드 (`document.fonts.load("16px 'Noto Sans Arabic'")`).
2. **mother_tongue=ar 사용자의 페이지**: 보조 영역이 항상 마운트되도록 페이즈 1에서 보장 (mother_tongue 단일 진실원 → SSR 시점에 lang 확정).
3. **검증**: P043(ar) 자격증명으로 학습 세션 진행 → PDF 다운로드 → 파일을 `/tmp/단계19.7-pdf-검증/`에 저장 → pdf-parse 또는 시각적 확인 + 파일 경로 보고서에 첨부.

---

## 0.3 PDF 보조 언어 미적용 (단계 19.6) — 진단

### 단계 19.6 보고서 주장

> "새 모델이 PDF 캡처에 자동 적용"

### 실제 (현재)

100% 한국어 출력 — 보조 언어가 반영 안 됨.

### 원인 진단

`BilingualText` (`src/components/ui/bilingual-text.tsx:25-105`):
- `useDisplayLanguage(motherTongueHint)` hook으로 lang 결정
- `lang === 'ko'` 또는 `multilingual[lang]`이 비어있으면 supplement 영역 자체를 렌더 안 함

`useDisplayLanguage` (`src/hooks/use-display-language.ts:62-79`) 우선순위:
1. localStorage[`kspai:lang:display`] (사용자 명시 선택)
2. inferDisplayLanguageFromMotherTongue(motherTongueHint)
3. DEFAULT_DISPLAY_LANGUAGE='ko'

**시나리오**:
- 사용자가 헤더 `DisplayLanguageToggle`을 만지지 않으면 → localStorage 비어있음 → mother_tongue 기반 → 보조 표시
- **단, mother_tongue 자연어 매칭 실패 시 'ko' 폴백 → 보조 영역 미존재 → PDF에도 미반영**

`inferDisplayLanguageFromMotherTongue` (`src/lib/i18n/display-language.ts:31-48`)의 정규식은 `^(en|eng|english|영어)$/i` 등으로 **엄격 매칭**. mother_tongue이 "English (Native)" 같은 자유 텍스트면 매칭 실패.

### 단계 19.6에서 자동 적용 보고가 잘못된 이유

자체 검증이 헤더 토글을 명시 클릭(localStorage 세팅) 후 PDF 다운로드를 검증했다면 보조가 표시됨 — 통과. 그러나 실제 사용자가 토글을 안 만지면 mother_tongue 매칭에 의존하며, P-시리즈 참여자의 mother_tongue 형식이 'en'/'vi'/'ar' 또는 자연어인지에 따라 결과가 갈린다.

또한 `useDisplayLanguage`는 `useSyncExternalStore`로 client-side hydration 시점에 lang 변경되는데, PDF 캡처가 hook update 후로 보장되더라도 **`getServerSnapshot`** (SSR) 시점에 motherTongueHint를 못 받는 코드 경로가 있을 수 있음 — Topbar는 받지만 BilingualText는 호출부가 motherTongueHint를 명시 전달해야 함.

### 페이즈 1·3 수정 전략

1. **mother_tongue 단일 진실원** (페이즈 1): SSR 시점에 mother_tongue → lang을 RSC props로 직접 주입. useDisplayLanguage hook의 localStorage 우선순위 제거(또는 hook 자체 제거).
2. **헤더 토글 제거** (페이즈 1): 사용자가 변경할 수단 없음 → mother_tongue 단독 결정.
3. **inferDisplayLanguage 강화**: 자연어 mother_tongue도 더 폭넓게 매칭 (예: 부분 일치).
4. **PDF 캡처 시점**: SSR로 lang이 확정되면 첫 렌더부터 보조 영역 존재 → html2canvas-pro 캡처 정상.

---

## 종합 — 페이즈 1~3 수정 위치 표

| 항목 | 파일:라인 | 수정 |
|---|---|---|
| 로그인 redirect 강제 locale 부착 | `app/research/login/page.tsx:121` | `?locale=${lang}` 제거 |
| 동의서 URL 파라미터 우선순위 | `app/research/consent/page.tsx:128-130` | URL 우선순위 제거, mother_tongue 단독 |
| 동의서 locale 토글 UI | `app/research/consent/page.tsx:147-168` | 제거 |
| 헤더 보조 언어 토글 | `src/components/layout/topbar.tsx:50-52` + `src/components/ui/display-language-toggle.tsx` | 제거 |
| useDisplayLanguage hook | `src/hooks/use-display-language.ts` | localStorage 무시 또는 hook 제거, mother_tongue만 |
| inferDisplayLanguageFromMotherTongue | `src/lib/i18n/display-language.ts:31-48` | 자연어 부분 매칭 강화 |
| PDF 폰트 워밍업 | `src/components/pdf-download-button.tsx:46-57` | `beforeCapture`에 force-load |

## 자체 검증 우회 차단 (페이즈 2~4 검증 의무)

페이즈 2~4 각 검증은 **반드시** 다음 경로:
1. 관리자 API로 P040(ko)·P041(en)·P042(vi)·P043(ar) 참여자 생성
2. Playwright가 `/research/login`을 URL 파라미터 없이 방문
3. 자격증명 입력 후 로그인 버튼 클릭
4. redirect 후 표시 언어를 DOM query로 확인 (`data-bilingual-supplement` 속성, lang 속성 등)
5. `page.screenshot()`으로 캡처 → `/tmp/단계19.7/` 경로 보고서에 명시

이전 단계에서 자체 검증이 통과한 그 경로(URL 파라미터·쿠키 직접 세팅)는 **금지**.
