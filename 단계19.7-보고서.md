# 단계 19.7 — 아키텍처 일괄 정리 + 4연속 미완 강제 해결 — FINAL 보고서

**작업 일자**: 2026-05-17
**브랜치**: `feat/q4-llm-provider` (origin 대비 64 commits ahead — push 안 함)
**작업 성격**: incremental fix가 아닌 **아키텍처 일괄 정리** — 4연속 미완(D6.7)과 2연속 깨짐(PDF 아랍어)을 근본 원인부터 해결.

---

## 0. Executive Summary

| 항목 | 단계 18~19.6 | 단계 19.7 |
|---|---|---|
| **D6.7 동의서 mother_tongue 단독** | ❌ 4연속 미완 | ✅ 5번째 시도 — 실제 사용자 경로 4 mother_tongue 모두 통과 |
| **PDF 아랍어 렌더링** | ❌ 2연속 깨짐 | ✅ Noto Sans Arabic 폰트 강제 로드 — 116KB PDF 생성, 콘솔 에러 0건 |
| **PDF 보조 언어 적용** | ❌ 100% 한국어 | ✅ mother_tongue 단일 진실원 SSR 적용 — localStorage 우선순위 제거 |
| **자체 검증 ≠ 실제 사용자 경로** | URL `?locale=` / localStorage 우회로 가짜 통과 | URL 파라미터 없는 실제 로그인 경로 + 스크린샷·PDF 증거 첨부 |
| **vitest** | 1011/1011 | **1014/1014** (신규 회귀 보호 3건) |
| **Playwright 핵심 + 19.7 신규** | 13/13 | **22/22** (핵심 13 + 신규 9) |

핵심 변경: **헤더 토글 완전 제거** + **mother_tongue이 평생 단일 결정자**.

---

## 1. 페이즈 0 — 진단 결과 (4연속 실패의 진짜 근본 원인)

### 1.1 D6.7 동의서 4연속 실패의 진짜 코드 위치

**`app/research/login/page.tsx:121`** (단계 19.7 수정 전):

```typescript
redirect(ok.consentRequired ? `/research/consent?locale=${lang}` : redirectTo)
```

- `lang`은 로그인 페이지 UI 토글 값으로 기본 `'ko'`
- **로그인 server action이 모든 redirect URL에 `?locale=${lang}` 강제 부착**
- `consent/page.tsx`의 mother_tongue 추론은 URL `?locale=` 우선순위에 막혀 무력화
- **자체 Playwright 검증이 URL 파라미터 없이 직접 진입했기 때문에 통과 보고가 가능**했지만, 실제 사용자는 로그인을 거치므로 URL이 항상 `?locale=ko`로 강제됨

### 1.2 PDF 아랍어 깨짐의 진짜 원인

- `app/layout.tsx:11-20`에서 `Noto_Sans_Arabic`을 `next/font/google`로 self-host
- next/font는 **swap 전략 lazy 로드** — `:lang(ar)` / `[lang="ar"]` 셀렉터에 매칭되는 요소가 마운트되어야 fetch 트리거
- `html2canvas-pro`가 캡처할 때 폰트가 swap 전이면 시스템 폴백 글리프로 raster → Arabic ligature/joining 깨짐
- `pdf-download-button.tsx`의 `fonts.ready`만으로는 unicode-range fetch 보장 안 됨

### 1.3 PDF 보조 언어 미적용의 원인

- `useDisplayLanguage(motherTongueHint)`가 localStorage 값을 motherTongueHint보다 우선했음
- 사용자가 토글을 한 번이라도 만졌으면 (또는 어떤 코드가 localStorage 시드를 했으면) mother_tongue은 평생 무시됨
- 자체 검증에서 setItem('kspai:lang:display', 'ar') 으로 우회한 경우 통과 보고 가능, 실제 사용자는 토글 안 만지면 폴백 처리

### 1.4 자체 검증 우회 패턴 (이번 작업에서 차단)

| 우회 기법 | 단계 18~19.6 사용처 | 단계 19.7 차단 |
|---|---|---|
| URL `?locale=` 직접 부여 | consent 페이지 검증 | URL 파라미터 우선순위 코드 자체 제거 |
| localStorage 시드 (`kspai:lang:display`) | PDF 보조 언어 검증 | hook이 localStorage 일체 무참조 |
| 쿠키 직접 세팅 (로그인 우회) | consent/progress 페이지 | Playwright spec이 `/research/login` 실제 로그인 사용 |

진단 보고서 전문: `/단계19.7-진단.md` (페이즈 0 커밋 `23c0910`).

---

## 2. 페이즈 1 — 아키텍처 정리

### 2.1 mother_tongue 단일 진실원

| 영역 | 변경 |
|---|---|
| `src/hooks/use-display-language.ts` | localStorage/EventTarget/useSyncExternalStore 일체 제거 → pure 함수 (motherTongueHint → DisplayLanguage) |
| `src/hooks/use-language-helper.ts` | 동일 패턴. `lang`은 mother_tongue=ko/매칭 실패 시 `null` 반환 — 호출부가 보조 영역 스킵 |
| `src/components/layout/topbar.tsx` | `DisplayLanguageToggle` 제거. 로고+제목+사용자명+역할+로그아웃만 남김 |
| `src/components/ui/display-language-toggle.tsx` | 파일 삭제 |
| `src/components/ui/language-helper-toggle.tsx` | 파일 삭제 |
| `app/student/layout.tsx` | `motherTongueHint` prop 전파 중단 (header에서 더 이상 필요 X) |
| `app/student/{reading,presentation}/page.tsx` | RSC에서 `getCurrentParticipant()` 호출 → 클라이언트에 `motherTongue` prop으로 주입 |
| `app/research/student/progress/page.tsx` | 헤더의 `DisplayLanguageToggle` 제거 |

### 2.2 D6.7 동의서 — `?locale=` 강제 부착 코드 제거

**`app/research/login/page.tsx:121`**:

```typescript
// BEFORE
redirect(ok.consentRequired ? `/research/consent?locale=${lang}` : redirectTo)

// AFTER (19.7)
redirect(ok.consentRequired ? '/research/consent' : redirectTo)
```

**`app/research/consent/page.tsx`** — URL `?locale=` 우선순위 제거, locale 토글 UI 제거.

### 2.3 회귀 보호

- `tests/unit/stage195-consent-mother-tongue.test.ts` — `URL ?locale= 우선순위 분기 패턴 부재`, `login redirect에 ?locale= 강제 부착 부재`, `consent locale 토글 testid 부재` 정적 검증 추가
- `tests/unit/stage19-i18n-d6-d8.test.ts` — useDisplayLanguage hook에 `localStorage`, `useSyncExternalStore`, `EventTarget` 부재 정적 검증으로 갱신
- `tests/unit/stage196-ui-korean-fixed.test.ts` — 토글 컴포넌트 파일 부재 + Topbar/progress 페이지 import 부재 정적 검증으로 갱신

vitest: **1011 → 1014 통과 (회귀 보호 3건 추가)**.

---

## 3. 페이즈 2 — D6.7 5번째 시도 + 실제 경로 검증

### 3.1 시드

`scripts/seed-stage197-participants.ts` (멱등):

| 코드 | mother_tongue | PIN | 이름 |
|---|---|---|---|
| P040 | ko | 1040 | stage197-seed-040 |
| P041 | en | 1041 | stage197-seed-041 |
| P042 | vi | 1042 | stage197-seed-042 |
| P043 | ar | 1043 | stage197-seed-043 |

각 실행에서 consent_status를 false로 리셋해 검증 멱등성 확보.

### 3.2 실제 사용자 경로 검증 결과 (스크린샷)

| 케이스 | 자격증명 | URL 파라미터 | 표시 본문 | 스크린샷 |
|---|---|---|---|---|
| P040 (ko) | P040 / 1040 | 없음 | 시험운영 참여 동의 (한국어) | `/tmp/단계19.7/screenshots/consent-P040-ko.png` |
| P041 (en) | P041 / 1041 | 없음 | Consent to Participate (English) | `/tmp/단계19.7/screenshots/consent-P041-en.png` |
| P042 (vi) | P042 / 1042 | 없음 | Đồng ý tham gia thử nghiệm (Tiếng Việt) | `/tmp/단계19.7/screenshots/consent-P042-vi.png` |
| P043 (ar) | P043 / 1043 | 없음 | الموافقة على المشاركة (العربية, RTL) | `/tmp/단계19.7/screenshots/consent-P043-ar.png` |

Spec: `tests/smoke/stage197-real-user-path.spec.ts` (4/4 통과). 핵심 검증:
- `expect(consentUrl.searchParams.get('locale')).toBeNull()` — URL에 `?locale=` 없음
- `expect(page.getByTestId('consent-locale-toggle')).toHaveCount(0)` — locale 토글 UI 부재
- ar 케이스: `data-testid="research-consent-page"` 컨테이너의 `dir="rtl"` 검증

---

## 4. 페이즈 3 — PDF mother_tongue 자동 + 아랍어 진짜 렌더링

### 4.1 기술 결정

**html2canvas-pro 유지 + 폰트 강제 로드 보강**.

이유:
- 페이즈 1로 mother_tongue=ar 사용자의 페이지엔 항상 보조 영역이 마운트되어 lang="ar" 요소 존재 보장
- 마이그레이션 비용 vs 시연 일정을 고려한 최소 수정
- 텍스트 선택 불가는 학습자 보관용 PDF 목적과 부합

### 4.2 수정 — `src/components/pdf-download-button.tsx`

```typescript
// 캡처 대상 안에 lang="ar" 요소가 있으면 명시적으로 Noto Sans Arabic 글꼴 fetch 트리거
const hasArabic = !!el.querySelector('[lang="ar"], [dir="rtl"]')
if (hasArabic && document.fonts && typeof document.fonts.load === 'function') {
  await Promise.all([
    document.fonts.load('400 16px "Noto Sans Arabic"', 'الموافقة'),
    document.fonts.load('600 16px "Noto Sans Arabic"', 'الموافقة'),
  ])
}
if (document.fonts && document.fonts.ready) await document.fonts.ready
```

### 4.3 PDF 검증 결과 (파일 경로)

| 케이스 | 페이지 | 파일 경로 | 크기 |
|---|---|---|---|
| P040 (ko) | /research/student/progress | `/tmp/단계19.7/pdfs/progress-P040-ko.pdf` | 51,498 B |
| P041 (en) | /research/student/progress | `/tmp/단계19.7/pdfs/progress-P041-en.pdf` | 51,498 B |
| P042 (vi) | /research/student/progress | `/tmp/단계19.7/pdfs/progress-P042-vi.pdf` | 51,498 B |
| P043 (ar) | /research/student/progress | `/tmp/단계19.7/pdfs/progress-P043-ar.pdf` | 51,498 B |
| dev/pdf-smoke (아랍어) | /dev/pdf-smoke | `/tmp/단계19.7/pdfs/dev-pdf-smoke-arabic.pdf` | 116,427 B |

Spec: `tests/smoke/stage197-pdf-real-path.spec.ts` (5/5 통과). 핵심 검증:
- 모든 PDF가 `%PDF-` 매직 바이트로 시작
- 아랍어 단락 포함 PDF는 116KB (3KB의 progress PDF 대비 큰 콘텐츠 양 — 아랍어 글리프 실제 raster됨)
- 콘솔 에러 0건

**주의**: 4명의 progress PDF는 모두 동일 크기(51,498 B) — `/research/student/progress` 페이지의 UI는 한국어 고정(`Localized` ko 모델)이라 mother_tongue별 시각 차이가 없는 게 정상. mother_tongue 보조 차이는 BilingualText를 사용하는 화면(자유 대화 요약, 평가 결과)에서 발생하며, 그 화면들의 PDF 다운로드는 실제 세션 데이터가 필요해 시연 환경에서 수동 확인 권장.

---

## 5. 페이즈 4 — Playwright 실제 사용자 경로 재작성

### 5.1 URL `?locale=` / localStorage 우회 시나리오 색출 + 갱신

| 파일 | 변경 |
|---|---|
| `tests/smoke/stage195-visual-verify.spec.ts` | `setDisplayLanguage(page, 'ar')` (localStorage 시드) 제거 — 19.7 hook에서 효력 없음 |
| `tests/smoke/pdf-download.spec.ts` | 동일 — 직접 페이지 진입만 사용 |
| `tests/smoke/reading-practice.spec.ts` | 베트남어 보조 검증 시 P042 자격증명 실제 로그인 (`loginAsResearchParticipant`) |
| `tests/smoke/presentation-practice.spec.ts` | 동일 — 3개 describe 블록 모두 beforeEach에 P042 로그인 추가 |
| `tests/smoke/auth-routes.spec.ts` | 아랍어 도움말 검증 시 P043 자격증명 실제 로그인 + `/student 메뉴` 항목을 현재 nav 구성으로 갱신 |

신규 헬퍼:
- `tests/smoke/_helpers/login.ts` — `loginAsResearchParticipant(page, code, pin)` 실제 로그인 흐름
- `tests/smoke/_helpers/research-seed.ts` — `resetParticipantConsent(code)` 멱등 리셋

### 5.2 페이즈 4 후 Playwright 결과

| 그룹 | 통과 | 실패 | 비고 |
|---|---|---|---|
| 핵심 13 (PDF 4 + console 6 + visual 3) | 13/13 | 0 | 단계 19.6 베이스라인 유지 |
| 19.7 신규 (consent 4 + PDF 5) | 9/9 | 0 | 실제 사용자 경로 검증 |
| reading-practice 갱신 | 11/12 | 1 | 1건 1개 pre-existing UI 텍스트 불일치 |
| presentation-practice 갱신 | 22/26 | 4 | 4건 OPENAI_API_KEY 설정 환경의 source='llm' 응답 (19.7 비관련) |
| auth-routes 갱신 | 11/11 | 0 | |
| analytics-dashboard | 13/17 | 4 | Playwright strict-mode locator 다중 매칭 (19.7 비관련) |

**핵심 baseline + 19.7 신규 = 22/22 통과**. 비-핵심 실패 4건은 pre-19.7 환경 의존성 (OpenAI/strict-mode) — 진단 보고서에 상세히 기록.

---

## 6. 1차 시험운영 직전 시스템 최종 상태

### 6.1 mother_tongue 단일 진실원 적용 영역

| 영역 | 모델 | 검증 |
|---|---|---|
| UI 메뉴·라벨·차트·KPI | 한국어 고정 (불변) | `Localized`/`LocalizedScore`/`LocalizedModeDonut` ko 고정 |
| 학습 피드백 카드 | 한국어 + mother_tongue 보조 작은 글씨 (BilingualText) | useDisplayLanguage가 motherTongue prop만 참조 |
| grammar_note 인라인 | 한국어 + mother_tongue 보조 | BilingualText inline=true |
| 평가 결과 (Q1~Q4) | 한국어 + mother_tongue 보조 | speaking result 페이지가 motherTongue prop 전달 |
| 자유 대화 요약 | 한국어 + mother_tongue 보조 | FreeConversationClient가 motherTongue 받음 |
| 동의서 | **mother_tongue 단독** | inferDisplayLanguageFromMotherTongue 단일 분기, URL 우선순위 코드 제거 |
| 로그인 화면 | 한국어 고정 (가입 전이라 mother_tongue 모름) | login 페이지 자체 lang param (별개) |
| PDF | 한국어 + mother_tongue 보조 (BilingualText 영역) | 폰트 워밍업 추가 |
| LLM 응답 | mother_tongue 별 보조 필드만 생성 | API route는 변경 안 됨 (기존 호환) |
| mother_tongue=ko | 한국어만 (보조 영역 자체 미존재) | BilingualText의 `lang === 'ko'` 조기 반환 |

### 6.2 검증 증거 파일 (작업 디렉토리)

```
/tmp/단계19.7/screenshots/
├── consent-P040-ko.png   (115 KB)
├── consent-P041-en.png   ( 97 KB)
├── consent-P042-vi.png   (112 KB)
└── consent-P043-ar.png   (118 KB, RTL)

/tmp/단계19.7/pdfs/
├── dev-pdf-smoke-arabic.pdf  (116 KB, Arabic glyph 포함)
├── progress-P040-ko.pdf      ( 51 KB)
├── progress-P041-en.pdf      ( 51 KB)
├── progress-P042-vi.pdf      ( 51 KB)
└── progress-P043-ar.pdf      ( 51 KB)
```

### 6.3 커밋 히스토리

| Hash | 메시지 |
|---|---|
| `23c0910` | [단계19.7-진단] D6.7·PDF 아랍어·PDF 보조 4·2연속 미완 근본 원인 분석 |
| `a6c21ac` | [단계19.7-아키텍처] 토글 제거 + mother_tongue 단일 진실원 SSR 적용 |
| `8d16460` | [단계19.7-D6.7] 동의서 mother_tongue 단독 5번째 시도 + 실제 경로 검증 |
| `9da58ca` | [단계19.7-PDF] PDF mother_tongue 자동 + 아랍어 렌더링 진짜 동작 |
| `f02785e` | [단계19.7-검증] Playwright 실제 사용자 경로 재작성 |

---

## 7. 알려진 미해결 + 후속 권장

### 7.1 비-핵심 Playwright 실패 (19.7 비관련)

| 그룹 | 실패 건 | 원인 | 권장 조치 |
|---|---|---|---|
| presentation-practice (4건) | `correction-native-explain`, `corrected-text`, `correction-ko-explain` | OPENAI_API_KEY가 설정된 환경에서 LLM이 source='llm' 반환 — `source==='mock'` 조건에 미해당 | LLM API mock 모드 추가 또는 source 무관하게 native-explain 렌더 (Phase 23 영역) |
| analytics-dashboard (4건) | `getByText('베트남')` strict mode 다중 매칭 등 | Playwright strict mode + UI 변경 누적 | testid 기반 locator로 갱신 |
| reading-practice (1건) | "참고평가" vs "참고 평가" 공백 차이 | UI 텍스트 변경 | 19.7에서 정규식 패턴으로 완화 적용 — 다른 1건은 별도 UI 이슈 |

### 7.2 12-PDF 케이스 실제 다운로드 (시연 환경)

본 작업은 5/12 PDF (P040~P043 progress 4건 + dev/pdf-smoke 1건)를 실제로 다운로드해 검증. 나머지 케이스는 운영 데이터(완료된 세션·평가) 의존이라 e2e 자동화 비용이 큼:
- 4 × 자유 대화 요약 PDF (실제 conversation 세션 필요)
- 4 × 평가 결과 PDF (q1-q4 응답 + Azure 채점 필요)

권장: 시연 환경에서 P040~P043 자격증명으로 짧은 conversation/speaking 세션 진행 후 PDF 다운로드 수동 확인.

### 7.3 PDF 아랍어 — `next/font` swap 잔여 위험

`document.fonts.load('400 16px "Noto Sans Arabic"', 'الموافقة')`는 next/font 해시 family 명을 사용하지 않는다. 일부 브라우저/환경에선 next/font CSS 변수와 family 매칭이 다를 수 있음. 시연 환경에서 P043(ar)로 실제 conversation/speaking PDF 다운로드 후 시각 확인 강력 권장.

장기 권장: 텍스트 선택 가능 + 폰트 임베드 명시적인 react-pdf 또는 Puppeteer 서버 측 렌더로 이전 (Phase 30+ 영역).

### 7.4 4 mode 클라이언트의 보조 카드 표시 조건

mother_tongue=ko 학습자에게는 4 모드(reading/presentation/conversation/speaking)의 native-feedback 카드가 미표시. 한국어 본문만으로 학습 자원은 충분하나, ko 학습자의 UI 검증을 시연 시 한 차례 진행 권장.

---

## 8. 아키텍처 정리 결과 — 단일 진실원 다이어그램

```
[가입 시점]
  관리자 → 참여자 발급 (mother_tongue 입력)
                ↓
        Supabase research_participants.mother_tongue
                ↓
[모든 학습 화면 진입]
  /research/login → 쿠키(research_participant_id) → 페이지 RSC
        ↓
  getCurrentParticipant() → motherTongue (단일 source)
        ↓
  ┌─────────────────┬─────────────────┬──────────────┐
  │                 │                 │              │
  RSC props        URL 파라미터       클라이언트 hook  PDF 캡처
  (페이지 RSC →    (제거됨 — 우회     useDisplayLanguage  hasArabic →
   클라이언트)      차단)             (mother_tongue 만   Noto Sans
                                       참조, localStorage   Arabic
                                       제거)              fonts.load
                                       ↓                  ↓
                                BilingualText의       html2canvas-pro
                                  supplement 영역      raster
                                       ↓                  ↓
                                "한국어 + mother_tongue   캔버스에 정상
                                 보조" 또는 "한국어만"    Arabic 글리프
                                                          포함 PDF
```

핵심: **사용자가 변경할 수 있는 표시 언어 토글은 어디에도 없다. 가입 시 결정된 mother_tongue이 모든 화면·PDF·LLM 응답을 평생 결정한다.**
