# 단계 19.8 — 시험운영 직전 잔여 보정 — 최종 보고서

## 0. 작업 요약

- **브랜치**: `feat/q4-llm-provider`
- **베이스라인**: b3fbc17 (`[단계19.7-보고서] 최종 검증 + 진단 + 증거 첨부`)
- **신규 커밋**: 4건 (페이즈별 1건씩)
- **변경 파일**: 24개
- **신규 테스트**: vitest 52건 + Playwright 28건

| 페이즈 | 항목 | 상태 | 커밋 |
|---|---|---|---|
| 1 | 사각형 KDLI 로고 헤더 복원 | ✅ | 44a328c |
| 2 | 평가결과 mother_tongue 보조 (V3↔V4 갭) | ✅ | a50124c |
| 3 | 사이드바·모드 카드·페이지 제목 보조 표기 | ✅ | ebc520e |
| 4 | LLM 환각 차단 시스템 프롬프트 | ✅ | f3c013d |

## 1. 최종 검증 상태

### 1.1 자동 테스트

| 항목 | 결과 | 비고 |
|---|---|---|
| vitest | **1066/0** | 베이스라인 1014 + 신규 52 |
| ESLint | **0 errors, 0 warnings** | |
| Production build (`npm run build`) | **통과** | Next.js 16.2.4 Turbopack |
| Playwright (chromium) | **236 passed, 1 skipped** | 회귀 0건 |
| Playwright (api) | **53 passed, 4 fail** | 모두 LLM 응답 phrasing 변동성 — 19.7부터 사전 존재 |

### 1.2 단계 19.8 신규 테스트 — 전부 통과

| Suite | 케이스 | 결과 |
|---|---|---|
| `tests/unit/stage198-kdli-brand.test.ts` | 7 | ✅ |
| `tests/unit/stage198-result-mother-tongue.test.ts` | 6 | ✅ |
| `tests/unit/stage198-ui-supplement.test.ts` | 23 | ✅ |
| `tests/unit/stage198-anti-hallucination.test.ts` | 16 | ✅ |
| `tests/smoke/stage198-logo-verify.spec.ts` | 4 | ✅ |
| `tests/smoke/stage198-realuser.spec.ts` | 4 | ✅ |
| `tests/smoke/stage198-ui-supplement.spec.ts` | 8 | ✅ |

### 1.3 단계 19.7 회귀 보호

| Suite | 결과 |
|---|---|
| `stage197-real-user-path.spec.ts` (mother_tongue 동의서) | 4/4 ✅ |
| `stage197-pdf-real-path.spec.ts` (mother_tongue PDF) | 4/4 + 아랍어 1/1 ✅ |
| `stage195-visual-verify.spec.ts` (KDLI placeholder + RTL) | 3/3 ✅ |

## 2. 페이즈 2 진단 — V3 ↔ V4 mother_tongue 갭 원인

### 2.1 두 경로 비교

| 영역 | V3 자유 대화 (정상) | V4 평가 결과 (누락) |
|---|---|---|
| 컴포넌트 | `BilingualText` | `MultilingualFeedback` |
| `motherTongueHint` 전달 | `motherTongueHint={motherTongue}` 명시 | **전달 없음** |
| 폴백 동작 | hint→ko인 경우 보조 영역 제거 | hint 없으면 `useDisplayLanguage`가 'ko'로 폴백 |
| 결과 | 보조 영역 정상 노출 | `if (displayLang === 'ko') return null` — DOM 자체 미생성 |

### 2.2 근본 원인

```tsx
// app/student/speaking/[questionId]/result/page.tsx (19.7 이전)
<MultilingualFeedback
  testId="q1-multilingual-feedback"
  vi={ml.vi} en={ml.en} ar={ml.ar}
  // motherTongueHint 전달 누락! ← 갭의 시작점
/>

// 결과: 카드 자체 DOM 미생성
// (MultilingualFeedback 내부에서 displayLang === 'ko'면 return null)
```

`useDisplayLanguage(motherTongueHint)`는 hint가 없으면 `DEFAULT_DISPLAY_LANGUAGE`(='ko')로 폴백. 따라서 hint를 전달하지 않으면 모든 학습자가 ko로 폴백되어 보조 카드가 사라진다.

### 2.3 V3는 왜 정상이었나

`free-conversation-client.tsx`는 `BilingualText` 호출 시마다 명시적으로 `motherTongueHint={motherTongue}`를 넘긴다. motherTongue은 props로 받아 클라이언트 컴포넌트가 직접 보유하며 18~19.7에 걸쳐 일관 유지됐다.

V4는 19.6에서 `MultilingualFeedback` 컴포넌트 자체에 motherTongueHint를 추가했지만, 호출부(result/page.tsx)에서 prop을 채우지 않았다. 19.7 검증은 자유 대화에 집중되어 이 누락을 잡지 못했다.

### 2.4 19.8 수정

```tsx
// app/student/speaking/[questionId]/result/page.tsx (19.8 이후)
<MultilingualFeedback
  testId="q1-multilingual-feedback"
  vi={ml.vi} en={ml.en} ar={ml.ar}
  motherTongueHint={participantMotherTongue}  // ← 단일 진실원 전달
/>
```

q1/q2/q3/q4 4건 모두 동일 수정. 추가로 `MultilingualFeedbackBlock`(총평)은 기존부터 명시 전달이라 변경 없음.

### 2.5 부수 발견 — Turbopack 모듈 분리

평가 결과 mock 검증을 위해 dev seed API(`/api/dev/seed-speaking-result`)를 추가한 결과, Next.js 16.2.4 Turbopack dev 모드에서 route handler와 page route가 동일 모듈의 별도 인스턴스를 평가해 in-memory `Map`이 분리되는 사례 발견. `globalThis.__speakingEvalStore`로 부착해 동일 프로세스 내 공유를 보장.

## 3. 페이즈별 변경 세부

### 페이즈 1 — 사각형 KDLI 로고 헤더 복원 (44a328c)

**진단**: Topbar(/student, /admin, /teacher)는 `kdli-logo-256.png` 정상 표시. 리서치 모드(/research/*)는 자체 헤더 사용 — 로고 부재 (단계 19.5 이후 추가된 적 없음).

**수정**:
- `src/components/layout/kdli-brand.tsx` 신설 — 공용 KDLI 사각형 로고 헤더
- `app/research/student/progress/page.tsx`: KdliBrand 마운트
- `app/research/consent/page.tsx`: KdliBrand 마운트
- `app/research/consent/declined/page.tsx`: KdliBrand 중앙 배치
- `app/research/admin/page.tsx`: KdliBrand 마운트

**검증**: Playwright 4건 (P040 로그인 → 리서치 동의서·진척 + /student/conversation-practice·speaking Topbar)

스크린샷:
- `/tmp/단계19.8/screenshots/p040-research-consent-logo.png`
- `/tmp/단계19.8/screenshots/p040-research-progress-logo.png`
- `/tmp/단계19.8/screenshots/p040-student-conversation-topbar-logo.png`
- `/tmp/단계19.8/screenshots/p040-student-speaking-topbar-logo.png`

### 페이즈 2 — 평가결과 mother_tongue 보조 (a50124c)

**수정**:
- `app/student/speaking/[questionId]/result/page.tsx`: q1~q4 4건 `motherTongueHint` 전달
- `src/lib/mock/speaking-store.ts`: in-memory store를 `globalThis`에 부착 (Turbopack 회피)
- `app/api/dev/seed-speaking-result/route.ts`: dev 검증용 평가 레코드 seed (production 404)

**검증** (실제 사용자 경로, mock 평가 + PDF):
| 케이스 | 보조 카드 상태 | mother_tongue 텍스트 |
|---|---|---|
| P040 (ko) | DOM 자체 미존재 (`toHaveCount(0)`) | — |
| P041 (en) | 마운트 ✅ | "passage" 등 영어 |
| P042 (vi) | 마운트 ✅ | "Bạn" 등 베트남어 |
| P043 (ar) | 마운트 ✅ RTL 컨테이너 | "النص" 아랍어 |

스크린샷+PDF:
- `/tmp/단계19.8/screenshots/q1-result-P04{0,1,2,3}-{ko,en,vi,ar}.png`
- `/tmp/단계19.8/pdfs/q1-result-P04{0,1,2,3}-{ko,en,vi,ar}.pdf` (4건, 353K~428K)

### 페이즈 3 — UI 라벨 보조 표기 (ebc520e)

**구조 변경**:
- `Localized` (단계 19.6 한국어 고정) → 19.8 본문(한국어) + 보조(mother_tongue)
- `supplementOnly` 플래그로 보조 영역 단독 출력 (PageHeader titleSupplement 같은 외부 본문이 있는 경우)
- `Sidebar`에 motherTongueHint prop 추가 + `NavItem.labelKey`로 SIDEBAR_LABELS 매핑
- `AppShell` → `Sidebar` 모국어 전달
- `app/student/layout.tsx`: RSC에서 `getCurrentParticipant`로 모국어 획득 후 AppShell에 전달

**i18n 데이터 추가**:
- `SIDEBAR_LABELS` 5건: 내 학습 현황 / 말하기 평가 / 읽기연습 / 발표연습 / 생성형 대화
- `PAGE_LABELS` 3건 추가: evaluationResult / freeConversationPractice / chooseConversationPartner
- 기존 progressTitle.ar 텍스트 통일 (`حالة التقدم في التعلم`)

**검증**: Playwright 8건 (4 mother_tongue × 2 시나리오)
- 진척 페이지: P040(ko)은 보조 영역 없음, en/vi/ar는 모드 카드/제목 보조 표기 ✅
- 사이드바: ko는 보조 영역 없음, en/vi/ar는 5개 메뉴 모두 작은 글씨 보조 ✅

스크린샷: `/tmp/단계19.8/screenshots/{progress-ui,sidebar}-P04{0,1,2,3}-{ko,en,vi,ar}.png`

### 페이즈 4 — LLM 환각 차단 (f3c013d)

**수정**:
- `src/lib/llm/build-persona-system-prompt.ts`: `antiHallucinationBlock()` 신설. `responsePrincipleBlock` 다음, `toolBlock`·`correctionBlock` 앞에 삽입. `forSummary` 모드도 동일 포함.
- `src/lib/llm/build-q4-persona-system-prompt.ts`: 주제 유지 블록 다음 같은 규칙 + 카페 시나리오 회귀 예시.

**규칙 명문화**:
- 모르는 사실은 절대 만들어내지 말고 "잘 모르겠어요"로 답할 것
- 비슷한 이름 추측 금지 ("루왁" → "루아" 같은 fabrication 차단)
- 구체적 매장·지명·인물·제품 정보는 일반화 ("강남에 카페가 많아요")
- 학습자가 모르는 한국어 단어 의미를 물을 때도 모르면 솔직히 되묻기

**Few-shot 예시 5건** (자유 대화):
1. ○○카페 알아? → "그 카페는 잘 모르겠어요. 어디에 있어요?"
2. 루왁커피 마셔봤어? → "이름은 들어봤는데 마셔본 적은 없어요. 어떤 맛이에요?"
3. ○○라는 가수 알아? → "잘 모르겠어요. 어떤 노래를 부르는 분이에요?"
4. 강남에 ○○동 있어? → "강남에 정확히 그 이름이 있는지는 잘 모르겠어요"
5. ○○라는 한국 음식 알아? → "처음 들어봐. 어디서 들었어?"

Q4 카페 시나리오 추가 예시: 학습자가 미션 무관 매장을 물을 때 "그 카페는 잘 모르겠어요. 오늘은 저희 매장에서 어떤 음료 드릴까요?"로 미션 흐름 회귀.

**검증**: vitest 16건 — 두 빌더 모두 환각 차단 블록 포함 + 4 mother_tongue 케이스 모두 유지 + 블록 위치(교정 블록보다 앞) 확인.

## 4. 시각 증거 (스크린샷 발췌)

### 4.1 평가 결과 PDF — P041 (en)

`/tmp/단계19.8/screenshots/q1-result-P041-en.png`

이 한 장에 단계 19.8 4페이즈가 모두 시각화된다:
- **Phase 1**: 좌상단 KDLI 사각형 로고 (Topbar)
- **Phase 3**: 사이드바 5개 메뉴 — Korean + English supplement (My Progress / Speaking Assessment / Reading Practice / Presentation Practice / Free Conversation)
- **Phase 3**: 페이지 제목 — "평가 결과" + "Assessment Result" 보조
- **Phase 2**: 총평 카드 — "훌륭합니다!..." + "Excellent! Your pronunciation..." 영어 보조

### 4.2 평가 결과 PDF — P043 (ar)

`/tmp/단계19.8/screenshots/q1-result-P043-ar.png`

아랍어 RTL 검증:
- 사이드바: Korean + Arabic 보조 (تقدم التعلم, تقييم التحدث, تدريب القراءة, تدريب العرض, محادثة حرة)
- 페이지 제목: "평가 결과" + "نتيجة التقييم"
- 총평 보조: "ممتاز! نطقك ونبرة صوتك تبدوان طبيعيتين..." (단어 단위 정상 shaping)

## 5. 시험운영 직전 시스템 최종 상태

### 작동 보장된 핵심 흐름

| 흐름 | 상태 |
|---|---|
| 참여자 P040~P043 로그인 → mother_tongue별 동의서 | ✅ (19.7 보장 + 19.8 회귀 보호) |
| 학습자 모드 KDLI 사각형 로고 | ✅ Topbar + 리서치 자체 헤더 |
| 사이드바 5개 메뉴 mother_tongue 보조 | ✅ (19.8 신규) |
| /research/student/progress 모드 카드 + 차트 + KPI 보조 | ✅ (19.8 신규) |
| 평가 결과 화면 mother_tongue 보조 카드 | ✅ (19.8 신규) |
| 평가 결과 PDF mother_tongue 보조 | ✅ (캡처 대상이 보조 카드 포함) |
| 자유 대화 mother_tongue 보조 | ✅ (19.7부터 유지) |
| LLM 환각 차단 (루왁커피·○○카페 같은 fabrication) | ✅ (19.8 시스템 프롬프트 강화) |
| 아랍어 RTL 표시 + PDF 폰트 임베드 | ✅ (19.7 유지) |

## 6. 알려진 미해결 / 후속 권장

### 6.1 사전 존재 LLM 응답 phrasing 변동성

`tests/smoke/api-smoke.spec.ts`의 4건이 실패하나 모두 LLM의 자연스러운 응답 phrasing이 expected substring과 다른 사례:
- "준비해 드리겠습니다" (expected) vs "포장해 드릴게요. 카드로 결제 도와드리겠습니다" (received)
- "/해드릴까요|주문|음료/" vs "몇 잔 드릴까요"

이 4건은 단계 19.8 변경 이전부터 LLM stochastic 응답에 종속된 flaky 테스트로, 환각 차단 시스템 프롬프트와 무관하다(같은 의미·교정 의도를 유지). 시험운영 영향 없음.

후속 권장: API 테스트의 substring 매칭을 더 넓은 의미 기반 검증 또는 정규식으로 완화.

### 6.2 평가 결과 strengths/improvements/evidence 다국어

페이즈 2에서 q1~q4 하단 "모국어 보조 안내" 카드(정적 템플릿)는 자동 노출되도록 수정. 상단 AI 피드백 카드의 strengths/improvements/missing/evidence 리스트는 LLM이 한국어만 반환하므로 한국어 본문만 표시.

mother_tongue 학습자에게는 하단 카드가 같은 의미를 모국어로 전달하므로 의사소통 측면에서는 보강 완료. 추가 다국어가 필요하면 LLM 스키마에 `strengths_multilingual`·`improvements_multilingual` 추가 + BilingualListItem 적용 (토큰 비용 증가 trade-off).

### 6.3 LLM 환각 차단 실증 검증

페이즈 4의 검증은 시스템 프롬프트에 환각 차단 블록이 포함되었음을 단위 테스트로 확인. 실제 LLM 호출 시 환각이 줄어드는지는 OpenAI 호출을 동반한 수동/통합 테스트가 필요.

추천 수동 시나리오 (시험운영 시작 전 OpenAI 키 설정 환경에서 5분 내 가능):
1. /student/conversation-practice → 페르소나 선택 → "루왁커피 마셔봤어?" → NPC가 "루아"·"루왁" 같은 가짜 이름 답을 하지 않고 "잘 모르겠어요. 어떤 맛이에요?" 류 응답
2. "○○카페 알아? 거기 맛있어?" → NPC가 일반화된 답변 ("강남이나 홍대에 카페가 많아요")
3. 가짜 가수 이름 → "잘 모르겠어요"
4. 모르는 한국어 단어 → "그 단어는 잘 못 들어봤어요. 어디서 봤어요?"
5. Q4 카페 시나리오에서 미션 외 매장 질문 → 짧게 모름 표시 후 미션 회귀

## 7. 커밋 그래프

```
f3c013d [단계19.8-환각차단] LLM 시스템 프롬프트 허위 정보 생성 금지 + few-shot
ebc520e [단계19.8-UI보조] 사이드바·모드 카드·페이지 제목 mother_tongue 보조 표기
a50124c [단계19.8-평가결과] 평가결과 mother_tongue 보조 자동 적용 (V3↔V4 갭 해결)
44a328c [단계19.8-로고] 사각형 KDLI 로고 헤더 전 페이지 복원
b3fbc17 [단계19.7-보고서] 최종 검증 + 진단 + 증거 첨부  ← 베이스라인
```

origin/feat/q4-llm-provider 대비 69 commits ahead (push 금지).

## 8. 신뢰성 노트

본 보고서의 모든 검증 결과는 다음 원칙을 따랐다:

1. **자체 검증 우회 차단**: URL `?locale=` 또는 localStorage 시드로 mother_tongue을 강제 주입하지 않음. 모든 Playwright 검증이 `/research/login` 자격증명 입력 → consent (필요 시) → 정상 redirect 경로.
2. **mock 평가 seed 제한**: 페이즈 2 검증을 위한 `/api/dev/seed-speaking-result`는 `process.env.NODE_ENV === 'production'`에서 404. 시험운영 환경에 노출되지 않음.
3. **회귀 보호 단위 테스트**: 각 페이즈마다 회귀 차단 단위 테스트(자산 존재·prop 전달·스키마 일관성·블록 포함) 신설.
4. **실제 PDF 다운로드 검증**: %PDF- 매직 바이트 + 5KB 이상 크기 + lang="ar"/dir="rtl" 컨테이너 존재.

— 단계 19.8 종료.
