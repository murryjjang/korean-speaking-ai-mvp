# 단계 19.16 — 자유 대화 STT 정확도 향상 + NPC LLM 발음·발화 흐름 통합 보고서

작성일: 2026-05-20
브랜치: `feat/q4-llm-provider`
범위: 자유 대화 STT 정확도 향상(whisper-1 → gpt-4o-mini-transcribe) + NPC LLM 발음/멈춤 컨텍스트 전달 (단계 19.15 흡수)

---

## 1. 요약

| 영역 | Before (V3 시점) | After (본 단계) |
|---|---|---|
| 자유 대화 STT 모델 | `whisper-1` — 짧고 더듬는 발화에서 환각 빈발 | `gpt-4o-mini-transcribe` (FREE_CONVERSATION_STT_PROVIDER) |
| pronunciation-azure referenceText | 이미 transcript 전달 중 (작업 지시서 가정 오류) | 동일 — 단, 정확한 transcript로 의미 있는 scripted 채점 |
| NPC LLM 입력 | latestStudentText만 | + pronunciationContext + speechFlowContext |
| LLM 환각 방지 | (자유 대화에서는 미적용) | 50점 미만 칭찬 금지, 친구 톤 유지 가이드 |
| 응답 메타 | (없음) | `feedback_inputs_version='stage19.16'`, pronunciation_included, speech_flow_included |
| 응답 지연 (pronEvalEnabled ON) | LLM과 PA 병렬 (~1.5s) | PA 응답 await 후 LLM (~2.5~3s) |

도미노 효과 정리:
```
[Before]
audio → whisper-1(부정확) → 환각 transcript
  ├→ pronunciation-azure (scripted, referenceText=환각) → 잘못된 점수
  └→ NPC LLM (latestStudentText=환각) → 맥락 환각 응답
                                          ↑ 발음 데이터 미전달

[After]
audio → gpt-4o-mini-transcribe(정확) → 정확한 transcript
  ├→ pronunciation-azure (scripted, referenceText=정확) → 의미 있는 점수
  └→ NPC LLM (latestStudentText=정확, +pronunciationContext, +speechFlowContext)
       → 맥락 일치 응답 + 발음 약점 친구 톤으로 자연 언급
```

---

## 2. 페이즈별 결과

### 페이즈 0 — 자유 대화 STT 흐름 + 환경 진단 (커밋 `a8cae02`)

진단 산출: `/tmp/단계19.16/diagnosis.md`

**핵심 발견 — 작업 지시서 가정 정정**:

| 작업 지시서 주장 | 실제 코드 | 영향 |
|---|---|---|
| "클라이언트가 referenceText 안 보냄 → unscripted 모드" | `free-conversation-client.tsx:761` — transcript를 referenceText로 이미 전달, scripted 모드로 호출 중 | 페이즈 2의 "referenceText 전달" 부분은 no-op (이미 됨) |
| AZURE_SPEECH_KEY 미설정 가정 | `.env.local`에 설정됨 | scripted Azure가 실제 동작 중 |

**실제 환각 원인 재정의**:
- `whisper-1`이 더듬는·짧은 발화에서 환각 (예: "자세한 내용은 더보기란에 링크가 있습니다")
- 환각 텍스트가 referenceText + NPC LLM 입력 양쪽에 그대로 들어감 → 도미노 손상
- 즉 작업 지시서의 본질("STT 정확도가 도미노의 출발")은 유효. 가정만 정정.

**환경 변수 상태**:
```
OPENAI_API_KEY               <SET>
AZURE_SPEECH_KEY             <SET>
AZURE_SPEECH_REGION          <SET>
STT_PROVIDER                 <SET>
PRONUNCIATION_PROVIDER       <SET>
FREE_CONVERSATION_STT_PROVIDER <NOT SET>  ← 본 단계에서 신규 도입
```

---

### 페이즈 1 — GPT-4o-mini-transcribe Provider 추가 (커밋 `5806ea5`)

#### 변경 코드

1. `src/providers/stt/index.ts`
   - `OpenAITranscribeSTTProvider` 클래스 추가 (gpt-4o-mini-transcribe / gpt-4o-transcribe).
   - whisper-1과 동일 SDK + 동일 한국어 환각 차단 prompt 공유.
   - `getSTTProvider(feature?)` factory에 feature override 도입.
   - 별칭 매핑 — `'openai' | 'openai-transcribe' | 'gpt-4o-mini-transcribe'` → gpt-4o-mini-transcribe (기존 `'openai'`도 신모델로 매핑).

2. `app/api/stt/route.ts`
   - questionId='free-conversation' 요청만 sttFeature='free-conversation'로 분기.
   - 다른 호출부(말하기 평가 등)는 영향 없음.

3. `.env.local.example` (gitignored)
   - STT_PROVIDER 가용 값 확장 + FREE_CONVERSATION_STT_PROVIDER 안내 추가.

#### 검증

- 신규 단위 테스트 15건 (`tests/unit/openai-transcribe-provider.test.ts`):
  - factory 결정 정책 (mock/whisper/openai-transcribe/gpt-4o-transcribe/별칭/대소문자)
  - feature='free-conversation' override 작동 + default 격리
  - OPENAI_API_KEY 미설정 시 throw → /api/stt mock 폴백 흐름 유지
  - 한국어 환각 차단 prompt 전달
  - 빈 응답 / API 오류 케이스
- vitest: 1101 → 1116 (+15)
- lint: 0 errors

---

### 페이즈 2 — 자유 대화 NPC LLM 발음·발화 흐름 통합 (커밋 `17c6c2b`)

단계 19.15 본질(자유 대화에 발음/멈춤 LLM 통합)을 본 단계에 흡수.

#### 변경 코드

1. `src/lib/llm/build-persona-system-prompt.ts`
   - `pronunciationContext` / `speechFlowContext` 인자 추가.
   - 신규 블록:
     - `[직전 학습자 발화 — 발음 평가 결과]` + `[발음 기반 응답 규칙 — 환각 차단]`
     - `[직전 학습자 발화 — 발화 흐름]` + `[발화 흐름 응답 규칙]`
   - 규칙 요지:
     - 50점 미만 칭찬 금지 / 70점 이상 코멘트 생략 / 50~69 친구 톤으로 한 단어만
     - 점수 직접 언급 금지 (페르소나 톤 유지)
     - 긴 멈춤 ≥2회 격려 / 짧은 멈춤 ≤1회 별도 언급 없이 자연 호응
     - NPC는 친구이지 코치가 아님 — 매 턴 반복 금지
   - `forSummary=true` 모드에서는 본 블록 미주입 (단계 19.13 llm-eval과 분리).

2. `app/api/conversation/free/respond/route.ts`
   - body에 `pronunciationContext` / `speechFlowContext` optional 수용.
   - `parsePronunciationContext` / `parseSpeechFlowContext` 안전 추출 가드:
     - 잘못된 weakWords 항목 / 비숫자 점수 / 알 수 없는 errorType → 폴백
     - 빈 객체 → undefined 반환 (시스템 프롬프트 미주입)
   - 응답 메타: `feedback_inputs_version='stage19.16'`, `pronunciation_included`, `speech_flow_included` — LLM 경로 · mock 폴백 · 예외 경로 모두 동일.
   - `motherTongue` 입력 분기 정식화 (기존 누락 정정).

3. `app/student/conversation-practice/free-conversation-client.tsx`
   - `pronEvalEnabled` ON일 때:
     - STT 결과 → pronunciation-azure 응답 await (~1~2s)
     - `buildPronunciationContext` / `buildSpeechFlowContext`로 컨텍스트 추출
     - NPC LLM 호출 시 함께 전달 (`sendMessageWithText` 시그니처 확장)
     - 기존 pronScore turn 부착 + research_assessments 누적 동일 유지
   - OFF일 때: 즉시 호출 흐름 (회귀 차단).
   - 텍스트 입력(키보드) 경로: 음성 없음 → 두 컨텍스트 undefined.

#### 검증

- 신규 단위 테스트 26건 (2 files):
  - `stage1916-free-conversation-pronunciation-context.test.ts` (14)
    - 두 블록 주입 / 미주입 / forSummary 분리 / 환각 차단 가이드 / 톤 가이드
  - `stage1916-respond-route-parsing.test.ts` (12)
    - 잘못된 형태 가드 / NaN·Infinity 거부 / count 자동 계산·명시값 우선 / FEEDBACK_INPUTS_VERSION 고정
- vitest: 1116 → 1142 (+26)
- lint: 0 errors

---

### 페이즈 3 — API 메타 스모크 + 사람 시연 가이드 (커밋 `338a8cb`)

#### 자동화 가능 영역

`tests/smoke/stage1916-respond-api-smoke.spec.ts` 신규 7건 — **전부 통과**:

| # | 시나리오 | 응답 메타 검증 |
|---|---|---|
| 1 | pronunciationContext 미전달 | `pronunciation_included=false`, `feedback_inputs_version='stage19.16'` |
| 2 | pronunciationContext 정상 전달 | `pronunciation_included=true` |
| 3 | speechFlowContext 정상 전달 | `speech_flow_included=true` |
| 4 | 빈 weakWords/pauses | 둘 다 false (시스템 프롬프트 보호) |
| 5 | 잘못된 형태 입력 | 라우트 크래시 없음, 둘 다 false |
| 6 | missing_topic | 400 |
| 7 | missing_student_text | 400 |

baseline `api-smoke.spec.ts` 4건 실패 — **본 단계와 무관**. 직전 커밋(`17c6c2b`)에서도 동일하게 실패 확인:
- `:134` corrected_answer 형태 가드
- `:562` beginner 카페 4목표 완료 응답
- `:626/650` 잘못된 표현 교정 응답 문자열 매칭

원인: 외부 LLM 응답 문자열의 자연 변동. 본 단계 변경 없음.

#### 사람 시연 위임

가이드 산출: `/tmp/단계19.16/scenarios/4-scenarios-verification-guide.md`

4시나리오 — 사용자 시연 필수 (Playwright는 마이크 입력 시뮬레이션 불가):

| 케이스 | 발화 예시 | 핵심 체크 |
|---|---|---|
| 1. 의도적 어색 발음 | "음.. 그.. 한국 음식 좋아요" | STT 일치 / 점수 의미 있음 / NPC 맥락 일치 / LLM 발음 약점 자연 언급 |
| 2. 의도적 멈춤 | "저는 [1.5s] 학생입니다" | pause 데이터 잡힘 / NPC 흐름 격려 / 코치 톤 X |
| 3. 정상 발음 | "저는 학생입니다" | STT 정확 / 점수 70+ / NPC 환각 없음 |
| 4. 매우 짧은 발화 | "네" | STT 정상 / audio_too_short 가드 / NPC 짧은 호응 |

Network 탭에서 `/api/conversation/free/respond` 응답 메타 확인:
```json
{ "feedback_inputs_version": "stage19.16", "pronunciation_included": true, ... }
```

---

## 3. 종합 검증

| 항목 | 결과 |
|---|---|
| vitest | **1142 / 1142** (1101 baseline + 41 신규) |
| Playwright API smoke (본 단계) | **7 / 7** |
| Playwright API smoke (baseline) | 53 / 57 (4건 baseline 회귀, 본 단계와 무관) |
| `npm run lint` | **0 errors** (4 warnings, baseline) |
| `npm run build` | **통과** |
| TypeScript | 본 단계 신규 코드 0 errors (사전 존재 test 파일 11건은 baseline) |

---

## 4. 비용·시간 영향

### gpt-4o-mini-transcribe 비용 (OpenAI 공식)

- $0.003 / 분 (transcription)
- 자유 대화 1세션 평균 5분 발화 = **$0.015 / 세션**
- 1차 시험운영 추정 (5명 × 1주 × 5세션) = **약 $0.375**
- whisper-1과 동일 가격대 — STT 단가 차이 없음.

### 응답 지연

| 흐름 | Before | After |
|---|---|---|
| STT (whisper → gpt-4o-mini-transcribe) | ~500ms | ~500ms (체감 동일) |
| pronEvalEnabled ON: NPC LLM 호출 시점 | STT 즉시 (병렬) | PA 응답(~1.5s) 후 (순차) |
| 사용자 인지 응답 시간 | ~1.5s | ~2.5~3s |

**+1~1.5초 추가 지연** — 도미노 차단을 위한 비용. 사용자 부담 < 환각 응답 손실.

### 토큰 소비

- NPC LLM 입력에 발음 블록 추가 → 시스템 프롬프트 약 +400 토큰
- pronunciationContext 있을 때만 — 자유 대화 1세션 약 10턴 = +4,000 tokens (입력)
- gpt-4o-mini 입력 가격 $0.15 / M tokens → **+$0.0006 / 세션** (무시 가능)

---

## 5. 도미노 효과 실현 정리

| 도미노 단계 | 이전 단계 영향 |
|---|---|
| STT 정확도 ↑ (whisper-1 → gpt-4o-mini-transcribe) | 모든 후속 처리의 기반 |
| pronunciation-azure scripted 정확도 ↑ | 더 의미 있는 발음 점수 |
| NPC 응답 맥락 일치 ↑ | 환각 응답 차단 |
| LLM 발음 피드백 의미 ↑ | 단계 19.13 (말하기) / 19.15 본질(자유 대화) 효과 실현 |

단계 19.13 (말하기 LLM 통합) — 자유 대화에는 미적용 상태였음. 본 단계로 흡수 완료.
단계 19.15 (사실상 미실시) — 본 단계에서 LLM 통합 부분 흡수.

---

## 6. 변경 파일

```
변경 (실제 코드):
- src/providers/stt/index.ts                                    (+88 lines)
- src/lib/llm/build-persona-system-prompt.ts                    (+84 lines)
- app/api/stt/route.ts                                          (~11 lines)
- app/api/conversation/free/respond/route.ts                    (+111 lines)
- app/student/conversation-practice/free-conversation-client.tsx (+121 lines, -35 lines)
- playwright.config.ts                                          (+1 line)

신규 (테스트):
- tests/unit/openai-transcribe-provider.test.ts                              (15 tests)
- tests/unit/stage1916-free-conversation-pronunciation-context.test.ts       (14 tests)
- tests/unit/stage1916-respond-route-parsing.test.ts                         (12 tests)
- tests/smoke/stage1916-respond-api-smoke.spec.ts                            (7 tests)

가이드:
- /tmp/단계19.16/diagnosis.md                  (페이즈 0 진단)
- /tmp/단계19.16/scenarios/4-scenarios-verification-guide.md  (사람 시연 가이드)
```

---

## 7. 운영 적용 안내

`.env.local`에 다음 한 줄 추가하면 즉시 활성화:

```
FREE_CONVERSATION_STT_PROVIDER=gpt-4o-mini-transcribe
```

설정하지 않으면 기존 `STT_PROVIDER`(현재 운영값)를 그대로 사용 — 회귀 없음.

---

## 8. 항목 커버리지

| # | 항목 | 페이즈 | 결과 |
|---|---|---|---|
| 1 | GPT-4o-mini-transcribe provider 추가 | 1 | ✅ 15 unit tests |
| 2 | 자유 대화 흐름 STT 통합 | 2 | ✅ 클라이언트 흐름 변경 |
| 3 | pronunciation-azure scripted 모드 전환 (referenceText 전달) | — | ⚠️ 진단 결과 이미 적용 중 (no-op) |
| 4 | NPC LLM에 정확한 텍스트 전달 (환각 차단) | 2 | ✅ feedback_inputs_version='stage19.16' |
| 5 | 단계 19.15 LLM 발음/멈춤 통합 흡수 | 2 | ✅ 14 unit tests + 7 smoke tests |
| 6 | 4시나리오 검증 | 3 | 🟡 자동화 부분 ✅, 시각 부분 사람 시연 위임 |

---

## 9. 미처리 후속 (지시서 §4 명시)

- 억양 평가 (Naver Clova 등) — 1차 시험운영 후 PoC 비교
- NPC voice 차별화 — 1차 후
- ETRI provider cleanup — 운영 종료 코드, 우선순위 낮음
- Naver Clova STT 비교 PoC — 1차 누적 데이터 후
- 본 단계 사람 시연 결과 (4시나리오) — 사용자가 시연 후 별도 보고서 추가

---

## 10. 커밋 히스토리

```
338a8cb [단계19.16-페이즈3] /api/conversation/free/respond API 메타 스모크 + 사람 시연 가이드
17c6c2b [단계19.16-페이즈2] 자유 대화 NPC LLM 발음·발화 흐름 통합 (단계 19.15 흡수)
5806ea5 [단계19.16-페이즈1] GPT-4o-mini-transcribe provider 추가
a8cae02 [단계19.16-페이즈0] 자유 대화 STT 흐름 + 환경 진단
```

푸시 금지 (작업 지시서 §1.2) — 로컬 4개 커밋 보존.
