# 단계 19.18 보고서 — STT Verbatim Prompt + 자유 대화 정체성 보완 + 종합 화면 점수

- **기간**: 2026-05-20 (페이즈 0 → 3, 약 1시간)
- **베이스라인**: vitest 1167 통과, 단계 19.17 종료
- **최종**: vitest 1173 통과 (+6 신규), lint 0 errors, next build OK, conversation/free smoke 7/7
- **브랜치**: `feat/q4-llm-provider`
- **커밋**: `a220060` (페이즈 0) · `31fe7a3` (페이즈 1) · `c603d5d` (페이즈 2) · 본 보고서

---

## 0. 시작 배경

단계 19.17 후 자유 대화 시험운영 중 추가 발견 3건:

| # | 발견 | 본 단계 범위 |
|---|---|---|
| 1 | gpt-4o-mini-transcribe가 어색한 발음을 정확한 표준어로 정정 (e.g., "마시써요" → "맛있어요") → 발음 점수의 reference가 흐려짐 | ✅ verbatim prompt로 부분 우회 시도 |
| 2 | 자유 대화 종료 후 종합 화면에 발음 점수 누적 데이터 미표시 (메시지별만 있음) | ✅ 평균·구간별 분포 카드 신설 |
| 3 | 자유 대화 정체성 모호 — 학습자가 발음 점수에 매달리거나 자유 대화의 표현·흐름 연습 목적을 이해하지 못함 | ✅ 시작 화면 안내 배너 + 종합 화면 주의문 |

### 솔직한 효과 기대치 명시 (본 단계의 도구가 인지)

- **Verbatim prompt — 효과 불확실**: OpenAI Whisper 논문에 따르면 disfluencies/정규화는
  의도된 모델 행동. prompt 트릭은 학술 보고에서 "hit or miss". 큰 모델일수록 효과 ↓
  경향. 한국어 검증 사례 거의 없음. **실제 효과는 사용자 시연 필요.**
- **자유 대화 UI 안내 + 종합 화면 점수 — 효과 확실**: 학습자 기대치 조정과 진척도
  추적은 코드 변경만으로 즉시 가치.

---

## 1. 페이즈 0 — 진단

### 1.1 STT 호출 위치 + 현재 prompt

- **파일**: `src/providers/stt/index.ts` (단일 파일에 Mock/Whisper/OpenAITranscribe 3종 + factory).
- **gpt-4o-mini-transcribe**: `OpenAITranscribeSTTProvider` (line 75~). factory가
  `feature='free-conversation'`이면 `FREE_CONVERSATION_STT_PROVIDER`를 우선.
- **현재 prompt**: `KOREAN_DICTATION_PROMPT`
  > 학습자의 한국어 발화를 정확히 그대로 받아쓰세요. 문법 교정이나 자연스럽게 다듬지
  > 말고 발화 그대로 옮기세요. 다만 뉴스 앵커 멘트·유튜브 outro는 환각이므로 출력하지
  > 마세요.
- **한계**:
  - "정확히 그대로"는 있으나 "어색한 발음을 그대로 유지하라"는 명시적 신호 없음.
  - 한국어 채움말("음/그/어") 예시·실제 정정 사례 없음 → 발음 정정 차단 약함.
- **temperature**: 0 (변경 없음).

### 1.2 자유 대화 종료 화면

- **파일**: `app/student/conversation-practice/free-conversation-client.tsx:1495~`
  (`stage === 'end'`).
- **현재 카드**: 대화 요약 / 학습 피드백 / 대화 히스토리.
- **누락**: 발음 점수 정량 데이터 (평균·구간 등). 메시지별 `pronScore`는 line 1300~
  chat 본문에서 표시.
- **데이터**: `turns[].pronScore: number` 클라이언트 state에 부착 — 별도 fetch 불요.
- **pause 카운트**: `ChatTurn`에 미저장 — 본 단계 scope 외 (후속).

### 1.3 자유 대화 UI 안내 추가 위치

- **start substep 1** (주제 선택, line 894~) — 학습자가 처음 만나는 화면.
- 페르소나·chat 단계 중복 노출 불필요.

### 1.4 i18n 라벨 추가 위치

- `src/lib/i18n/dashboard-labels.ts` `PRACTICE_LABELS` (line 615~).
- 7개 언어 (ko/en/vi/ar/th/ms/km) `LabelMap`.

**산출**: `/tmp/단계19.18/diagnosis.md` (휘발성, 본 보고서로 보존).

**커밋**: `a220060 [단계19.18-페이즈0] STT prompt·종합 화면·UI 안내 진단`

---

## 2. 페이즈 1 — STT Verbatim Prompt 추가 (⚠ 효과 시연 필요)

### 2.1 신규 prompt

```
const KOREAN_VERBATIM_PROMPT =
  '학습자는 한국어를 배우는 비원어민입니다. ' +
  '들리는 발음 그대로 받아 적으세요. 어색한 발음, 머뭇거림, 채움말("음", "그", "어")을 그대로 유지하세요. ' +
  '정확한 표준어로 정정하지 마세요. ' +
  '예: "마시써요"는 "맛있어요"로 바꾸지 마세요. "항쿡"은 "한국"으로 바꾸지 마세요. "음... 그..."는 제거하지 마세요. ' +
  '다만 뉴스 앵커 멘트·유튜브 outro 같은 무관한 환각 텍스트는 출력하지 마세요.'
```

설계 의도 :
- **(a) 학습자 비원어민 컨텍스트** — 모델에게 정정 hook이 비원어민 학습자의 발음에는
  부적절함을 명시.
- **(b) 채움말 예시** — 한국어 disfluencies "음/그/어" 보존.
- **(c) 실제 정정 사례** — "마시써요" / "항쿡" 같은 시연에서 관찰된 정정을 negative
  example로 제시.

### 2.2 환경변수 토글 — 회귀 영향 최소

```
src/providers/stt/index.ts (신규)
function getActivePrompt(): string {
  const raw = process.env.STT_VERBATIM_PROMPT_ENABLED
  const enabled = typeof raw === 'string' && raw.toLowerCase() === 'true'
  return enabled ? KOREAN_VERBATIM_PROMPT : KOREAN_DICTATION_PROMPT
}
```

- **기본 OFF**: 시연 시에만 `.env.local`에 `STT_VERBATIM_PROMPT_ENABLED=true` 설정.
- **Whisper + gpt-4o-mini-transcribe + gpt-4o-transcribe 동일 적용**.
- **temperature=0 유지** (verbatim 토글과 무관).

### 2.3 단위 테스트 6개 추가

`tests/unit/openai-transcribe-provider.test.ts`:
1. 미설정이면 기본 prompt 전달 (verbatim 비적용).
2. `'false'` 설정 시 기본 prompt 그대로.
3. `'true'` 설정 시 verbatim prompt 전달 (채움말 + 정정 사례 + 비원어민 컨텍스트 포함).
4. 대소문자 무관 (`'TRUE'`·`'True'`도 verbatim).
5. whisper-1에도 동일 토글 적용.
6. temperature는 0 유지.

검증 결과: `21 passed` (기존 15 + 신규 6).

### 2.4 효과 검증 — 시연 가이드

⚠ **단위 테스트는 prompt 전달만 검증**. 실제 STT 출력 영향은 음성으로만 확정 가능.

**시연 시나리오**:

1. **준비**: `.env.local`에 `STT_VERBATIM_PROMPT_ENABLED=true` 추가 → `npm run dev` 재시작.
2. **자유 대화 진입** → 주제·페르소나 선택 → 발음 평가 토글 ON 확인.
3. **의도적 어색 발음 발화**:
   - "마시써요" (의도) → 음성으로 발화.
   - "항쿡 음쒹 마시쒀요" (의도) → 음성으로 발화.
   - "음... 그... 김... 그... 김 밥" (멈춤 의도) → 음성으로 발화.
4. **STT 인식 결과 관찰**:
   - 기대: "마시써요"가 "맛있어요"로 정정되지 않음, 채움말 "음/그" 보존.
   - 결과가 여전히 정정되면 → verbatim prompt가 효과 없음 (모델 자체 한계).
5. **Before/After 비교** (선택):
   - `.env.local`에서 `STT_VERBATIM_PROMPT_ENABLED=false` (또는 줄 삭제) → 동일 발화로
     비교.

**커밋**: `31fe7a3 [단계19.18-페이즈1] STT verbatim prompt 추가 (효과 미검증, 시연 필요)`

---

## 3. 페이즈 2 — 자유 대화 UI 안내 + 종합 화면 점수

### 3.1 자유 대화 시작 화면 안내 배너

**위치**: `app/student/conversation-practice/free-conversation-client.tsx` start substep 1
(주제 선택 화면 제목 + 진행도 아래).

**내용**:
```
💡 이 활동은 표현과 대화 흐름을 연습하는 단계입니다.
   정확한 발음 평가는 "읽기연습"과 "발표연습"에서 진행됩니다.
```
- amber 배경 배너 (`bg-amber-50 border-amber-200`), `role="note"`.
- `data-testid="free-conv-purpose-notice"`.
- 한국어 본문 + mother_tongue 보조 (Localized + practice key).
- 닫기 버튼 없음 — 상시 표시 (학습자 기대치 일관 유지).

### 3.2 종합 화면 발음 점수 요약 카드

**컴포넌트**: `PronunciationScoreSummary({ turns, motherTongue })` (free-conversation-client
내부, line 190 근처).

**데이터**:
- `turns.filter(t => t.role==='student' && typeof t.pronScore==='number').map(t => t.pronScore)`.
- 별도 fetch·DB 조회 없음 (DB 누적은 단계 19.16부터 `research_assessments`에 저장 중).

**표시**:
- **평균** + **평가된 발화 수** (2-column grid).
- **구간별 분포** (4-row list, 한국어 본문 + mother_tongue 보조 inline):
  - `< 50` (50점 미만)
  - `50–69`
  - `70–84`
  - `≥ 85` (85점 이상)
- **주의문**: `⚠ 표현 연습용 점수입니다. 정식 발음 평가는 읽기·발표 연습에서 확인하세요.`

**빈 데이터**: 발음 평가 OFF·평가 실패 시 별도 안내 카드 (`conversation-pron-summary-empty`).

### 3.3 i18n 라벨 — 9 키 × 7 언어 = 63 항목

`src/lib/i18n/dashboard-labels.ts` `PRACTICE_LABELS`:
- `free_conv_purposeNotice`
- `free_conv_summaryPronTitle / Avg / Count / Distribution / BucketLow / BucketMid / BucketHigh / BucketTop / Notice / Empty`

언어 커버리지:
- **정식 번역**: ko / en / vi / ar.
- **임시 번역**: th / ms / km — 단계 23 검수 대상 (기존 합의된 임시 처리 패턴 동일).

### 3.4 스크린샷 — 첨부 불요 (사람 시연 시 직접 확인)

`data-testid` 부착 위치:
- `free-conv-purpose-notice` (시작 화면 배너)
- `conversation-pron-summary` (종합 화면 점수 카드)
- `pron-summary-avg-value` / `pron-summary-count-value`
- `pron-bucket-low` / `mid` / `high` / `top`
- `conversation-pron-summary-empty` (빈 데이터 안내)

**커밋**: `c603d5d [단계19.18-페이즈2] 자유 대화 UI 안내 + 종합 화면 점수 표시`

---

## 4. 페이즈 3 — 검증

### 4.1 자동 검증

| 항목 | 결과 |
|---|---|
| vitest | 57 files / **1173 passed** (1167 → 1173, +6 verbatim 토글 테스트) |
| ESLint | 0 errors (4 pre-existing warnings — scripts/ 미사용 변수, 본 단계 무관) |
| next build | OK (`/student/conversation-practice` 빌드 성공) |
| TypeScript | 신규 변경 파일 0 에러 (사전 존재 에러는 본 단계 무관) |
| Playwright `stage1916-respond-api-smoke` (관련) | **7/7 passed** |

### 4.2 관찰: 전체 API smoke의 사전 존재 실패 4건

- `tests/smoke/api-smoke.spec.ts`에서 LLM 출력 텍스트 regex 매칭 실패 4건:
  - `corrected_answer는 transcript를 복사하지 않음` (line 134)
  - `beginner 카페: 4개 미션 목표 모두 포함 시 완료 응답 반환` (line 562)
  - `"나이스 아메리칸 던지세요 이게 맞나요?" → language question 감지` (line 626)
  - `"아이스 아메리카노 주세요가 맞아요?" → 표현 확인 질문 처리` (line 650)
- 모두 `/api/evaluate-speaking` 또는 `/api/dialogue/respond` LLM 텍스트 비교 — **단계
  19.18 변경(STT prompt, free-conv UI, summary 점수)과 무관**.
- 마지막 api-smoke 수정은 2026-05-07 (`6650479 phase 10-e stabilize demo scoring…`). 본
  단계와 시간적·범위적 분리.
- **53 passed** / 4 LLM-text flaky failures (pre-existing).
- 본 단계 변경의 핵심 라우트 (`/api/conversation/free/respond`)는 `stage1916` 스모크
  7/7 모두 통과.

---

## 5. 미처리 후속 (1차 시험운영 후)

| # | 항목 | 우선순위 | 비고 |
|---|---|---|---|
| 1 | Naver Clova STT verbatim 옵션 PoC | B (1차 후) | gpt-4o-mini-transcribe verbatim prompt 효과가 미진하면 검토 |
| 2 | 자유 대화 pause 카운트를 `ChatTurn`에 저장 → 종합 화면 노출 | C | `flowCtx.longPauseCount + shortPauseCount`를 turn에 부착하는 작은 변경 |
| 3 | 자유 대화 발음 평가의 본질적 한계 (L2 ASR · CrisperWhisper 등) | 장기 | 학술 검토 |
| 4 | th/ms/km 임시 번역 정식 검수 | 단계 23 | i18n_review 워크플로우 |
| 5 | api-smoke.spec.ts LLM-text 4개 실패 회복 | C | 본 단계와 무관, prompt 갱신·regex 완화 등 별도 작업 |
| 6 | 단계 19.17 외 누적 사항 | — | 단계 19.17 보고서의 미처리 항목 참조 |

---

## 6. 변경 요약

```
3 commits, +405 lines, -2 lines

a220060  페이즈 0  empty commit (진단 산출)
31fe7a3  페이즈 1  src/providers/stt/index.ts             +26 -2
                  tests/unit/openai-transcribe-provider   +68
c603d5d  페이즈 2  src/lib/i18n/dashboard-labels.ts       +105
                  app/student/conversation-practice/
                    free-conversation-client.tsx          +206
```

---

## 7. 시연 가이드 (사용자용)

### 7.1 Verbatim prompt 효과 측정

1. `.env.local`에 추가:
   ```
   STT_VERBATIM_PROMPT_ENABLED=true
   ```
2. `npm run dev` 재시작.
3. 학생 로그인 → 자유 대화 → 임의 주제·페르소나 → 발음 평가 ON.
4. 의도적 어색 발음 발화: "마시써요", "항쿡 음쒹", "음... 그... 김밥".
5. chat 본문에서 STT 인식 결과 확인:
   - 정정되면 → verbatim 효과 미진 → 후속 항목 1(Naver Clova PoC) 진행.
   - 그대로 유지되면 → 효과 확인됨. 이후 1차 시험운영 본격 적용 검토.
6. 비교 (선택): `.env.local`의 `STT_VERBATIM_PROMPT_ENABLED` 줄 삭제 → 재시작 → 동일
   발화로 before 확인.

### 7.2 자유 대화 UI 안내·종합 화면 점수 확인

1. 학생 로그인 → 자유 대화 진입.
2. **확인 A**: 주제 선택 화면 상단에 amber 배너 "💡 이 활동은 표현과 대화 흐름을 연습하는
   단계입니다…" 노출. 모국어 등록 학습자는 보조 표기 작게 함께.
3. 임의 주제 + 페르소나 + 3분 대화 (발음 평가 ON, 적어도 학습자 턴 2회 이상).
4. 대화 종료 → 종합 화면.
5. **확인 B**: "대화 요약" 카드 위에 **발음 점수 요약** 카드:
   - 평균 점수 (e.g., 72/100)
   - 평가된 발화 수 (e.g., 5개)
   - 구간별 분포 (50 미만 / 50–69 / 70–84 / 85+)
   - 하단 "⚠ 표현 연습용 점수입니다…" 주의문.
6. **확인 C**: 발음 평가 토글 OFF로 동일 시나리오 → 종합 화면에서 "이번 대화에서는 발음
   평가가 활성화되지 않았거나…" 빈 데이터 카드 노출.

---

## 8. 결론

- 단계 19.17 후 발견된 자유 대화 정체성·종합 화면 누락 2건은 **코드 변경만으로 즉시
  가치 확보** (UI 안내·점수 요약). 학습자가 발음 점수에 매달리지 않고 본인 진척도를
  대화 종료 직후 확인할 수 있다.
- STT verbatim prompt는 **시도 가치(비용 0)는 있으나 효과는 시연으로만 확정 가능**.
  도구는 prompt 전달 자동화·환경변수 토글·단위 테스트까지 완료. 본 단계 보고서는
  "STT 정확도 개선" 같은 단정 표현을 의도적으로 사용하지 않으며, 사용자 시연 후
  Naver Clova PoC 진행 여부를 결정한다.
- 1차 시연 운영 일정에 영향 없음 (회귀 없음, 신규 1173 통과).
